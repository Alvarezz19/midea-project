# 能力边界说明

更新时间：2026-05-07

本文说明当前系统适合做什么、不适合承诺什么，以及哪些修改必须由工程师确认。本文不是技术实现说明，而是用于对齐项目验收和使用边界。

## 一句话定位

当前系统更适合做“基于历史模板的楼控工程改造副驾”，不适合承诺“通过自然语言对话从零自动生成完整 AHU 或机房群控工程”。

系统的核心价值是：

- 根据用户需求，从历史工程模板中推荐相近基底。
- 帮助梳理需求缺口，提示需要补充的设备、通讯、点位和保护信息。
- 理解当前工程 JSON 的页面、节点、参数和局部上下游关系。
- 对现有工程做受控的局部修改，例如改名、改设定值、改点位、接线、复制成熟功能块。
- 在修改前做 dry-run、diff、结构校验和风险提示。
- 对中高风险修改要求人工确认，避免直接改坏工程。

## 适合使用的场景

### 1. 模板推荐和需求初筛

适合输入：

```text
我要做 AHU，包含直膨机、排风机、CO2 控制、新风阀和 Modbus 通讯。
```

系统可以输出：

- 推荐哪个历史模板作为基底。
- 模板已覆盖哪些需求。
- 哪些需求可能缺失。
- 后续需要确认哪些设备数量、点位和保护要求。

### 2. 当前工程理解和定位

适合输入：

```text
水泵控制里的比较判断在哪里？
旁通阀压差设定相关节点有哪些？
刚才修改影响了哪些节点？
```

系统可以输出：

- 候选页面和节点。
- 节点类型、名称、关键参数。
- 局部上游和下游摘要。
- 本次修改影响的节点列表。

当前系统能做的是“局部工程理解”，不是完整控制策略审图。对于复杂联锁、保护链路和设备动作顺序，仍需要工程师复核。

### 3. 明确目标的局部修改

适合输入：

```text
把水泵控制里的比较判断改名为 水泵运行台数判断。
把旁通阀压差设定值改为 45。
把节点 22a27d1 的物理输入通道改为扩展模块 1、通道 31。
```

系统可以执行：

- 节点改名。
- 修改已有参数或常量。
- 修改白名单内的 IO/通讯点位字段。
- 生成修改前后 diff。
- 创建新的工程版本，不覆盖原版本。

### 4. 成熟功能块复用

适合输入：

```text
复制旁通阀控制功能块到旁通阀控制页面，暂不接外部线。
把已有过滤网报警逻辑复制到新页面，先给我看边界。
```

系统可以执行：

- 从功能块索引中找到相似功能块。
- 复制功能块内部节点和内部连线。
- 重写复制节点 id。
- 默认断开外部入口和出口连线。
- 生成入口、出口和边界接线预览。
- 提示 BACnet 对象号复用、点位映射和外部接线风险。

功能块复制后，外部线、点位和设备绑定必须由工程师确认。

## 不适合直接承诺的场景

### 1. 从一句话自动生成完整工程

不应承诺：

```text
用户说“做一个 AHU 程序”，系统就自动生成完整可交付 JSON。
```

原因：

- AHU 和机房群控涉及设备选型、点表、保护联锁、通讯协议、现场接线和控制策略。
- 当前资料不足以覆盖所有项目差异。
- 大模型可能生成结构合法但控制逻辑错误的 JSON。
- 楼控工程中保护、联锁、点位错误会带来实际设备风险。

### 2. 从零设计复杂功能块并直接落盘

不应承诺：

```text
帮我设计一套 CO2 控制功能块，自动接入新风阀、报警、联锁和通讯输出。
```

当前系统可以辅助生成设计草案、推荐相似功能块或规划局部补丁，但不能保证从零设计的功能块完整可靠。

如果要支持这类能力，需要先建立功能块规格、输入输出边界、点位要求、保护要求和验收规则。

### 3. 自动删除或旁路保护逻辑

不应自动执行：

```text
把防冻保护删掉。
把故障联锁断开。
保护先不要了。
```

这类需求必须按高风险处理。系统应提示风险、要求人工确认，必要时拒绝自动生成可应用补丁。

### 4. 自动决定现场点位和通讯地址

不应由系统凭空决定：

- 硬接 IO 通道。
- Modbus 地址和寄存器。
- BACnet 对象号。
- MQTT topic。
- 设备编号和现场接线关系。

系统可以修改用户明确给出的点位字段，但点表一致性必须由工程师复核。

## 风险分级原则

### 低风险

通常包括：

- 节点改名。
- 备注新增。
- 明确目标上的普通参数修改。
- 新增空页面。

低风险修改可以在 dry-run 和校验通过后自动创建新版本。

### 中风险

通常包括：

- 新增节点。
- 启用动态输入端口。
- 新增或调整连线。
- 新增设定值并接入比较判断或 PID。

中风险修改应展示计划、diff 和影响节点，建议人工确认后应用。

### 高风险

通常包括：

- 复制功能块。
- 修改 IO/通讯点位。
- 断开连线。
- 修改设备数量。
- 涉及保护、故障、反馈、联锁、防冻、水流、急停等链路。
- 删除节点、删除功能块或删除页面。

高风险修改必须人工确认。系统应说明影响范围、风险原因和需要现场复核的内容。

## 当前系统能操作的 JSON 修改

系统当前通过结构化补丁修改工程 JSON，主要支持：

- `rename_node`：节点改名。
- `update_param`：修改已有参数，不新增未知字段。
- `replace_constant`：修改常量、软件输入或比较阈值等设定值。
- `add_tab`：新增空页面。
- `add_comment`：新增备注。
- `add_node_from_schema`：按 schema 新增节点。
- `enable_dynamic_input`：启用 compare、PID、linear 等支持的动态输入端口。
- `connect`：新增连线。
- `disconnect`：断开连线。
- `copy_block`：复制成熟功能块内部节点和内部连线。
- `set_io_point`：修改白名单内 IO/通讯点位字段。

这些操作是底层编辑能力，不等同于完整业务设计能力。

## 推荐交付方式

建议将项目验收目标定义为：

```text
基于历史模板的工程理解、需求澄清、成熟功能块复用和安全局部改造。
```

不建议将当前阶段验收目标定义为：

```text
自然语言自动生成完整 AHU/机房群控工程 JSON。
```

更合理的验收样例应包含：

- 原始工程 JSON。
- 用户自然语言修改需求。
- 工程师期望修改后的 JSON 或 diff。
- 修改说明。
- 风险确认要求。
- 不应自动执行的危险操作清单。

## 需要美的工程师提供的资料

为了提升系统能力，需要逐步补充：

- 典型 AHU 和机房群控项目的需求样例。
- 手工修改前后的工程 JSON 对照。
- 成熟功能块清单及其适用场景。
- 功能块输入、输出、点位、参数和保护边界说明。
- 点表命名规范、设备编号规范、通讯地址规范。
- 防冻、故障、反馈、启停顺序、联锁等保护规则。
- 项目验收时必须检查的风险项。

资料越完整，系统越能从“局部修改工具”升级为“工程改造副驾”。在资料不足时，系统应优先追问和提示风险，而不是直接生成复杂工程。

## 使用建议

推荐使用方式：

1. 先让系统推荐模板。
2. 再让系统列出已覆盖和缺失需求。
3. 对缺失功能优先查找相似成熟功能块。
4. 复制功能块时先查看边界预览。
5. 明确点位、参数、入口和出口后再应用补丁。
6. 对保护、联锁、通讯和 IO 相关变更进行人工确认。
7. 导出前查看结构校验和需求覆盖报告。

当前系统的正确定位不是替代楼控工程师，而是帮助工程师减少查找、复制、定位、改参数、看 diff 和风险检查的重复工作。

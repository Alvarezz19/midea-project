# 智能体系统整体架构设计

更新时间：2026-04-27

## 1. 结论

本项目建议采用“前端工程配置界面 + Python 后端服务 + LangGraph 智能体编排 + 结构化 JSON 工具链 + 模板/知识检索库”的架构。

LangGraph 适合本项目，但不应把它当成“一个大模型聊天封装”。它更适合作为有状态、多步骤、可中断、可回放的工作流编排层，用来组织需求澄清、模板选择、局部加载、差异改造、校验、用户确认和导出这些步骤。LangGraph 官方文档将其定位为低层级的智能体编排框架，重点能力包括持久执行、流式输出、人工参与、短期/长期记忆和调试可观测性，这与本项目“用户持续对话并迭代工程 JSON”的需求匹配。

核心设计原则：

1. 不把完整工程 JSON 每轮塞进 LLM 上下文。
2. LLM 负责理解需求、选择修改策略、生成结构化修改意图。
3. 程序负责读取 JSON、定位节点、应用补丁、校验 schema、导出结果。
4. 会话状态只保留用户需求摘要、已选模板、修改计划、差异摘要和必要局部片段。
5. 模板和知识库采用“渐进式披露、按需加载”的方式，先检索摘要，再加载相关片段，必要时才加载完整局部子图。

参考的官方文档：

- LangGraph Overview: https://docs.langchain.com/oss/python/langgraph/overview
- LangGraph Persistence: https://docs.langchain.com/oss/python/langgraph/persistence
- LangGraph Memory: https://docs.langchain.com/oss/python/langgraph/add-memory
- LangGraph Interrupts: https://docs.langchain.com/oss/python/langgraph/human-in-the-loop

## 2. 推荐技术栈

### 2.1 后端

建议优先使用 Python：

- Web 框架：FastAPI
- 智能体编排：LangGraph
- LLM 接入：LangChain model interface 或直接封装模型 SDK
- JSON 处理：标准 `json`、`jsonschema`、`jsonpatch` 或自定义 AST patch 工具
- 向量检索：Chroma、Qdrant、pgvector 三选一
- 关系存储：PostgreSQL，早期原型可用 SQLite
- 后台任务：FastAPI BackgroundTasks、Celery 或 Dramatiq
- 观测调试：LangSmith 可选，至少保留本地 trace 日志

选择 Python 的原因：

- LangGraph Python 生态更成熟，和当前官方文档示例一致。
- JSON 模板、schema、向量索引、校验工具都容易实现。
- 后续如果要接入脚本化批处理、自动评测和离线索引，Python 更方便。

### 2.2 前端

建议使用 React 或 Vue。当前项目没有已有前端代码时，推荐：

- React + Vite + TypeScript
- UI 组件库：Ant Design、Arco Design 或 shadcn/ui
- 图形查看：React Flow，用于展示流程节点、局部子图和变更前后差异
- JSON 查看：Monaco Editor 或 CodeMirror

前端不应该只是聊天框。建议首屏就是工程生成工作台：

- 顶部：项目类型切换，机房群控程序/AHU 程序
- 左侧：对话和需求清单
- 中间：当前方案、模板匹配结果、变更说明
- 右侧：局部流程图、JSON diff、校验结果
- 底部：确认应用、撤销、导出 JSON

### 2.3 数据存储

建议拆成四类存储：

| 数据类型 | 推荐存储 | 内容 |
| --- | --- | --- |
| 会话状态 | LangGraph checkpointer + PostgreSQL/SQLite | thread_id、短期记忆、当前阶段、待确认动作 |
| 工程版本 | PostgreSQL 或文件对象存储 | 原始模板、当前工程 JSON、版本快照、diff |
| 检索索引 | 向量库 + 关键词索引 | 模板摘要、节点摘要、schema 摘要、知识片段 |
| 规则知识 | Markdown/JSON + 向量库 | 控制策略、命名规范、模块用法、行业规则 |

## 3. 整体架构

```text
用户浏览器
  |
  | HTTP / WebSocket
  v
FastAPI 后端
  |
  | 调用
  v
LangGraph 编排层
  |
  |-- 需求理解节点
  |-- 澄清问题节点
  |-- 模板检索节点
  |-- 模板摘要/局部加载节点
  |-- 修改规划节点
  |-- JSON 补丁执行节点
  |-- schema/拓扑校验节点
  |-- 用户确认 interrupt 节点
  |-- 导出节点
  |
  +--> 模板索引库
  +--> 模块 schema 库
  +--> 控制知识库
  +--> 工程版本库
```

### 3.1 LangGraph 在系统中的位置

LangGraph 负责“流程状态机 + 智能体决策”，不直接负责所有业务逻辑。

适合放进 LangGraph 的内容：

- 当前用户处于什么阶段：选择项目类型、补充需求、选择模板、局部修改、确认导出。
- 多轮对话记忆：用户已经确认的需求、禁止变更点、偏好模板。
- 中断等待用户确认：例如“是否用风冷热泵标准模板作为基底”。
- 出错后的恢复：修改失败或校验失败时回到修改规划节点。
- 版本回放和对比：基于 checkpoint 和工程版本快照追踪每次改造。

不建议放进 LangGraph 的内容：

- 大 JSON 全量对象长期塞在 graph state 中。
- 复杂 JSON 遍历、节点定位、连线修复逻辑完全交给 LLM。
- 模板索引构建这种离线任务。

Graph state 中建议只存轻量引用：

```python
class AgentState(TypedDict):
    messages: list
    project_type: Literal["plant_room", "ahu"] | None
    requirement_summary: dict
    selected_template_id: str | None
    current_project_version_id: str | None
    loaded_context_refs: list[str]
    pending_plan: dict | None
    validation_report: dict | None
```

完整 JSON 放在工程版本库，Graph state 只保存 `current_project_version_id`。

## 4. 推荐 LangGraph 流程

### 4.1 主流程节点

```text
START
  -> classify_project_type
  -> collect_requirements
  -> retrieve_template_candidates
  -> ask_template_confirmation
  -> load_template_summary
  -> plan_modification
  -> retrieve_relevant_json_fragments
  -> generate_patch_intent
  -> apply_patch_programmatically
  -> validate_project
  -> summarize_change_for_user
  -> wait_user_feedback
       | 用户满意 -> export_json -> END
       | 用户继续修改 -> collect_requirements
       | 校验失败 -> plan_modification
```

### 4.2 节点职责

| 节点 | 职责 | 是否调用 LLM |
| --- | --- | --- |
| `classify_project_type` | 判断或接收用户选择：机房群控/AHU | 可选 |
| `collect_requirements` | 把用户自然语言转成结构化需求清单 | 是 |
| `retrieve_template_candidates` | 在对应模板目录检索最相近模板 | 否，优先检索算法 |
| `ask_template_confirmation` | 展示候选模板和理由，等待用户确认 | 可用 interrupt |
| `load_template_summary` | 加载模板级摘要、tab 列表、模块统计 | 否 |
| `plan_modification` | 形成修改计划：新增/删除/改参/改线/改命名 | 是 |
| `retrieve_relevant_json_fragments` | 按计划加载相关 tab、节点邻域、schema | 否 |
| `generate_patch_intent` | 生成结构化补丁意图，不直接输出整包 JSON | 是 |
| `apply_patch_programmatically` | 程序化应用补丁，生成新版本 | 否 |
| `validate_project` | JSON 语法、schema、连线、引用、命名校验 | 否 |
| `summarize_change_for_user` | 用自然语言解释本次改动 | 是 |
| `wait_user_feedback` | 等待用户继续改或导出 | 可用 interrupt |
| `export_json` | 导出最终 JSON | 否 |

### 4.3 用户确认点

建议至少设置以下人工确认点：

1. 项目类型确认：机房群控程序或 AHU 程序。
2. 模板确认：展示最相近 1 到 3 个模板，并说明差异。
3. 高风险改动确认：删除节点、删除连线、改变设备数量、改变 IO 点表。
4. 导出确认：展示最终校验结果和主要变更摘要。

LangGraph 的 interrupt 能把图执行暂停并保存状态，用户确认后用同一个 `thread_id` 恢复。

## 5. 模板选择策略

当前仓库已有模板：

| 类型 | 文件 | 规模 |
| --- | --- | --- |
| AHU | `programs/AHU程序/三山经开区/flows_20251210190941.json` | 约 913 节点 |
| AHU | `programs/AHU程序/云霞县华龙科技/flows_20251229175702.json` | 约 956 节点 |
| AHU | `programs/AHU程序/泰安宁阳中医院/flows_20260206160555.json` | 约 992 节点 |
| 机房群控 | `programs/机房群控程序/20230707_机房群控示例程序{2主机+3冷冻冷却水泵+2冷却塔].json` | 约 2133 节点 |
| 机房群控 | `programs/机房群控程序/风冷热泵标准控制程序[风冷涡旋]20240905.json` | 约 3450 节点 |

模板选择不建议只做向量相似度。建议采用混合评分：

```text
score = 0.35 * 设备拓扑匹配
      + 0.25 * 控制功能匹配
      + 0.15 * IO/通讯方式匹配
      + 0.15 * 模板领域关键词相似度
      + 0.10 * 修改成本预估
```

示例特征：

- 机房群控：主机数量、水泵数量、冷却塔数量、是否风冷热泵、是否水冷机组、蝶阀/旁通阀/联动控制。
- AHU：新风/回风/排风、直膨机/水阀/电加热、CO2、湿度、过滤网、防冻、风机变频、定时启停。
- 通讯：Modbus、BACnet、MQTT、硬接 IO。
- 修改成本：需要新增节点数量、需要改动 tab 数量、需要重连的节点邻域数量。

## 6. 长 JSON 的上下文加载策略

### 6.1 采用“渐进式披露、按需加载”

可以借鉴 skills 的思想，但要工程化为固定层级：

1. 文件级索引：文件名、项目类型、tab 列表、节点数量、主要节点类型统计、摘要。
2. tab 级索引：tab 名称、包含的设备/控制环节、节点类型统计、关键输入输出。
3. 功能块级索引：围绕备注、命名、引用、IO、控制输出聚类形成“局部功能块”。
4. 节点级索引：节点 id、type、name、z、输入输出数量、wires、关键参数摘要。
5. 邻域加载：按节点 id 加载 N 跳上下游节点，通常 1 到 2 跳足够。
6. 完整片段加载：只有当修改需要重连或复制功能块时，才加载完整 JSON 子图。

LLM 默认只能看到前 3 层摘要。只有进入具体修改时，才加载第 4 到第 6 层。

### 6.2 不推荐的方式

不推荐：

- 每轮把完整 JSON 放进 prompt。
- 让 LLM 返回完整 JSON。
- 用正则字符串替换 JSON。
- 只靠向量库召回节点，不做拓扑邻域扩展。
- 只保存聊天记录，不保存结构化需求状态。

### 6.3 推荐的数据预处理

对每个模板生成这些派生文件或数据库表：

```text
indexes/
  templates/
    plant_room/*.template.json
    ahu/*.template.json
  tabs/
    *.tab.json
  nodes/
    *.node.jsonl
  blocks/
    *.block.jsonl
  embeddings/
```

模板摘要示例：

```json
{
  "template_id": "plant_room_air_source_heat_pump_20240905",
  "project_type": "plant_room",
  "source_path": "programs/机房群控程序/风冷热泵标准控制程序[风冷涡旋]20240905.json",
  "node_count": 3450,
  "tabs": ["使用配置", "硬接/调用信号", "通讯信号", "风冷热泵控制", "蝶阀控制", "水泵控制", "联动控制", "旁通阀控制"],
  "features": {
    "plant_type": "风冷热泵",
    "has_valve_control": true,
    "has_pump_control": true,
    "has_bypass_valve": true,
    "has_modbus_output": true
  },
  "summary": "适合风冷热泵机房群控，包含主机、蝶阀、水泵、联动、旁通阀和通讯信号相关逻辑。"
}
```

节点摘要示例：

```json
{
  "node_id": "abc123",
  "template_id": "plant_room_air_source_heat_pump_20240905",
  "tab_label": "水泵控制",
  "type": "compare",
  "name": "冷冻泵运行台数判断",
  "inputs": 1,
  "outputs": 1,
  "wires_to": ["def456"],
  "key_params": {"as": "ge", "tripPoint": 2},
  "text_for_embedding": "水泵控制 比较判断 冷冻泵运行台数 大于等于 2"
}
```

### 6.4 上下文预算建议

每次 LLM 调用建议控制为：

| 上下文内容 | 预算 |
| --- | --- |
| 系统提示和任务约束 | 10% |
| 用户需求摘要 | 15% |
| 已确认工程状态摘要 | 15% |
| 模板/知识检索摘要 | 20% |
| 局部 JSON 片段 | 30% |
| 输出格式约束 | 10% |

完整会话历史不应持续放入 prompt。应把历史压缩成结构化状态：

```json
{
  "confirmed_requirements": [
    "项目类型：AHU",
    "包含 CO2 浓度控制",
    "风机需要变频控制",
    "需要保留原有 Modbus 输出结构"
  ],
  "open_questions": [
    "送风温度设定值是否固定为 18 摄氏度"
  ],
  "rejected_changes": [
    "不要删除原模板中的防冻保护逻辑"
  ]
}
```

## 7. JSON 改造方式

### 7.1 让 LLM 生成“补丁意图”

LLM 不直接生成最终 JSON，而是生成类似下面的结构化补丁意图：

```json
{
  "intent": "modify_existing_logic",
  "target": {
    "tab": "水泵控制",
    "anchor_nodes": [
      {"match_by": "name", "value": "冷冻泵运行台数判断"}
    ]
  },
  "operations": [
    {
      "op": "update_param",
      "node_selector": {"type": "compare", "name_contains": "运行台数"},
      "params": {"tripPoint": 3}
    },
    {
      "op": "add_comment",
      "near": {"name_contains": "运行台数"},
      "text": "根据用户需求，将运行台数阈值调整为 3。"
    }
  ],
  "validation_expectations": [
    "compare.inputs 与 wires 输入数量一致",
    "被修改节点仍在原 tab 内",
    "不改变上游 IO 节点"
  ]
}
```

然后由后端 JSON 工具执行：

- 解析节点选择器。
- 检查是否唯一匹配。
- 应用参数更新。
- 自动维护 `inputs`、`outputs`、`wires`。
- 生成新版本。
- 输出 diff 和校验报告。

### 7.2 支持的操作类型

第一阶段建议只支持可控的操作集合：

| 操作 | 含义 |
| --- | --- |
| `update_param` | 修改节点参数 |
| `rename_node` | 修改节点名称 |
| `add_node_from_schema` | 根据 `schemas` 模块模板新增节点 |
| `copy_block` | 从模板中复制局部功能块 |
| `connect` | 新增连线 |
| `disconnect` | 删除连线 |
| `add_comment` | 新增说明备注 |
| `replace_constant` | 修改常量值或设定值 |
| `enable_dynamic_input` | 打开模块动态输入端口并修复 wires |

暂缓支持：

- 大范围自动重构。
- 跨 tab 大规模搬迁。
- 自动删除大量未知节点。
- 没有 schema 支撑的新模块自由生成。

### 7.3 校验层

校验分四级：

1. JSON 语法校验：能否解析，是否仍是数组结构。
2. 节点字段校验：对照 `schemas/*/*.json` 的 `parameters_schema`。
3. 拓扑校验：`inputs`、`outputs`、`wires` 长度一致，连线目标 id 存在。
4. 工程规则校验：对象名唯一、IO 点位不冲突、通讯点表不重复、关键保护逻辑未被删除。

校验失败时不直接交给用户，而是回到 `plan_modification` 或 `generate_patch_intent` 节点修复。

## 8. 记忆设计

### 8.1 短期记忆

短期记忆用于一个工程生成会话。建议保存：

- 原始对话消息的最近几轮。
- 结构化需求摘要。
- 当前模板和版本 id。
- 待确认问题。
- 最近一次变更计划和校验报告。

LangGraph 官方建议用 checkpointer 作为线程级持久化能力，生产环境应使用数据库型 checkpointer，而不是内存型。

### 8.2 长期记忆

长期记忆用于跨会话积累：

- 用户偏好：常用控制策略、命名习惯、导出格式。
- 已确认项目案例：项目类型、最终模板、关键变更。
- 领域知识沉淀：常见 AHU 控制策略、机房群控策略。

长期记忆不能替代工程版本库。完整 JSON 仍应作为版本文件或对象存储管理。

## 9. 是否需要额外知识库

需要。现有完整工程模板是“可复制的工程样例”，但不足以支撑稳定生成。建议至少补充三类知识。

### 9.1 模块 schema 知识库

当前 `schemas` 目录已经具备很好的基础，应继续完善：

- `description` 和 `keywords` 用于检索。
- `parameters_schema` 用于参数合法性。
- `ports_definition` 用于自动连线。
- `template_json` 用于新增节点。

这类知识是“生成合法节点”的基础。

### 9.2 控制策略知识库

需要把工程师经验写成可检索的文档，例如：

- AHU 新风阀、回风阀、排风机联动策略。
- AHU 防冻保护、过滤网报警、CO2 控制、湿度控制。
- 机房群控主机加减载策略。
- 水泵轮值、故障切换、最小运行时间。
- 冷却塔启停、旁通阀控制、压差控制。
- 风冷热泵和水冷机组的策略差异。

这类知识用于“知道该怎么改”，不是直接生成节点。

### 9.3 工程规范知识库

需要补充项目内规范：

- 节点命名规范。
- tab 划分规范。
- IO 点表命名和地址规则。
- Modbus/BACnet/MQTT 字段规范。
- 保护逻辑不可删除清单。
- 导出前检查清单。

这类知识用于降低工程风险。

## 10. 离线索引构建流程

建议建立一个离线索引命令：

```powershell
conda activate midea
python scripts\build_indexes.py
```

索引步骤：

1. 扫描 `programs/机房群控程序` 和 `programs/AHU程序`。
2. 解析每个 JSON 文件。
3. 识别 tab 节点和普通节点。
4. 统计节点类型、tab、连线、关键参数。
5. 根据 comment、name、wires 邻域聚类出功能块。
6. 生成模板摘要、tab 摘要、节点摘要和功能块摘要。
7. 写入数据库和向量库。
8. 运行一致性校验并输出索引报告。

## 11. API 设计草案

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/api/sessions` | POST | 创建会话，返回 `thread_id` |
| `/api/sessions/{thread_id}/message` | POST | 发送用户消息，驱动 LangGraph |
| `/api/templates/search` | POST | 按项目类型和需求检索模板 |
| `/api/projects/{project_id}/versions` | GET | 查看工程版本 |
| `/api/projects/{project_id}/diff` | GET | 查看版本差异 |
| `/api/projects/{project_id}/validate` | POST | 手动触发校验 |
| `/api/projects/{project_id}/export` | GET | 导出最终 JSON |

流式交互建议使用 WebSocket 或 Server-Sent Events：

- LLM 正在分析需求。
- 正在检索模板。
- 找到候选模板。
- 正在应用补丁。
- 校验通过或失败。
- 等待用户确认。

## 12. 分阶段落地路线

### 第一阶段：可用原型

目标：能从两类模板中选择一个，做少量参数和命名修改，并导出 JSON。

工作项：

1. 建立 FastAPI 服务。
2. 建立 LangGraph 主流程。
3. 建立模板文件级和 tab 级索引。
4. 实现结构化需求摘要。
5. 实现 `update_param`、`rename_node`、`add_comment`。
6. 实现 JSON 基础校验和导出。

### 第二阶段：局部子图改造

目标：能新增节点、复制功能块、修改连线。

工作项：

1. 建立节点级索引和邻域加载工具。
2. 对接 `schemas` 生成节点。
3. 实现 `add_node_from_schema`、`connect`、`disconnect`。
4. 加入拓扑校验。
5. 前端展示局部流程图和 diff。

### 第三阶段：工程策略知识库

目标：能根据专业控制需求提出合理改造方案。

工作项：

1. 编写 AHU 控制策略知识库。
2. 编写机房群控控制策略知识库。
3. 编写工程规范和导出检查清单。
4. 将知识库纳入混合检索。
5. 加入自动评测案例。

### 第四阶段：生产化

目标：支持多人、多项目、可回滚、可审计。

工作项：

1. PostgreSQL checkpointer 和工程版本库。
2. 用户权限和项目管理。
3. LangSmith 或本地 trace 观测。
4. 导出审计报告。
5. 回滚和分支版本。

## 13. 风险与控制措施

| 风险 | 控制措施 |
| --- | --- |
| LLM 幻觉生成不存在字段 | 用 schema 约束和程序化补丁执行 |
| 长 JSON 超上下文 | 分层索引、按需局部加载、Graph state 只存引用 |
| 连线被错误破坏 | 拓扑校验、变更前后 diff、用户确认 |
| 模板选择错误 | 混合评分 + 候选模板解释 + 用户确认 |
| 删除保护逻辑 | 建立不可删除规则和高风险变更 interrupt |
| 多轮需求丢失 | 结构化需求摘要 + checkpointer 持久化 |
| 修改不可追踪 | 工程版本快照 + patch 记录 + 校验报告 |

## 14. 最小可行实现建议

如果现在开始编码，建议先做这条最短路径：

1. 离线扫描模板，生成 `template_index.json`。
2. 用户选择机房群控或 AHU。
3. 系统用需求和 `template_index.json` 匹配 1 到 3 个模板。
4. 用户确认模板后复制为一个工程版本。
5. 对话中只允许做参数修改、节点改名、添加备注。
6. 所有修改用结构化 patch 执行。
7. 校验通过后导出 JSON。

这样可以最快验证产品闭环，同时为后续“新增节点、改连线、复制功能块”打基础。


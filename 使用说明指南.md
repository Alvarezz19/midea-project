# 使用说明指南

更新时间：2026-05-01

本文面向第一次接触本项目的工程师，说明项目能做什么、如何启动、如何通过工作台完成工程 JSON 改造，以及常用 API、运行时文件和验收命令。

## 1. 项目是什么

本项目是“美的工程 JSON 智能体原型”。它的目标不是让大模型直接重写整份工程 JSON，而是把工程改造拆成可追踪、可校验、可回滚的步骤：

1. 从 `programs/` 原始模板库中检索合适模板。
2. 用 LangGraph 编排需求分析、模板确认、补丁规划、风险确认和校验流程。
3. 用确定性服务读取、复制、修改、校验工程 JSON。
4. 用结构化补丁描述局部改造，成功后创建新的工程版本。
5. 通过 FastAPI API 和 React 工作台完成多轮理解、设计摘要、受控改造、局部图高亮、需求覆盖门禁和导出验证。

当前主要支持两类工程：

- 机房群控程序。
- AHU 程序。

## 2. 核心概念

| 概念 | 说明 |
| --- | --- |
| 模板 | `programs/` 下的原始工程 JSON，只作为基底，不应被运行时覆盖。 |
| 索引 | `indexes/` 下由 `scripts/build_indexes.py` 生成的模板、页面、功能块、节点索引。 |
| 会话 | 一次需求交互流程，对应 `thread_id`。文件型存储时写入 `projects/sessions/`。 |
| 项目 | 从模板创建出的运行时工程实例，对应 `project_id`。 |
| 版本 | 每次模板创建、补丁应用、回滚指针切换都会围绕版本工作。版本文件默认在 `projects/versions/{project_id}/`。 |
| 补丁 | 修改工程 JSON 的结构化操作，如重命名节点、替换常量、复制功能块、修改 IO 点位。 |
| dry-run | 只执行补丁模拟和校验，不保存文件。中高风险操作必须先 dry-run 并人工确认。 |
| 设计摘要 | 模板选择后的工程师可审阅中间产物，包含设备、控制、点表、保护、需改造项和风险项。 |
| 需求覆盖报告 | 导出前的需求满足性复核；有需求上下文时明显缺失会阻塞导出。 |
| Trace / 事件 | 阶段 7 可观测性数据，记录 API、工作流步骤、LLM 调用、反馈和指标。 |

## 3. 环境准备

项目在 Windows + Conda 环境下开发。新的终端会话中，先进入项目根目录并激活环境：

```powershell
cd D:\yjsproject\midea-project
conda activate midea
```

安装 Python 依赖：

```powershell
python -m pip install -e ".[dev]"
```

如需使用 PostgreSQL 运行时存储或 LangGraph PostgreSQL checkpointer，再安装：

```powershell
python -m pip install -e ".[postgres]"
```

前端依赖位于 `frontend/`：

```powershell
cd frontend
npm install
cd ..
```

## 4. 配置文件

本地配置使用 `.env`，可从 `.env.example` 复制：

```powershell
Copy-Item .env.example .env
```

常用配置：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `PROJECT_VERSIONS_DIR` | `projects/versions` | 工程版本 JSON 输出目录。 |
| `PROJECT_SESSIONS_DIR` | `projects/sessions` | 文件型会话状态目录。 |
| `RUNTIME_STORE_BACKEND` | `file` | 运行时存储后端，可设为 `postgres`。 |
| `LANGGRAPH_CHECKPOINTER_BACKEND` | `memory` | LangGraph checkpoint 后端，可设为 `postgres`。 |
| `DATABASE_URL` | 空 | PostgreSQL 连接串。 |
| `LLM_PROVIDER` | `deepseek` | LLM 供应商。 |
| `DEEPSEEK_API_KEY` | 空 | 使用 DeepSeek 需求抽取或 LLM planner 时需要。 |

没有 LLM key 时，仍可使用模板检索、规则 planner、结构化补丁、校验和导出等能力；复杂结构改造会返回需要补充页面、节点、端口或点位信息的追问。开启真实 LLM planner 或真实 LLM 测试时才需要配置密钥。

## 5. 构建索引

模板、页面、功能块、节点检索依赖 `indexes/`。修改 `programs/` 模板后需要重新生成：

```powershell
conda activate midea
python scripts\build_indexes.py
```

生成结果包括：

- `indexes/templates/template_index.json`
- `indexes/tabs/tab_index.jsonl`
- `indexes/blocks/block_index.jsonl`
- `indexes/nodes/node_index.jsonl`

## 6. 启动系统

### 6.1 启动后端和内置工作台

```powershell
conda activate midea
uvicorn app.main:app --reload
```

打开：

```text
http://127.0.0.1:8000/
```

FastAPI 根路径默认托管 `app/static/workbench/` 中的阶段 7 React 工作台。旧静态原型保留在：

```text
http://127.0.0.1:8000/legacy
```

### 6.2 前端开发模式

如果需要修改 React 前端源码：

```powershell
conda activate midea
cd frontend
npm run dev
```

前端开发服务默认监听 `127.0.0.1`。生产构建输出到 `app/static/workbench/`：

```powershell
cd frontend
npm run build
cd ..
```

## 7. 工作台使用流程

工作台是新人最推荐的入口。主页面分为会话需求、模板计划、局部图校验，以及底部版本/Trace/反馈抽屉。

### 7.1 创建会话

1. 在顶部选择项目类型：`机房群控程序` 或 `AHU 程序`。
2. 点击左侧“新建”创建会话。
3. 在输入框描述需求，例如：

```text
我要做 AHU 程序，需要直膨机、排风机、CO2 控制、新风阀和 Modbus 通讯。
```

系统会生成结构化需求摘要，并在信息不足时展示澄清问题。

### 7.2 确认模板

右侧“模板候选”会展示：

- 匹配分。
- 节点数和页面数。
- 匹配项、缺失项。
- 预计改造成本。
- 风险点。
- 设计摘要，包括设备计划、控制计划、点表计划、保护计划和导出前门禁提示。

确认合适模板后，系统会从 `programs/` 创建项目初始版本，不会覆盖原始模板。

### 7.3 继续描述局部改造

模板确认后，可以继续输入改造需求，例如：

```text
把送风温度设定值改为 24。
```

或：

```text
把节点 1154c92 改名为 演示-AHU比较节点。
```

系统会生成结构化计划，并执行 dry-run。低风险计划可自动应用；中高风险计划会停在“高风险确认”区域，需要人工确认。

### 7.4 确认或取消风险操作

当出现待确认补丁时：

- 点击“确认应用”：从当前版本创建新的子版本。
- 点击“取消”：不改变当前版本。

常见中高风险操作包括：

- 启用动态输入端口。
- 新增节点并连线。
- 复制功能块。
- 修改 IO / 通讯点位。
- 断开保护相关链路。

### 7.5 查看局部流程图和 diff

“局部流程图与校验”区域会展示当前版本摘要图：

- 可点击 diff 节点定位影响范围。
- 可查看新增、删除、修改节点。
- `copy_block` dry-run 会显示入口/出口边界预览。
- 图只加载预算内的摘要数据，不拉取完整工程 JSON。

### 7.6 校验和导出

点击顶部“校验”会调用后端校验当前版本。只有 `error` 级问题为 0 时才允许导出。

点击“导出”会再次校验目标工程：

- 校验通过：下载当前版本 JSON。
- 校验失败：拒绝导出，并返回阻塞原因。
- 找不到需求上下文时：仍会做结构校验，但需求覆盖报告会标记“需求覆盖未复核”。

注意：如果用户明确要求防冻、过滤网报警、旁通压差等需求，而当前模板中找不到可证明证据，需求覆盖门禁可能会阻塞导出。这是预期行为，不是工作台故障。

### 7.7 版本、Trace 和反馈

点击右下角“版本 / Trace / 反馈”打开底部抽屉：

- `版本历史`：查看版本列表、任意版本对比、回滚到历史版本。
- `Trace`：查看当前 `thread_id`、`trace_id`、`project_id`、`version_id`。
- `事件流`：查看 SSE 工作流事件。
- `指标`：查看事件、Trace、LLM 调用、成本和趋势。
- `反馈`：给当前 trace 或项目提交评分和备注。

回滚只切换当前版本指针，不删除历史版本文件。

## 8. 常用 API

后端入口为 `app/main.py`。启动后可访问 OpenAPI：

```text
http://127.0.0.1:8000/docs
```

常用接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/health` | 健康检查。 |
| `POST` | `/api/sessions` | 创建会话。 |
| `GET` | `/api/sessions/{thread_id}/events` | 读取 SSE 工作流事件。 |
| `POST` | `/api/sessions/{thread_id}/message` | 发送需求或确认模板。 |
| `POST` | `/api/sessions/{thread_id}/patch-confirmation` | 确认或取消待确认补丁。 |
| `POST` | `/api/templates/search` | 检索模板。 |
| `POST` | `/api/blocks/search` | 检索功能块。 |
| `POST` | `/api/blocks/context` | 加载功能块局部上下文。 |
| `POST` | `/api/knowledge/search` | 检索知识库。 |
| `POST` | `/api/requirements/extract` | LLM 结构化需求抽取。 |
| `POST` | `/api/planner/plan` | 补丁规划。 |
| `POST` | `/api/planner/dry-run` | 补丁 dry-run。 |
| `GET` | `/api/projects/{project_id}/versions` | 获取版本历史。 |
| `GET` | `/api/projects/{project_id}/diff` | 获取版本 diff。 |
| `GET` | `/api/projects/{project_id}/versions/{version_id}/flow` | 获取局部流程图摘要。 |
| `POST` | `/api/projects/{project_id}/validate` | 校验当前项目版本。 |
| `POST` | `/api/projects/{project_id}/rollback` | 回滚当前版本指针。 |
| `GET` | `/api/projects/{project_id}/export` | 导出当前版本 JSON。 |
| `GET` | `/api/traces/{trace_id}` | 查询 Trace 详情。 |
| `POST` | `/api/feedback` | 提交反馈。 |
| `GET` | `/metrics` | Prometheus 文本指标。 |

## 9. API 调用示例

### 9.1 创建会话

```powershell
$body = @{
  project_type = "ahu"
  auto_confirm_template = $false
  use_llm_planner = $true
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:8000/api/sessions" -ContentType "application/json" -Body $body
```

### 9.2 发送需求

```powershell
$threadId = "替换为上一步返回的 thread_id"
$body = @{
  message = "我要做 AHU 程序，需要直膨机、排风机、CO2 控制、新风阀和 Modbus 通讯。"
  project_type = "ahu"
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:8000/api/sessions/$threadId/message" -ContentType "application/json" -Body $body
```

### 9.3 确认模板

```powershell
$body = @{
  message = "确认使用模板 template_id"
  selected_template_id = "替换为候选模板 template_id"
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:8000/api/sessions/$threadId/message" -ContentType "application/json" -Body $body
```

### 9.4 确认补丁

```powershell
$body = @{
  action = "approve"
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri "http://127.0.0.1:8000/api/sessions/$threadId/patch-confirmation" -ContentType "application/json" -Body $body
```

取消补丁时把 `action` 改为 `cancel`。

## 10. 结构化补丁示例

补丁可以通过 planner 生成，也可以通过 `/api/planner/dry-run` 或服务层手动传入。

### 10.1 重命名节点

```json
{
  "op": "rename_node",
  "node_selector": {"id": "3a4c97e"},
  "new_name": "水泵比较判断"
}
```

### 10.2 替换常量或设定值

```json
{
  "op": "replace_constant",
  "node_selector": {"id": "67febfa"},
  "field": "fixedValue",
  "value": "6"
}
```

### 10.3 启用动态输入

```json
{
  "op": "enable_dynamic_input",
  "node_selector": {"id": "a2f6cc2"},
  "input_option": "highPidOutLimit"
}
```

### 10.4 从 schema 新增节点

```json
{
  "op": "add_node_from_schema",
  "module_type": "constInput",
  "tab_selector": {"label": "水泵控制"},
  "params": {
    "user_defined_name": "新增常量",
    "fixedValue": 9
  },
  "x": 220,
  "y": 260
}
```

### 10.5 连接节点

```json
{
  "op": "connect",
  "source_node_selector": {"id": "source_id"},
  "source_output": 0,
  "target_node_selector": {"id": "target_id"},
  "target_input": 1
}
```

本项目的 `wires` 统一按 `input_sources` 语义理解：当前节点每个输入端保存上游源引用。因此 `connect` 修改的是目标节点的 `wires[target_input]`。

### 10.6 复制功能块

```json
{
  "op": "copy_block",
  "block_id": "block_3f996bfafceb",
  "target_tab_selector": {"label": "旁通阀控制"},
  "x_offset": 80,
  "y_offset": 80,
  "name_prefix": "复制-"
}
```

`copy_block` 默认只复制内部节点和内部连线，会重写节点 id，丢弃外部入口/出口连线，并禁用复制节点上的本地 BACnet 暴露对象。dry-run 会返回边界预览，人工确认后可通过 `boundary_connections` 显式补接外部线。

### 10.7 修改 IO / 通讯点位

```json
{
  "op": "set_io_point",
  "node_selector": {"id": "22a27d1"},
  "params": {
    "hwExpander": "1",
    "hwChannelIndex": "31"
  }
}
```

该操作只允许修改已有 IO、Modbus、BACnet、BACnet/IP、MQTT 节点上的白名单字段，并会复用冲突校验。

## 11. 运行时文件

默认文件型存储会写入：

```text
projects/
  sessions/                 会话状态
  events/                   SSE / workflow 事件 JSONL
  traces/                   trace 详情 JSON
  feedback/                 用户反馈 JSONL
  versions/
    {project_id}/
      {version_id}.json
      {version_id}.meta.json
```

`programs/` 是原始模板库，运行时不要覆盖。工程输出和会话状态放在 `projects/`。

切换到 `RUNTIME_STORE_BACKEND=postgres` 后，session、project/version 元数据、patch、validation、export、audit 和观测数据写入 PostgreSQL；工程 JSON 文件本体仍保存在版本目录。

## 12. 测试和验收

后端测试：

```powershell
conda activate midea
pytest -q
```

前端验收：

```powershell
conda activate midea
cd frontend
npm run lint
npm run test
npm run build
npm run e2e
cd ..
```

完整常用验收：

```powershell
conda activate midea
python scripts\build_indexes.py
pytest -q
cd frontend
npm run lint
npm run test
npm run build
npm run e2e
cd ..
```

第 6 天评测和业务验收：

```powershell
conda activate midea
pytest -q tests/test_requirement_extraction_evals.py tests/test_business_acceptance.py
```

真实 LLM 测试默认跳过。需要配置 `.env` 中的 API key，并显式开启：

```powershell
conda activate midea
$env:RUN_LLM_INTEGRATION='1'
pytest tests\test_llm_gateway.py::test_requirement_extractor_real_deepseek -q -s
```

## 13. PostgreSQL 本地集成

需要 Docker 时可启动本地 PostgreSQL：

```powershell
conda activate midea
python -m pip install -e ".[postgres]"
docker run -d --name midea-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=midea_agent -p 5432:5432 postgres:16-alpine
```

执行迁移：

```powershell
$env:DATABASE_URL='postgresql://postgres:postgres@localhost:5432/midea_agent?sslmode=disable'
Get-Content -Raw -Encoding UTF8 'migrations/postgres/001_initial_schema.sql' | docker exec -i midea-postgres psql -U postgres -d midea_agent -v ON_ERROR_STOP=1
Get-Content -Raw -Encoding UTF8 'migrations/postgres/002_runtime_public_ids.sql' | docker exec -i midea-postgres psql -U postgres -d midea_agent -v ON_ERROR_STOP=1
Get-Content -Raw -Encoding UTF8 'migrations/postgres/003_observability.sql' | docker exec -i midea-postgres psql -U postgres -d midea_agent -v ON_ERROR_STOP=1
```

启用 PostgreSQL 后端并运行集成测试：

```powershell
$env:RUNTIME_STORE_BACKEND='postgres'
$env:LANGGRAPH_CHECKPOINTER_BACKEND='postgres'
$env:LANGGRAPH_CHECKPOINTER_SETUP='true'
$env:RUN_POSTGRES_INTEGRATION='1'
pytest tests\test_postgres_integration.py -q
```

## 14. 主要目录速查

| 路径 | 用途 |
| --- | --- |
| `app/main.py` | FastAPI 入口、API 路由、静态工作台托管。 |
| `app/graph/` | LangGraph 状态、节点、workflow、checkpoint。 |
| `app/services/` | 检索、知识库、LLM、planner、补丁、校验、版本、观测等服务层。 |
| `frontend/` | React / TypeScript 工作台源码。 |
| `app/static/workbench/` | 前端构建产物，后端根路径默认托管。 |
| `programs/` | 原始工程 JSON 模板库。 |
| `indexes/` | 检索索引。 |
| `knowledge/` | 控制策略和工程规范知识文档。 |
| `schemas/` | 模块 schema、节点生成模板和说明资料。 |
| `rules/` | 机器可读保护逻辑规则。 |
| `projects/` | 默认运行时会话、版本、事件、trace、反馈输出。 |
| `tests/` | 后端 pytest 测试。 |
| `evals/` | 模板选择、需求抽取、LLM planner 和业务验收样例。 |
| `文档/` | 项目结构、演进记录和生产化方案。 |

更完整的结构说明见 `文档/项目结构.md`。

## 15. 常见问题

### 15.1 工作台打开后没有最新前端效果

先构建前端：

```powershell
conda activate midea
cd frontend
npm run build
cd ..
uvicorn app.main:app --reload
```

### 15.2 模板检索结果不符合新增模板

重新生成索引：

```powershell
conda activate midea
python scripts\build_indexes.py
```

### 15.3 导出按钮不可用

先点击“校验”。如果存在 `error` 级结构问题，导出会被阻塞；如果需求覆盖报告存在明显缺失项，也会阻塞导出。需要根据校验摘要或覆盖报告修复后重新校验。

### 15.4 中高风险计划没有自动落盘

这是预期行为。中高风险补丁只会 dry-run，必须在工作台或 API 中确认后才会创建新版本。

### 15.5 Node 或 Python 出现 Windows 环境变量相关初始化错误

当前命令进程可能缺少 Windows 基础环境变量，可临时设置后重试：

```powershell
$env:SystemRoot='C:\Windows'
$env:windir='C:\Windows'
$env:ComSpec='C:\Windows\System32\cmd.exe'
```

## 16. 推荐上手路径

1. 运行 `python scripts\build_indexes.py` 确认索引可生成。
2. 运行 `pytest -q` 确认后端基础能力正常。
3. 启动 `uvicorn app.main:app --reload`。
4. 打开 `http://127.0.0.1:8000/`。
5. 选择 `AHU 程序`，新建会话，输入一个包含设备、通讯和保护要求的需求。
6. 确认模板，继续输入一个低风险修改需求。
7. 查看 dry-run、diff、校验摘要和版本历史。
8. 校验通过后导出 JSON。

## 17. 演示脚本

正式演示建议按固定脚本走，避免现场临时编造需求导致误入未覆盖边界。脚本见：

```text
文档/演示脚本.md
```

当前推荐演示顺序：

1. AHU 新建工程：直膨机、排风机、CO2、新风阀和 Modbus。
2. AHU 低风险节点改名：展示自动应用、局部图定位和导出。
3. 机房群控中风险断线：展示 dry-run、人工确认、版本 diff 和回滚。
4. 需求覆盖门禁：展示过滤网报警/防冻保护缺失时拒绝导出。

# 下一步实施计划

更新时间：2026-04-27

## 1. 目标

本计划用于把当前“智能体系统架构设计”推进到可运行原型。第一阶段的目标不是一次性实现所有复杂自动改造能力，而是先做出一个稳定闭环：

1. 用户选择项目类型：机房群控程序或 AHU 程序。
2. 系统根据用户需求推荐最接近的完整工程模板。
3. 用户确认模板后，系统生成一个工程副本。
4. 用户继续提出局部修改需求。
5. 系统通过结构化补丁改造 JSON。
6. 系统校验 JSON，并支持导出最终文件。

第一版应优先保证可靠、可追踪、可回滚，不追求让大模型直接重写完整工程 JSON。

## 2. MVP 边界

### 2.1 第一版必须支持

- 项目类型选择：机房群控程序 / AHU 程序。
- 模板检索：从 `programs/机房群控程序` 或 `programs/AHU程序` 中推荐 1 到 3 个候选模板。
- 模板确认：用户确认后复制为当前工程版本。
- 低风险 JSON 改造：
  - 修改节点名称。
  - 修改已有节点参数。
  - 添加备注节点。
- 基础校验：
  - JSON 能正常解析。
  - 节点 id 不重复。
  - `wires` 引用的目标节点存在。
  - 节点 `inputs` 与 `wires` 数组长度一致。
- 导出最终 JSON。

### 2.2 第一版暂不支持

- 大规模自动重连。
- 自动删除复杂保护逻辑。
- 跨 tab 大规模搬迁。
- 没有 schema 支撑的新模块自由生成。
- 根据一句话完整生成全新工程。
- 直接让 LLM 输出完整 JSON 文件。

这些能力可以在 MVP 稳定后逐步扩展。

## 3. 推荐目录结构

建议新增以下目录和文件：

```text
app/
  main.py
  graph/
    state.py
    workflow.py
    nodes.py
  services/
    template_index.py
    json_project.py
    patch_engine.py
    validator.py
    retrieval.py
  models/
    requirement.py
    template.py
    patch.py
    validation.py

scripts/
  build_indexes.py

indexes/
  templates/
  tabs/
  nodes/

knowledge/
  AHU控制策略.md
  机房群控控制策略.md
  工程规范.md

projects/
  versions/
  exports/
```

目录职责：

| 目录 | 职责 |
| --- | --- |
| `app/` | 后端服务和 LangGraph 工作流 |
| `app/graph/` | 图状态、节点函数、流程编排 |
| `app/services/` | JSON 处理、索引、检索、补丁、校验等确定性逻辑 |
| `app/models/` | Pydantic 数据模型 |
| `scripts/` | 离线索引构建和维护脚本 |
| `indexes/` | 模板、tab、节点等派生索引 |
| `knowledge/` | 额外控制策略和工程规范知识 |
| `projects/` | 用户工程版本和导出文件 |

## 4. 阶段一：离线模板索引

### 4.1 目标

建立模板摘要，让系统不需要读取完整 JSON 就能完成模板推荐和初步需求匹配。

### 4.2 输入目录

```text
programs/机房群控程序
programs/AHU程序
schemas
```

### 4.3 输出文件

```text
indexes/templates/template_index.json
indexes/tabs/tab_index.jsonl
indexes/nodes/node_index.jsonl
```

### 4.4 模板索引字段

每个模板至少包含：

```json
{
  "template_id": "plant_room_air_source_heat_pump_20240905",
  "project_type": "plant_room",
  "source_path": "programs/机房群控程序/风冷热泵标准控制程序[风冷涡旋]20240905.json",
  "file_name": "风冷热泵标准控制程序[风冷涡旋]20240905.json",
  "node_count": 3450,
  "tabs": ["使用配置", "硬接/调用信号", "通讯信号", "风冷热泵控制", "蝶阀控制", "水泵控制"],
  "node_type_counts": {
    "quote": 1009,
    "swInput": 999,
    "logic": 161
  },
  "features": {
    "has_pump_control": true,
    "has_valve_control": true,
    "has_modbus_output": true
  },
  "summary": "适合风冷热泵机房群控，包含主机、蝶阀、水泵、联动和旁通阀控制。"
}
```

### 4.5 验收标准

执行命令：

```powershell
conda activate midea
python scripts\build_indexes.py
```

能够完成：

- 正常扫描所有模板 JSON。
- 输出 5 个模板的摘要。
- 输出每个模板的 tab 列表。
- 输出每个模板的节点类型统计。
- 不修改原始模板文件。

## 5. 阶段二：JSON 工具层

### 5.1 目标

把大 JSON 的读取、定位、修改和校验做成确定性工具，LLM 只负责生成结构化修改意图。

### 5.2 核心模块

建议实现：

```text
app/services/json_project.py
app/services/patch_engine.py
app/services/validator.py
```

### 5.3 `json_project.py`

职责：

- 读取工程 JSON。
- 保存工程 JSON。
- 生成工程副本。
- 根据 id、type、name、tab 查询节点。
- 生成局部摘要。
- 生成版本快照。

关键函数建议：

```python
def load_project(path: str) -> list[dict]:
    ...

def save_project(path: str, nodes: list[dict]) -> None:
    ...

def find_nodes(nodes: list[dict], selector: dict) -> list[dict]:
    ...

def summarize_project(nodes: list[dict]) -> dict:
    ...
```

### 5.4 `patch_engine.py`

第一版支持三种操作：

```json
{
  "op": "update_param",
  "node_selector": {"type": "compare", "name_contains": "运行台数"},
  "params": {"tripPoint": 3}
}
```

```json
{
  "op": "rename_node",
  "node_selector": {"id": "abc123"},
  "new_name": "冷冻泵运行台数判断"
}
```

```json
{
  "op": "add_comment",
  "tab_selector": {"label": "水泵控制"},
  "text": "根据用户需求，将运行台数阈值调整为 3。"
}
```

执行原则：

- 匹配结果必须唯一；不唯一时返回需要用户确认。
- 所有修改先生成 diff 摘要，再保存新版本。
- 不直接覆盖原始模板。
- 每次补丁执行后都调用校验器。

### 5.5 `validator.py`

第一版校验：

- JSON 根节点必须是数组。
- 每个节点必须有 `id` 和 `type`。
- 节点 id 不能重复。
- `tab` 节点 id 必须能被普通节点的 `z` 引用。
- `wires` 必须是数组。
- `wires` 中引用的目标 id 必须存在。
- 如果节点有 `inputs` 字段，`len(wires)` 应与 `inputs` 保持一致。

后续再加入：

- 对照 `schemas/*/*.json` 校验参数。
- 校验动态端口条件。
- 校验通讯点位冲突。
- 校验关键保护逻辑未被删除。

### 5.6 验收标准

能够完成一次手工调用：

1. 复制一个模板为工程版本。
2. 找到一个指定节点。
3. 修改一个参数。
4. 保存新版本。
5. 输出修改摘要。
6. 校验通过。

## 6. 阶段三：LangGraph 最小工作流

### 6.1 目标

建立可多轮运行的智能体流程，但每个节点尽量只做一件事。

### 6.2 Graph State

建议 state 只保存轻量字段：

```python
class AgentState(TypedDict):
    messages: list
    project_type: str | None
    requirement_summary: dict
    selected_template_id: str | None
    current_project_version_id: str | None
    pending_patch: dict | None
    validation_report: dict | None
```

完整 JSON 不进入 state，只保存版本 id 或文件路径。

### 6.3 第一版节点

```text
START
  -> classify_project_type
  -> collect_requirements
  -> retrieve_template_candidates
  -> confirm_template
  -> create_project_version
  -> plan_patch
  -> apply_patch
  -> validate_project
  -> summarize_result
  -> wait_user_feedback
```

### 6.4 节点说明

| 节点 | 职责 |
| --- | --- |
| `classify_project_type` | 判断用户要做机房群控还是 AHU |
| `collect_requirements` | 把自然语言整理成结构化需求 |
| `retrieve_template_candidates` | 从索引中推荐模板 |
| `confirm_template` | 等待用户确认候选模板 |
| `create_project_version` | 复制模板，创建当前工程版本 |
| `plan_patch` | 生成结构化补丁意图 |
| `apply_patch` | 调用补丁工具执行修改 |
| `validate_project` | 调用校验器 |
| `summarize_result` | 生成用户可读的修改摘要 |
| `wait_user_feedback` | 等待继续修改或导出 |

### 6.5 验收标准

通过 API 或命令行能完成：

```text
用户：我要做一个 AHU 程序，有 CO2 控制和风机变频。
系统：推荐 3 个 AHU 模板，并说明理由。
用户：用第一个。
系统：创建工程版本。
用户：把送风温度设定值改为 18。
系统：生成补丁，修改 JSON，校验并输出摘要。
用户：导出。
系统：输出最终 JSON 文件路径。
```

## 7. 阶段四：最小 API

### 7.1 目标

先用 API 调通完整流程，前端可以后置。

### 7.2 接口草案

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/api/sessions` | POST | 创建会话，返回 `thread_id` |
| `/api/sessions/{thread_id}/message` | POST | 发送用户消息，驱动 LangGraph |
| `/api/templates/search` | POST | 检索模板 |
| `/api/projects/{project_id}/versions` | GET | 查看工程版本 |
| `/api/projects/{project_id}/diff` | GET | 查看版本差异 |
| `/api/projects/{project_id}/validate` | POST | 手动触发校验 |
| `/api/projects/{project_id}/export` | GET | 导出最终 JSON |

### 7.3 验收标准

可以用 PowerShell 调用：

```powershell
conda activate midea
uvicorn app.main:app --reload
```

然后通过 API 完成：

1. 创建会话。
2. 发送需求。
3. 获取模板推荐。
4. 确认模板。
5. 发送修改需求。
6. 获取校验结果。
7. 导出 JSON。

## 8. 阶段五：知识库建设

### 8.1 为什么需要知识库

完整工程模板只能告诉系统“已有项目怎么写”，但不能稳定回答“新需求应该怎么改”。因此需要额外知识库来约束和指导改造。

### 8.2 第一批知识文档

建议新增：

```text
knowledge/AHU控制策略.md
knowledge/机房群控控制策略.md
knowledge/工程规范.md
```

### 8.3 `AHU控制策略.md`

建议包含：

- AHU 常见设备构成。
- 新风阀、回风阀、排风机联动。
- 风机启停和变频控制。
- 送风温度控制。
- CO2 浓度控制。
- 湿度控制。
- 防冻保护。
- 过滤网报警。
- 直膨机或水阀控制。

### 8.4 `机房群控控制策略.md`

建议包含：

- 主机启停和加减载。
- 冷冻水泵控制。
- 冷却水泵控制。
- 冷却塔控制。
- 蝶阀联动。
- 旁通阀控制。
- 压差控制。
- 故障切换。
- 轮值策略。

### 8.5 `工程规范.md`

建议包含：

- 节点命名规范。
- tab 划分规范。
- IO 点位命名规范。
- 通讯点位规则。
- 保护逻辑不可删除清单。
- 导出前检查清单。
- 人工确认规则。

### 8.6 验收标准

第一版可以先不接向量库，只做关键词检索。要求：

- 能根据用户需求检索到相关策略段落。
- 能把策略段落作为 `plan_patch` 的参考上下文。
- 能在修改摘要中说明依据了哪些规则。

## 9. 阶段六：局部子图改造

### 9.1 目标

在 MVP 稳定后，支持新增节点、连线和复制功能块。

### 9.2 新增操作

```text
add_node_from_schema
connect
disconnect
copy_block
enable_dynamic_input
```

### 9.3 前置条件

必须先完成：

- 节点级索引。
- tab 级索引。
- 邻域加载。
- schema 参数校验。
- wires 拓扑校验。
- diff 展示。

### 9.4 验收标准

系统能完成：

- 根据 `schemas` 生成合法新节点。
- 在指定 tab 中插入节点。
- 自动生成唯一 id。
- 自动设置 `x`、`y` 坐标。
- 自动维护 `wires`。
- 校验所有目标节点存在。

## 10. 执行优先级

建议按以下顺序推进：

1. `scripts/build_indexes.py`
2. `app/services/json_project.py`
3. `app/services/validator.py`
4. `app/services/patch_engine.py`
5. `app/graph/state.py`
6. `app/graph/workflow.py`
7. `app/main.py`
8. `knowledge/*.md`
9. 前端工作台
10. 局部子图改造能力

最先做索引和 JSON 工具层，是因为它们决定系统是否可靠。LangGraph 应该建立在这些确定性能力之上，而不是让 LLM 直接承担 JSON 修改和校验。

## 11. 近期一周计划

### 第 1 天

- 建立 `scripts/build_indexes.py`。
- 扫描所有模板。
- 生成 `template_index.json`。
- 打印模板摘要。

### 第 2 天

- 生成 `tab_index.jsonl`。
- 生成 `node_index.jsonl`。
- 实现基本模板检索。

### 第 3 天

- 实现 `json_project.py`。
- 实现工程版本复制。
- 实现节点查询。

### 第 4 天

- 实现 `validator.py`。
- 校验 id、tab、wires、inputs。
- 对现有模板跑一次校验报告。

### 第 5 天

- 实现 `patch_engine.py`。
- 支持 `update_param`、`rename_node`、`add_comment`。
- 完成一次真实模板改造测试。

### 第 6 天

- 搭建 LangGraph 最小流程。
- 接入模板检索和工程版本创建。
- 完成命令行对话原型。

### 第 7 天

- 搭建 FastAPI。
- 暴露会话、消息、导出接口。
- 写一份端到端演示记录。

## 12. 风险控制

| 风险 | 控制措施 |
| --- | --- |
| LLM 修改 JSON 出错 | LLM 只生成补丁意图，程序执行修改 |
| 长 JSON 超上下文 | 使用模板索引、tab 索引、节点索引和局部加载 |
| 节点匹配不唯一 | 返回用户确认，不自动修改 |
| wires 被破坏 | 每次修改后做拓扑校验 |
| 原始模板被覆盖 | 所有修改写入 `projects/versions` |
| 多轮需求丢失 | LangGraph state 保存结构化需求摘要 |
| 高风险修改误执行 | 删除节点、改 IO、删保护逻辑必须人工确认 |

## 13. 下一步立即动作

初始建议从这里开始：

1. 新建 `scripts/build_indexes.py`。
2. 新建 `indexes/` 目录。
3. 扫描 `programs` 下的 5 个模板。
4. 生成 `indexes/templates/template_index.json`。
5. 在终端输出模板摘要，确认索引结果可信。

完成这一步后，再进入 JSON 工具层开发。

## 14. 当前实施进度

截至 2026-04-27，已完成以下内容：

| 计划项 | 状态 | 已落地内容 |
| --- | --- | --- |
| `scripts/build_indexes.py` | 已完成 | 已扫描 `programs` 下 5 个模板，生成模板、tab、节点索引 |
| `indexes/` | 已完成 | 已生成 `template_index.json`、`tab_index.jsonl`、`node_index.jsonl` |
| `app/services/json_project.py` | 已完成 | 已支持工程 JSON 读取、保存、摘要、节点查询、工程版本复制 |
| `app/services/validator.py` | 已完成 | 已支持基础结构校验、id 校验、tab/subflow 引用校验、`wires` 目标校验 |
| `app/services/patch_engine.py` | 已完成 | 已支持 `update_param`、`rename_node`、`add_comment`，并限制修改结构字段 |
| `app/services/retrieval.py` | 已完成 | 已支持模板检索、tab 检索、节点检索、局部邻域加载 |
| `app/graph/state.py`、`app/graph/nodes.py`、`app/graph/workflow.py` | 已完成 | 已搭建 LangGraph 最小工作流 |
| `app/main.py` | 已完成 | 已暴露会话、消息、模板检索、知识检索、校验、导出等最小 API |
| `knowledge/*.md` | 已完成第一版 | 已新增 AHU 控制策略、机房群控控制策略、工程规范 |
| `app/services/planner.py` | 已完成第一版 | 已支持节点改名、参数修改、添加备注的确定性规划；无法唯一定位时返回澄清问题 |
| 工作流修改阶段路由 | 已优化 | 已有工程版本的会话再次收到用户消息时，直接进入补丁规划，不再重新检索模板 |
| `tests/` | 已新增 | 已用 pytest 固化索引、检索、校验、补丁、工作流、API 验收 |
| 前端工作台 | 未开始 | 后端闭环稳定后再做 |
| 局部子图改造能力 | 未开始 | 需要在补丁规划和 schema 生成能力更稳定后推进 |

当前全量验收命令：

```powershell
conda activate midea
python scripts\build_indexes.py
pytest -q
```

当前验收结果：

```text
模板数量: 5
页面数量: 31
节点数量: 8413
21 passed
```

## 15. 关于 `projects/` 目录

计划中设计了 `projects/versions` 和 `projects/exports`，用于保存真实用户工程版本和导出文件。

当前代码已经支持工程版本复制，默认路径也是 `projects/versions`。但是到目前为止，所有自动化测试和验收都显式使用临时目录创建工程版本，目的是避免测试过程污染项目根目录。因此你现在可能看不到 `projects/` 目录。

后续接入真实前端或正式 API 调试时，如果不传入临时 `versions_dir`，系统会按默认配置创建：

```text
projects/
  versions/
```

是否现在创建 `projects/versions` 和 `projects/exports` 空目录，可以等进入真实交互调试阶段再决定。当前阶段不创建它们，不影响已完成能力。

## 16. 下一步实际推进点

下一步应继续按原计划推进，但当前“下一步立即动作”已经完成，因此实际下一步不是重新做索引，而是进入以下二选一：

1. 补丁规划层增强：继续扩展确定性 `planner`，支持更多安全修改场景，例如按 tab + 节点类型 + 参数名定位、按唯一节点名称定位、批量但可确认的参数修改。
2. 前端工作台雏形：在后端 API 已闭环的基础上，做最小 UI，用于创建会话、选择模板、发送修改需求、查看校验和导出。

从工程质量角度，建议先做补丁规划层。原因是前端可以晚一点接入，但如果没有稳定的规划层，前端只能调用手写补丁，无法体现智能体能力。

## 17. 最近一次推进记录

本次推进完成时间：2026-04-27。

完成内容：

1. 优化 LangGraph 工作流路由：已有 `current_project_path` 的会话再次收到用户消息时，跳过模板检索，直接进入工程版本节点和补丁规划节点。
2. 扩展 planner 场景：已验证可通过“页面 + 节点类型 + 参数名”定位唯一节点，例如“把水泵控制比较判断的阈值改为 3”。
3. 增加测试保护：验证修改阶段不会重跑模板检索并覆盖原候选模板列表。
4. 全量测试通过。

最新验收命令：

```powershell
conda activate midea
pytest -q
```

最新验收结果：

```text
21 passed
```

下次开工建议：

1. 继续增强 `app/services/planner.py`，优先支持“按唯一节点名称定位”和“按字段别名修改常见参数”。
2. 或开始搭建前端工作台雏形，调用现有 FastAPI 闭环。

# 2026-05-06 演进详细记录

用途：记录 2026-05-06 的 deadline 后补丁能力增量和当天文档同步；新对话默认只读取上级 `文档/演进记录.md`。

## 4. 详细记录

### ER-20260506-001 `add_tab` 新增空页面补丁操作

状态：已完成

修改人：用户提交，Codex 补充文档同步

关联文件：

- `app/services/patch_engine.py`
- `app/services/planner.py`
- `app/services/llm_planner.py`
- `app/graph/nodes.py`
- `frontend/src/features/planning/PlanningPanel.tsx`
- `tests/test_services.py`
- `tests/test_llm_gateway.py`
- `tests/test_planner_evals.py`
- `README.md`
- `文档/deadline前智能体目标实现方案.md`
- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-06.md`

背景：

2026-05-06 17:43:31 提交 `eefb668` 新增了 `add_tab` 补丁能力，但当时只同步了 README 和部分项目结构说明，演进记录未更新。该能力用于把“新增一个 CO2 控制页面”这类明确低风险需求转成结构化补丁，不从零生成页面内完整控制逻辑。

目标：

- 允许结构化补丁新增空页面/tab 节点。
- 规则 planner 能识别明确的新建页面自然语言。
- LLM planner schema 和 prompt 明确允许 `add_tab`。
- 风险摘要和前端计划预览能以业务语言展示“新增页面”。
- 保持所有工程 JSON 修改仍由 patch engine 执行，后续新增节点、复制功能块或接线继续走对应 op 和风险确认。

代码/契约变化：

- `patch_engine.py` 新增 `add_tab` 执行分支：
  - 支持 `{"op": "add_tab", "label": "CO2 控制"}`。
  - 创建 `type="tab"` 节点，写入 `label`、`disabled`、`info`。
  - 默认拒绝重复页面标签，除非显式设置 `allow_duplicate_label=true`。
  - dry-run 返回新增节点 diff 和 `changes` 摘要。
- `planner.py` 新增规则规划：
  - 可识别“新增、添加、创建、新建 + 页面/页签/tab”的明确请求。
  - 对缺少页面名称或标签重复的场景返回面向用户的追问。
  - 避免把“在某页面里新增某内容”误判为新建页面。
- `llm_planner.py`：
  - `PatchOp`、`PlannedOperation` schema 和 prompt 均允许 `add_tab`。
  - 明确 `add_tab` 只新增空页面，不自动创建功能节点或连线。
  - 新增空页面通常标记为 low risk。
- `app/graph/nodes.py`：
  - 操作摘要支持 `add_tab`，显示“新增页面：xxx”。
- `PlanningPanel.tsx`：
  - 前端计划预览将 `add_tab` 显示为“新增页面”，并展示页面标签。
- `文档/deadline前智能体目标实现方案.md`：
  - 将当前底座能力更新为包含 `add_tab`。

影响：

- 用户可直接输入“新增一个 CO2 控制页面”，系统生成低风险 `add_tab` 补丁并可自动应用。
- 新页面只是空页面，不代表已经生成 CO2 控制逻辑。
- 如后续要在页面内新增模块、复制功能块或接线，仍需要 `add_node_from_schema`、`copy_block`、`connect` 等操作，并按中高风险策略 dry-run 和确认。

验证：

- 提交 `eefb668` 已新增覆盖：
  - patch engine 新增页面和重复标签拒绝。
  - 规则 planner 生成 `add_tab` 并 dry-run。
  - LLM planner 接受 `add_tab` schema。
  - 真实 LLM planner 测试允许 op 集合包含 `add_tab`。
- 本次文档同步未重跑测试，原因是只修改文档；测试基线沿用提交中的新增测试覆盖和此前普通全量基线 `125 passed, 4 skipped`。

风险/回滚：

- 回滚该能力时，需要同时撤回 `patch_engine.py`、`planner.py`、`llm_planner.py`、`nodes.py`、前端计划预览和对应测试中的 `add_tab` 支持。
- `allow_duplicate_label` 只应在显式用户意图下使用；默认拒绝重复标签是当前安全边界。

文档同步：

- `README.md` 已在提交中同步 `add_tab` 能力和示例。
- `文档/项目结构.md` 已补充 `add_tab` 的模块职责、主流程、结构性限制和前端预览说明。
- `文档/演进记录.md` 已新增 ER-20260506-001 和新对话恢复提示。

后续动作：

- 若继续提升“新增页面后补逻辑”的体验，优先实现当前工程语义索引、会话工作记忆、语义定位器和按需检索式 planner。
- 不要把 `add_tab` 扩展成隐式生成完整页面逻辑；页面内逻辑仍应拆成可 dry-run、可校验、可确认的结构化补丁操作。

DoD：

- `add_tab` 能从规则 planner、LLM planner、patch engine、风险摘要、前端计划预览和测试链路完整闭环。
- 文档入口、变更索引和详细归档均已同步。

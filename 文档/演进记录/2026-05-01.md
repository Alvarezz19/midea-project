# 2026-05-01 演进详细记录

用途：记录 Deadline P0 第 1 天相关实现和当天文档同步；新对话默认只读取上级 `文档/演进记录.md`。

## 4. 详细记录

### ER-20260501-001 修复基线和需求槽位归一化

状态：已完成

修改人：Codex

关联文件：

- `app/services/requirement_model.py`
- `app/graph/state.py`
- `app/graph/nodes.py`
- `app/main.py`
- `frontend/src/api/types.ts`
- `frontend/src/features/session/SessionPanel.tsx`
- `frontend/src/features/planning/PlanningPanel.tsx`
- `frontend/src/features/session/SessionPanel.test.tsx`
- `frontend/src/features/planning/PlanningPanel.test.tsx`
- `tests/test_requirement_model.py`
- `tests/test_workflow_api.py`
- `app/static/workbench/**`
- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-01.md`

背景：

`文档/deadline前智能体目标实现方案.md` 将第 1 天目标定为“修复基线和需求槽位归一化”。当时主流程已经有确定性需求摘要和 LLM 需求抽取服务，但二者字段未统一，主工作流仍主要依赖规则摘要；前端也存在 `control_targets/protections` 与后端 `control_features/protection_logic` 的字段错位，以及模板改造成本 `reason/reasons` 展示错位。

目标：

- 修复当前测试基线和前后端字段契约错位。
- 新增 `RequirementSlots` 归一化层，作为规则抽取和 LLM 抽取的统一汇入点。
- 保持主流程既有 `requirement_summary/open_questions/confirmed_requirements` 兼容字段。
- 支持多轮需求累计、数量覆盖和否定语义。
- 已创建工程版本后，用户局部修改请求不重新进入新工程模板澄清。

代码/契约变化：

- 新增 `app/services/requirement_model.py`：
  - `merge_requirement_slots()` 合并规则摘要、LLM 抽取结果和历史槽位。
  - `requirement_summary_from_slots()` 从槽位派生主流程兼容摘要。
  - 槽位记录 `project_type`、`task_type`、`equipment`、`control_features`、`communication`、`io_points`、`protection_logic`、`design_constraints`、`missing_fields`、`risk_level`、`risk_items`、`message_history`、`slot_sources` 和 `slot_status`。
  - 支持“改成、调整为、不是、不要、取消、不需要、改为”等覆盖或否定语义。
  - 支持设备数量抽取和数量变化风险标记。
  - LLM 不可用时不影响规则路径。
- `app/graph/state.py`：
  - `AgentState` 新增 `requirement_slots`、`design_brief`、`conformance_report`。
- `app/graph/nodes.py`：
  - `collect_requirements` 先执行 `analyze_requirement()`，再尝试 `extract_requirement_with_llm()`，最后合并到 `RequirementSlots`。
  - LLM 抽取失败捕获为规则回退，不抛出到工作流。
  - 已有工程版本时，槽位 `task_type` 进入 `modify_existing`，路由继续进入补丁规划。
- `app/main.py`：
  - `_empty_state()` 和 `_public_state()` 公开/保存 `requirement_slots/design_brief/conformance_report`。
- 前端契约：
  - `RequirementSummary` 改为读取 `control_features/protection_logic/io_points`。
  - `TemplateCandidate.estimated_modification_cost` 支持后端返回的 `reasons` 数组。
  - `SessionPanel` 正确展示后端结构化需求字段。
  - `PlanningPanel` 正确展示改造成本原因数组。
- 测试：
  - 新增 `tests/test_requirement_model.py`，覆盖 AHU/机房群控抽取、模糊追问、设备数量覆盖、排风机否定、LLM 合并和已有工程修改路由。
  - 更新 `tests/test_workflow_api.py` 的 React 工作台标题断言。
  - 新增前端组件契约测试，防止字段错位回归。
- 重新构建 `app/static/workbench/`。

影响：

- 主工作流不再直接让 LLM 输出替换 `requirement_summary`，而是统一经槽位层派生兼容字段，降低状态兼容风险。
- 用户第一轮说“2 台水泵”，后续说“水泵改成 3 台”，当前有效需求为 3 台，旧数量保留为来源/覆盖信息但不作为有效需求展示。
- 用户第一轮说“需要排风机”，后续说“不要排风机”，排风机槽位标记为 `rejected`，不再出现在当前有效需求中。
- 用户只说“我要做控制程序”仍追问项目类型；只说“我要做 AHU”仍追问主要设备或控制目标。
- 已创建工程后，局部改名、常量修改等请求不会因为缺少完整新工程槽位被误拦截。

验证：

```powershell
conda activate midea
pytest -q tests/test_requirement_model.py tests/test_template_selection_evals.py tests/test_llm_gateway.py
pytest -q tests/test_workflow_api.py tests/test_template_selection_evals.py tests/test_planner_evals.py tests/test_llm_gateway.py
pytest -q tests/test_requirement_model.py tests/test_workflow_api.py tests/test_template_selection_evals.py tests/test_planner_evals.py tests/test_llm_gateway.py
pytest -q
cd frontend
npm run test -- --run src/features/session/SessionPanel.test.tsx src/features/planning/PlanningPanel.test.tsx
npm run lint
npm run test
npm run build
```

结果：

```text
需求槽位/模板/LLM 相关测试：29 passed
方案指定后端回归集：40 passed
后端相关回归集：54 passed
全量 pytest：113 passed, 4 skipped
前端定向组件测试：5 passed
npm run lint：通过
npm run test：27 passed
npm run build：通过，输出 app/static/workbench/
```

风险/回滚：

- 风险中等。工作流需求状态入口发生变化，但保留了既有兼容字段并补充了覆盖/回退测试。
- 需求槽位当前仍是轻量启发式模型，数量、否定和风险判断不应被视为完整工程设计校验。
- LLM 抽取失败被降级为规则路径，避免无 API key 环境阻断主流程。
- 回滚可移除 `requirement_model.py` 并让 `collect_requirements` 恢复直接使用 `analyze_requirement()`；前端字段修复应保留，因为后端真实字段已是 `control_features/protection_logic`。

文档同步：

- 已同步 `文档/项目结构.md`。
- 已同步 `文档/演进记录.md`。
- 已在本归档新增 ER-20260501-001。

后续动作：

- 进入 Deadline 第 2 天：新增轻量 `DesignBrief`，复用模板候选解释输出设计摘要和模板覆盖矩阵。
- 创建设计摘要后，需要把摘要与会话/版本上下文关联，为后续需求覆盖门禁做准备。

DoD：

- 需求槽位归一化层存在并接入主流程。
- LLM 抽取失败时规则回退可工作。
- 主流程兼容字段仍存在。
- 多轮覆盖和否定语义有测试。
- 已有工程后的修改请求进入补丁规划。
- 前后端字段错位已修复并有契约测试。
- 后端全量测试和前端基础验收通过。

### ER-20260501-002 LLM 结构化规划默认开启

状态：已完成

修改人：Codex

关联文件：

- `app/main.py`
- `frontend/src/store/workbenchStore.ts`
- `tests/test_workflow_api.py`
- `app/static/workbench/**`
- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-01.md`

背景：

Deadline 方案要求复杂结构改造更多依赖 LLM planner dry-run，而不是要求用户手动打开前端开关。工作台已有“LLM 结构化规划”开关，但默认值为关闭；直接调用创建会话 API 时默认同样关闭。

目标：

- 新建会话默认启用 LLM 结构化规划。
- 前端首次打开工作台时开关默认开启。
- 保留用户手动关闭开关的能力。
- API 返回状态中缺省 `use_llm_planner` 时按开启处理。

代码/契约变化：

- `app/main.py`：
  - `CreateSessionRequest.use_llm_planner` 默认值改为 `True`。
  - `_empty_state(use_llm_planner=True)`。
  - `_public_state()` 默认返回 `use_llm_planner=True`。
- `frontend/src/store/workbenchStore.ts`：
  - Zustand 初始 `useLlmPlanner` 改为 `true`。
  - `setSession()` 从后端状态回填时，缺省值改为 `true`。
- `tests/test_workflow_api.py`：
  - API 端到端测试新增断言：创建会话默认 `use_llm_planner` 为 `true`。
- 重新构建 `app/static/workbench/`。

影响：

- 新会话默认走 LLM planner 能力开关；如果 LLM 不可用，已有 planner 节点仍返回澄清/不可用提示，不应报 500。
- 前端开关仍可手动关闭，并通过 `createSession()`、`sendMessage()` 传回后端。
- 已有旧会话状态如果明确保存了 `use_llm_planner=false`，仍会按旧会话状态关闭。

验证：

```powershell
conda activate midea
pytest -q tests/test_workflow_api.py
cd frontend
npm run test -- --run src/features/session/SessionPanel.test.tsx
npm run lint
npm run build
```

手工验证：

```powershell
conda activate midea
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
Invoke-WebRequest -UseBasicParsing -Method Post -ContentType 'application/json' -Body '{}' http://127.0.0.1:8000/api/sessions
```

结果：

```text
tests/test_workflow_api.py：23 passed
SessionPanel 定向测试：2 passed
npm run lint：通过
npm run build：通过
POST /api/sessions 返回 "use_llm_planner": true
```

风险/回滚：

- 风险较低。只改默认值，用户和旧会话仍可显式关闭。
- 如果本地无 LLM API key，复杂修改请求可能更多进入 LLM 不可用提示；需求抽取和规则回退不受影响。
- 回滚可把后端和前端默认值恢复为 `false`，无需迁移会话数据。

文档同步：

- 已同步 `文档/项目结构.md`。
- 已同步 `文档/演进记录.md`。
- 已在本归档新增 ER-20260501-002。

后续动作：

- 第 4 天复杂 planner fallback 时，需要在后端自动判断规则 planner 失败且属于结构改造意图时尝试 LLM dry-run，而不是仅依赖 UI 开关。

DoD：

- 新会话 API 默认 `use_llm_planner=true`。
- 前端开关默认开启。
- 用户仍可关闭。
- 相关 API 和前端测试通过。
- 静态工作台构建产物已更新。

### ER-20260501-003 项目结构同步到需求槽位阶段

状态：已完成

修改人：Codex

关联文件：

- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-01.md`

背景：

完成 Deadline P0 第 1 天后，长期结构信息已经变化：新增 `requirement_model.py`，`AgentState` 新增需求槽位、设计摘要和覆盖报告状态字段，主流程从“规则需求摘要 + 显式 LLM 抽取 API”变为“规则摘要 + LLM 抽取尝试 + RequirementSlots 归一化”。`文档/项目结构.md` 仍停留在 2026-04-29，需要同步。

目标：

- 只同步长期稳定结构信息，不把普通实现细节写入结构文档。
- 更新当前主流程和结构性限制。
- 让新对话能从项目结构文档快速理解需求槽位归一化边界。

文档变化：

- `文档/项目结构.md` 更新时间改为 `2026-05-01`。
- `app/graph/state.py` 说明增加需求槽位、设计摘要和覆盖报告状态字段。
- `app/services/` 新增 `requirement_model.py` 职责说明。
- 当前主流程加入 `requirement_extractor.py` 主流程尝试接入和 `requirement_model.py` 合并派生步骤。
- 测试清单新增 `tests/test_requirement_model.py`。
- 结构性限制补充：
  - `RequirementSlots` 已进入主流程和 API 公开状态。
  - `design_brief`、`conformance_report` 仍是后续设计摘要和需求覆盖门禁的状态边界。
  - 澄清逻辑只阻塞项目类型和主要设备/控制目标。
  - 已有工程版本后的输入默认进入局部修改规划。
  - LLM 结构化 planner dry-run 默认开启。
- `文档/演进记录.md` 更新当前总览、变更索引、验收基线、归档入口和下次建议。

影响：

- 新对话读取 `文档/项目结构.md` 后，可以看到需求槽位归一化已经是主流程结构边界。
- 方案文档仍保持方案属性，不记录进度。
- 本轮为文档同步，不改变运行时代码。

验证：

```text
文档-only，未运行测试。
```

风险/回滚：

- 风险低。只改文档。
- 如果后续代码未提交或回滚，需要同步回滚 `项目结构.md` 中关于 `requirement_model.py` 和默认开启 LLM planner 的描述。

文档同步：

- 已同步 `文档/项目结构.md`。
- 已同步 `文档/演进记录.md`。
- 已在本归档新增 ER-20260501-003。

后续动作：

- 进入 Deadline 第 2 天前，建议先阅读 `文档/deadline前智能体目标实现方案.md` 的 P0-2，以及 `文档/项目结构.md` 当前主流程。

DoD：

- 项目结构文档能反映新增服务、状态字段和主流程边界。
- 演进记录主索引和日期归档均包含 2026-05-01 记录。

### ER-20260501-004 轻量设计摘要和模板能力解释

状态：已完成

修改人：Codex

关联文件：

- `app/services/design_brief.py`
- `app/graph/nodes.py`
- `app/services/json_project.py`
- `frontend/src/api/types.ts`
- `frontend/src/features/session/SessionPanel.tsx`
- `frontend/src/features/planning/PlanningPanel.tsx`
- `frontend/src/features/session/SessionPanel.test.tsx`
- `frontend/src/features/planning/PlanningPanel.test.tsx`
- `tests/test_template_selection_evals.py`
- `tests/test_workflow_api.py`
- `app/static/workbench/**`
- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-01.md`

背景：

`文档/deadline前智能体目标实现方案.md` 将第 2 天目标定为“轻量设计摘要和模板能力解释”。第 1 天已经完成 `RequirementSlots` 归一化，但用户在模板确认前仍主要看到模板分数和简短摘要，缺少工程师可审阅的“设备、控制策略、点表、保护、已覆盖、需改造和风险”中间产物。

目标：

- 新增轻量 `DesignBrief`，复用现有模板候选解释，不另起复杂覆盖算法。
- 模板候选和设计摘要都能解释已满足项、需改造项、改造成本、风险点和推荐理由。
- 模板确认后按用户选定模板刷新设计摘要。
- 创建工程版本和补丁子版本时保存需求槽位和设计摘要关键上下文，为后续需求覆盖门禁做准备。
- 前端会话面板和规划面板展示设计摘要，避免字段缺失或 `undefined`。

代码/契约变化：

- 新增 `app/services/design_brief.py`：
  - `build_design_brief()` 输入 `RequirementSlots + requirement_summary + template_candidates`。
  - 输出 `selected_template`、`recommendation_reasons`、`satisfied_requirements`、`modification_items`、`clarification_items`、`risk_items`、`equipment_plan`、`control_plan`、`point_plan`、`protection_plan`、`export_gate`、`template_coverage` 和 `summary`。
  - 复用 `explain_template_candidate()` 的 `matched_items/missing_items/estimated_modification_cost/risk_points`。
  - 额外用模板特征和节点索引给出轻量证据，不让 LLM 直接写设计结论。
- `app/graph/nodes.py`：
  - `retrieve_template_candidates()` 生成模板候选后同步生成 `design_brief`。
  - 自动确认、已有 `selected_template_id` 和 LangGraph interrupt resume 人工确认三条路径都会按选定模板刷新 `design_brief`。
  - 无项目类型或无候选模板时清空设计摘要，避免前端残留旧推荐。
  - 模板候选新增 `recommendation_reasons`。
  - assistant summary 在等待模板确认时追加设计摘要和推荐理由。
- `app/services/json_project.py`：
  - `create_project_version()` 和 `create_project_version_from_nodes()` 新增可选 `requirement_slots/design_brief`。
  - 版本元数据写入 `requirement_context.requirement_slots` 和 `requirement_context.design_brief`。
- 前端契约：
  - `frontend/src/api/types.ts` 新增 `DesignBrief`、`TemplateCoverage`，并在 `SessionState` 中公开 `requirement_slots/design_brief/conformance_report`。
  - `SessionPanel` 展示轻量设计摘要、设备/控制/点表/保护计划和待确认项。
  - `PlanningPanel` 展示推荐模板、模板得分、已满足/需改造计数、设备/控制/点表/保护标签、推荐理由和需改造提示。
  - 模板候选描述展示后端 `recommendation_reasons`。
- 测试：
  - `tests/test_template_selection_evals.py` 新增设计摘要能力解释测试。
  - `tests/test_workflow_api.py` 验证工作流创建版本后 `design_brief` 与选定模板一致，并写入版本元数据。
  - `PlanningPanel.test.tsx` 和 `SessionPanel.test.tsx` 新增设计摘要展示测试。
- 重新构建 `app/static/workbench/`。

影响：

- 用户选模板前可以看到推荐理由、已覆盖项、需改造项和风险提示，而不是只看到分数。
- 设计摘要能回答当前工程包含哪些设备、控制策略、点表/通讯和保护项，以及哪些需求需要后续补丁或人工复核。
- 初始版本和补丁子版本元数据开始携带需求/设计上下文，后续 `RequirementConformanceReport` 可优先从版本或会话上下文恢复需求依据。
- 前端设计摘要展示仍是只读确认视图，不提供完整画布编辑或自动点表生成能力。

验证：

```powershell
conda activate midea
pytest -q tests/test_template_selection_evals.py::test_design_brief_explains_selected_template_capability tests/test_workflow_api.py::test_workflow_can_create_project_version
pytest -q tests/test_template_selection_evals.py tests/test_requirement_model.py
pytest -q tests/test_workflow_api.py
cd frontend
npm run test -- PlanningPanel SessionPanel
npm run lint
npm run build
```

结果：

```text
DesignBrief/版本元数据定向后端测试：2 passed
模板选择 + 需求槽位测试：17 passed
工作流/API 测试：23 passed
PlanningPanel + SessionPanel 定向测试：7 passed
npm run lint：通过
npm run build：通过，输出 app/static/workbench/
```

风险/回滚：

- 风险中等偏低。新增设计摘要只读展示和版本元数据字段，不直接修改工程 JSON。
- 设计摘要当前是轻量证据式解释，不应被视为完整设计说明书或需求覆盖门禁。
- 节点索引命中用于辅助解释，命中缺失不代表工程一定不能满足需求，后续覆盖报告需要更严格证据分级。
- 回滚可移除 `design_brief.py` 接入和前端展示；版本元数据里的 `requirement_context` 是向后兼容扩展字段，可保留。

文档同步：

- 已同步 `文档/项目结构.md`。
- 已同步 `文档/演进记录.md`。
- 已在本归档新增 ER-20260501-004。

后续动作：

- 进入 Deadline 第 3 天：新增 `RequirementConformanceReport`，导出前结合结构校验和需求覆盖校验。
- 导出覆盖门禁应明确区分“已覆盖、部分覆盖、缺失、需人工复核”和“找不到需求上下文无法复核”。

DoD：

- `DesignBrief` 服务存在并接入主流程。
- 模板候选包含推荐理由、已满足、需改造、改造成本和风险项。
- 模板确认后设计摘要按选定模板刷新。
- 设计摘要随工程版本元数据保存。
- 前端会话和规划面板展示设计摘要。
- 后端和前端相关回归测试通过。

### ER-20260501-005 需求覆盖校验和导出门禁

状态：已完成

修改人：Codex

关联文件：

- `app/services/requirement_conformance.py`
- `app/main.py`
- `app/graph/nodes.py`
- `app/services/json_project.py`
- `frontend/src/api/types.ts`
- `frontend/src/features/workbench/WorkbenchPage.tsx`
- `frontend/src/features/planning/PlanningPanel.tsx`
- `frontend/src/features/versions/VersionHistoryPanel.tsx`
- `frontend/src/features/graph/GraphPanel.tsx`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `app/static/workbench/**`
- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-01.md`

背景：

`文档/deadline前智能体目标实现方案.md` 将第 3 天目标定为“需求覆盖校验和导出门禁”。第 2 天已把 `RequirementSlots` 和 `DesignBrief` 写入会话/版本上下文，但导出前仍主要证明 JSON 结构合法，不能证明满足用户明确提出的需求；仅按临时 path 导出时也可能没有需求上下文，不能假装覆盖已复核。

目标：

- 新增 `RequirementConformanceReport`，补上“满足用户意图”的验收层。
- 导出前合并结构校验和需求覆盖校验。
- 只在能找到会话/项目/版本需求上下文时执行覆盖门禁。
- 找不到需求上下文时明确提示“需求覆盖未复核”，不伪造覆盖结论。
- 明显缺失的用户明确需求阻塞导出。
- 前端在导出前展示覆盖报告和阻塞原因。

代码/契约变化：

- 新增 `app/services/requirement_conformance.py`：
  - `build_requirement_conformance_report()` 输入工程节点、`requirement_slots` 和 `design_brief`，输出 `context_available`、`valid_for_requirement`、`covered`、`partial`、`missing`、`blocked`、`evidence`、`warnings` 和 `summary`。
  - `merge_conformance_into_validation()` 把需求覆盖结果合并到现有校验报告；需求阻塞会把 `exportable` 置为 `false` 并追加 `blocked_export_reasons`。
  - 覆盖检查只针对用户明确提出或槽位记录的需求；不能自动证明的项标为 `partial`，不假定已满足。
  - 当前显式阻塞规则覆盖 AHU 防冻保护、过滤网报警、机房水泵故障/运行反馈、旁通阀压差控制等 deadline P0 要求场景。
- `app/main.py`：
  - 独立项目校验、按 path 校验和导出 API 均调用 `_validate_with_requirement_gate()`。
  - `_requirement_context_for_project()` 优先从会话状态读取需求上下文，找不到时回退版本元数据。
  - `_persist_conformance_context()` 将覆盖报告保存回会话状态和文件型版本元数据的 `requirement_context.conformance_report`。
  - 导出失败 detail 返回合并后的 `validation_report`，其中包含 `conformance_report` 和需求覆盖阻塞原因。
  - 校验/导出 trace payload 增加需求覆盖复核状态摘要。
- `app/graph/nodes.py`：
  - 工作流 `validate_current_project_node` 接入需求覆盖报告。
  - 初始版本和补丁子版本创建时继续传递需求/设计/覆盖上下文。
- `app/services/json_project.py`：
  - 版本创建元数据支持保存 `conformance_report`。
  - 新增文件型 `update_project_version_metadata()`，供校验/导出后刷新版本元数据中的 `exportable`、校验摘要和覆盖报告摘要。
- 前端契约和展示：
  - `frontend/src/api/types.ts` 新增 `RequirementConformanceReport` 和 `RequirementConformanceEntry`。
  - `WorkbenchPage` 校验后展示“需求覆盖已复核 / 存在阻塞项 / 未复核”提示，并把覆盖摘要写入 UI 状态。
  - `PlanningPanel` 新增“导出门禁”区域，展示阻塞、缺失和需人工复核原因。
  - `VersionHistoryPanel` 在版本标签中展示覆盖未复核、覆盖阻塞或覆盖通过。
  - `GraphPanel` 顶部导出状态改用合并后的 `exportable`，避免只看结构 `valid`。
- 重新构建 `app/static/workbench/`。

影响：

- JSON 结构合法但明显不满足用户明确需求时，不再能静默导出。
- 有项目 ID 或会话/版本上下文的导出会执行需求覆盖门禁。
- 仅 path 导出且无上下文时仍允许结构合法 JSON 导出，但校验报告和前端会明确提示“需求覆盖未复核”。
- 需求覆盖报告是证据式校验，不替代 `validator.py` 的结构安全职责。
- 当前覆盖算法仍是 deadline 版本的轻量规则/关键词证据，无法证明的需求默认进入人工复核，不自动标记 covered。

验证：

```powershell
conda activate midea
pytest -q tests/test_services.py::test_requirement_conformance_blocks_explicit_missing_filter_alarm tests/test_services.py::test_requirement_conformance_reports_not_reviewed_without_context tests/test_workflow_api.py::test_validate_by_path_reports_unreviewed_requirement_coverage tests/test_workflow_api.py::test_export_by_project_blocks_missing_requirement_conformance
pytest -q tests/test_services.py
pytest -q tests/test_requirement_model.py
pytest -q tests/test_workflow_api.py
pytest -q tests/test_template_selection_evals.py tests/test_planner_evals.py tests/test_llm_gateway.py
pytest -q
cd frontend
npm run lint
npm run test
npm run build
```

结果：

```text
需求覆盖门禁定向后端测试：4 passed
tests/test_services.py：51 passed
tests/test_requirement_model.py：14 passed
tests/test_workflow_api.py：25 passed
P0 回归集 tests/test_template_selection_evals.py tests/test_planner_evals.py tests/test_llm_gateway.py：18 passed
全量 pytest：118 passed, 4 skipped
npm run lint：通过
npm run test：29 passed
npm run build：通过，输出 app/static/workbench/
```

备注：

一次 P0 回归中 `tests/test_llm_gateway.py::test_llm_planner_dry_run_api_executes_structured_plan` 曾在 Windows 文件型 trace 写入 `os.replace()` 处出现偶发 `PermissionError`，单测重跑和整组重跑均通过，判断为既有文件锁偶发问题，不属于本次需求覆盖门禁逻辑。

风险/回滚：

- 风险中等。导出 `exportable` 从“只看结构校验”升级为“结构校验 + 有上下文时需求覆盖门禁”，可能改变部分历史会话的导出结果。
- 覆盖证据当前依赖轻量关键词和节点文本，需后续用真实业务样例校准，避免误阻塞或漏阻塞。
- 回滚可在 `app/main.py` 中恢复直接调用 `validate_project()`，移除 `merge_conformance_into_validation()` 参与导出判定；`requirement_context.conformance_report` 作为扩展元数据可保留，不影响旧版本读取。

文档同步：

- 已同步 `文档/项目结构.md`。
- 已同步 `文档/演进记录.md`。
- 已在本归档新增 ER-20260501-005。

后续动作：

- 进入 Deadline 第 4 天：复杂结构改造 planner fallback 和高风险确认强化。
- 后续覆盖报告应逐步接入更多真实 AHU/机房群控业务样例，特别是点表、通讯映射和保护链路证据。
- 需求覆盖证据可继续与局部流程图 diff 结合，让用户从覆盖项定位到相关页签/节点。

DoD：

- `RequirementConformanceReport` 服务存在并有单元测试。
- 校验 API 和导出 API 均返回覆盖报告。
- 有需求上下文且明显缺失时导出被拒绝。
- 无需求上下文时明确提示未复核，不伪造覆盖。
- 版本元数据和会话状态保存最近覆盖报告。
- 前端展示导出门禁和阻塞原因。
- 后端全量测试和前端 lint/test/build 通过。

### ER-20260501-006 复杂 planner fallback 和高风险确认强化

状态：已完成

修改人：Codex

关联文件：

- `app/services/planner.py`
- `app/graph/nodes.py`
- `app/main.py`
- `tests/test_llm_gateway.py`
- `tests/test_workflow_api.py`
- `文档/项目结构.md`
- `文档/演进记录.md`
- `文档/演进记录/2026-05-01.md`

背景：

`文档/deadline前智能体目标实现方案.md` 将第 4 天目标定为“复杂 planner fallback 和高风险确认强化”。此前新会话默认启用 LLM planner，但工作流在 `use_llm_planner=false` 时仍会停在规则 planner 的澄清结果；独立 `/api/planner/dry-run` 也主要依赖显式 `use_llm=true`。对于“新增 CO2 设定并接入控制链路”“新增旁通阀压差设定”“修改 IO 通道”等复杂结构改造，系统需要后端自动识别并尝试 LLM dry-run feedback，而不是完全依赖前端开关。同时，中高风险确认摘要只给出粗粒度风险等级，工程师难以快速判断 `copy_block`、`set_io_point`、连线和断线具体要确认什么。

目标：

- 规则 planner 继续优先处理明确低风险小修改。
- 规则 planner 无法规划且用户输入属于结构改造意图时，后端自动 fallback 到 LLM planner dry-run feedback。
- 前端开关关闭时，后端仍能对复杂结构改造尝试自动 fallback 或返回明确可执行追问。
- 中高风险补丁继续只 dry-run，不确认不落盘。
- 强化 `copy_block`、`set_io_point`、`connect`、`disconnect`、`enable_dynamic_input` 和 `add_node_from_schema` 的确认摘要。
- 增加 AHU/机房群控关键改造样例测试。

代码/契约变化：

- `app/services/planner.py`：
  - 新增 `is_structural_change_intent()`，识别新增、接入、连线、复制、功能块、动态设定/输入、IO/I/O、点位、通道、地址、Modbus、BACnet、通讯、设备数量、台数、旁通阀、压差、CO2 等复杂结构改造意图。
  - 明确排除“添加备注/新增备注/加备注”，避免把低风险备注误判为结构改造 fallback。
- `app/graph/nodes.py`：
  - `plan_patch_node()` 改为规则 planner 优先；规则规划成功直接走原路径。
  - 规则规划失败时，如果命中结构改造意图，或会话仍启用 `use_llm_planner`，自动调用 `plan_patch_with_llm_dry_run_feedback()`。
  - LLM 不可用、schema/dry-run 反馈重试失败时，不抛 500；返回 `awaiting_patch_clarification`，并给出需要补充目标页面、节点 id、端口、点位字段和值的追问。
  - fallback 结果在 `planner_result` 中标记 `fallback_from="rule"`，并保留 `rule_planner_result`，便于 trace 和前端解释规则路径为何失败。
  - `_assess_patch_risk()` 接收 dry-run 结果，生成 `confirmation_summary` 和 `operation_summaries`。
  - LangGraph `patch_confirmation` interrupt payload 额外携带 `confirmation_summary` 和 `operation_summaries`。
  - assistant summary 在待确认状态展示操作级确认摘要，同时保留原有“需要确认后再应用”兼容文案。
- `app/main.py`：
  - 独立 `/api/planner/dry-run` 在 `use_llm=false` 且规则 planner 无补丁时，如果识别为结构改造意图，会自动调用 LLM dry-run fallback。
  - 独立 API fallback 成功时同样标记 `fallback_from="rule"` 和 `rule_planner_result`。
  - 独立 API fallback 失败时返回 `needs_clarification` 结构化结果，而不是把 LLM 不可用直接升级为 500。
- 高风险确认摘要：
  - `copy_block`：说明复制来源、目标页面、复制节点数、边界接线数，并提示确认入口/出口边界和 BACnet 对象号复用风险。
  - `set_io_point`：展示点位字段前后变化，并提示点表字段和值、通道/地址/对象号冲突和现场接线/通讯表复核。
  - `connect`/`disconnect`：展示源输出、目标输入和连线方向，并提示保护、反馈、故障链路风险。
  - `enable_dynamic_input`：提示新增动态输入端口用途和后续连线目标端口。
  - `add_node_from_schema`：提示新增节点类型、目标页面和后续接线/点表复核。
- 测试：
  - `tests/test_workflow_api.py` 新增工作流自动 fallback 测试：AHU “新增 CO2 浓度设定并接入新风阀控制”，即使 `use_llm_planner=false`，也能进入 LLM dry-run fallback 并停在中风险确认。
  - `tests/test_workflow_api.py` 新增 IO 通道修改高风险确认摘要测试，覆盖 `set_io_point` 操作级确认要点。
  - `tests/test_llm_gateway.py` 新增独立 `/api/planner/dry-run` 在 `use_llm=false` 下对“新增旁通阀压差设定”的自动 fallback 测试。

影响：

- 明确的低风险规则补丁仍不调用 LLM，减少不必要的模型依赖。
- 复杂结构改造不再完全依赖 UI 开关；关闭前端开关时，后端仍会自动判断并尝试 LLM dry-run fallback。
- LLM 不可用或 dry-run feedback 后仍失败时，用户看到的是可执行追问，不是服务端错误。
- 中高风险计划仍然不会直接落盘；必须通过 LangGraph interrupt/API 风险确认后才会应用。
- 前端现有风险确认面板可继续读取 `risk_assessment`，新增的 `confirmation_summary/operation_summaries` 是向后兼容扩展字段。

验证：

```powershell
conda activate midea
pytest -q tests/test_workflow_api.py::test_workflow_falls_back_to_llm_for_structural_change_when_switch_is_off tests/test_workflow_api.py::test_workflow_high_risk_io_fallback_has_point_confirmation_summary tests/test_workflow_api.py::test_workflow_plans_disconnect_patch_and_waits_for_confirmation tests/test_workflow_api.py::test_workflow_uses_llm_planner_when_enabled
python -m compileall app/graph/nodes.py app/services/planner.py app/main.py
pytest -q tests/test_workflow_api.py::test_workflow_interrupts_and_resumes_patch_confirmation tests/test_workflow_api.py::test_workflow_llm_planner_requires_confirmation_for_non_low_risk tests/test_workflow_api.py::test_workflow_falls_back_to_llm_for_structural_change_when_switch_is_off tests/test_workflow_api.py::test_workflow_high_risk_io_fallback_has_point_confirmation_summary
pytest -q tests/test_llm_gateway.py::test_planner_dry_run_api_falls_back_to_llm_for_structural_intent tests/test_llm_gateway.py::test_llm_planner_dry_run_api_executes_structured_plan
pytest -q tests/test_services.py tests/test_llm_gateway.py tests/test_planner_evals.py tests/test_workflow_api.py
pytest -q
```

结果：

```text
复杂 planner fallback 和高风险确认定向测试：4 passed
失败兼容/interrupt 确认相关回归：4 passed
独立 planner dry-run fallback API 回归：2 passed
planner/workflow 核心回归集：93 passed
全量 pytest：121 passed, 4 skipped
```

风险/回滚：

- 风险中等。补丁规划路由从“显式 LLM 开关优先”调整为“规则优先 + 复杂意图自动 fallback”，可能改变部分复杂修改请求的交互路径。
- 结构改造意图识别是关键词启发式，可能仍有漏判；误判时 LLM 不可用会返回澄清而不是 500，且低风险规则成功路径不会进入 fallback。
- 操作级确认摘要依赖 dry-run `changes`，如果某些补丁没有返回细粒度 changes，会退回到补丁操作本身的摘要，不影响确认门。
- 回滚可让 `plan_patch_node()` 恢复 `use_llm_planner` 开关直接控制 LLM 路径，并移除 `/api/planner/dry-run` 的自动 fallback；`risk_assessment` 新增字段是向后兼容字段，可保留或删除。

文档同步：

- 已同步 `文档/项目结构.md`。
- 已同步 `文档/演进记录.md`。
- 已在本归档新增 ER-20260501-006。

后续动作：

- 进入 Deadline 第 5 天：增强轻量变更可视化视图。
- 变更可视化应优先消费当前已有 `planner_dry_run.diff`、`patch_result.diff`、`risk_assessment.operation_summaries` 和 `copy_block.boundary_preview`，避免拉取完整工程 JSON。
- 继续补业务验收样例时，应覆盖 CO2 设定接入、旁通阀压差设定、PID 动态设定、IO 点位修改和 copy_block 边界确认。

DoD：

- 规则 planner 仍可处理明确低风险小修改。
- 复杂结构改造在规则 planner 失败时自动 fallback 到 LLM planner dry-run feedback。
- 前端开关关闭时，后端仍能给出自动 fallback 或明确不可用提示。
- 中高风险计划不会直接落盘。
- `copy_block`、`set_io_point`、连线、断线、动态输入和新增节点确认摘要有操作级信息。
- 工作流和独立 planner API 均有 fallback 回归测试。
- 全量后端测试通过。

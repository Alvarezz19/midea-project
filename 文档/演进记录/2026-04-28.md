# 2026-04-28 演进详细记录

来源：从 `文档/演进记录.md` 的“4. 详细记录”拆分归档。

用途：按需追溯 ER-20260428-001 到 ER-20260428-036 的完整实现历史；新对话默认只读取上级 `文档/演进记录.md`。

## 4. 详细记录

### ER-20260428-001 基础工程配置和依赖整理

状态：已完成

修改人：Codex

关联文件：

- `pyproject.toml`
- `.env.example`
- `app/core/__init__.py`
- `app/core/config.py`
- `app/core/logging.py`
- `README.md`
- `文档/项目结构.md`

背景：项目缺少依赖声明、环境变量示例和集中配置入口，不利于持续开发和部署准备。

目标：建立轻量工程配置基础，不引入不必要复杂依赖。

代码/契约变化：

- 新增 `pyproject.toml`，声明核心依赖 `fastapi`、`uvicorn`、`langgraph`、`pydantic`，声明 dev 和 postgres 可选依赖。
- 新增 `.env.example`，记录应用名、版本、日志级别、版本目录、session 目录、LangGraph checkpointer 后端、数据库和 LLM 变量名。
- 新增 `app/core/config.py`，通过环境变量加载 `Settings`。
- 新增 `app/core/logging.py`，提供基础日志初始化。

影响：

- 本地安装可使用 `python -m pip install -e ".[dev]"`。
- 后续服务模块统一从 `settings` 读取目录和运行配置。

验证：

- `python scripts\build_indexes.py`
- `pytest -q`
- 最近结果：索引规模 5/31/8413，`34 passed`

风险/回滚：

- 风险低，未改变业务流程。
- 回滚可删除新增配置文件，并在 `app/main.py` 恢复硬编码应用名和版本。

文档同步：

- README 已补充依赖安装和运行时目录说明。
- `文档/项目结构.md` 已补充 `app/core` 和 `pyproject.toml`。

后续动作：

- 生产部署前补依赖锁定策略。
- 接入数据库时补真实 `DATABASE_URL` 校验和启动前检查。

DoD：

- 配置模块可导入。
- `.env.example` 不包含真实密钥。
- 全量测试通过。

### ER-20260428-002 LangGraph 单例编译和 thread_id 调用

状态：已完成

修改人：Codex

关联文件：

- `app/graph/workflow.py`
- `app/main.py`
- `tests/test_workflow_api.py`
- `文档/项目结构.md`

背景：原 `invoke_workflow()` 每次调用都会重新构建并编译 graph，不适合生产运行。

目标：进程内复用已编译 graph，并为 LangGraph 原生 `thread_id` 配置打基础。

代码/契约变化：

- 新增 `get_workflow()`，使用进程内锁保护单例编译。
- `invoke_workflow(state, thread_id=..., workflow=...)` 支持传入已编译 graph。
- API 层通过 `app.state.workflow` 复用 graph。
- 无 `thread_id` 的直接调用使用临时 `adhoc_*` thread id。

影响：

- API 请求不再重复编译 graph。
- 测试中直接调用 `invoke_workflow(state)` 仍兼容。

验证：

- 增加单例复用测试。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中低，graph 节点行为未改。
- 回滚可恢复 `invoke_workflow()` 内部直接 `build_workflow()`。

文档同步：

- `文档/项目结构.md` 已说明 `workflow.py` 负责单例编译。

后续动作：

- 接入 PostgreSQL checkpointer 后，单例编译应注入持久化 checkpointer。

DoD：

- `get_workflow() is get_workflow()`。
- API 通过 `app.state.workflow` 调用。
- 全量测试通过。

### ER-20260428-003 `wires` 方向术语统一为 `input_sources`

状态：已完成

修改人：Codex

关联文件：

- `scripts/build_indexes.py`
- `app/services/retrieval.py`
- `app/services/validator.py`
- `indexes/nodes/node_index.jsonl`
- `knowledge/工程规范.md`
- `README.md`
- `文档/项目结构.md`
- `tests/test_services.py`

背景：项目 JSON 中的 `wires` 表示“当前节点输入端连接的上游源”，原索引字段 `wires_to` 容易误导。

目标：统一术语，避免后续检索、补丁和校验维护误判方向。

代码/契约变化：

- 索引字段由 `wires_to` 改为 `input_sources`。
- `scripts/build_indexes.py` 的提取函数改为 `extract_input_sources()`。
- `retrieval.py` 的邻域图按上游/下游语义构建。
- `validator.py` 的错误码和文案改为上游源语义。
- 检索排序保留旧 `wires_to` 兼容读取逻辑。

影响：

- 新生成的 `indexes/nodes/node_index.jsonl` 不再包含 `wires_to`。
- 后续功能块索引、diff 和局部图应沿用 `input_sources` 语义。

验证：

- 索引构建规模保持：模板 5、页面 31、节点 8413。
- 测试断言节点索引包含 `input_sources` 且不包含 `wires_to`。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中等，依赖旧字段的未发现外部脚本可能受影响。
- 回滚可恢复索引字段名；服务层当前仍兼容旧 `wires_to`。

文档同步：

- README、`knowledge/工程规范.md`、`文档/项目结构.md` 已同步。

后续动作：

- 功能块索引、前端局部图、copy_block 继续使用 `input_sources`。

DoD：

- 新索引无 `wires_to`。
- 邻域检索结果测试保持稳定。
- 全量测试通过。

### ER-20260428-004 PostgreSQL 初始 schema 迁移

状态：已完成

修改人：Codex

关联文件：

- `migrations/postgres/001_initial_schema.sql`
- `tests/test_migrations.py`
- `pyproject.toml`
- `文档/项目结构.md`

背景：阶段 1 要求设计 PostgreSQL 表结构和迁移脚本。

目标：提供生产数据库的初始业务 schema，先完成可审阅、可测试的 SQL 契约。

代码/契约变化：

- 新增 `schema_migrations`。
- 新增业务表：`users`、`projects`、`project_versions`、`patch_records`、`validation_reports`、`sessions`、`audit_events`。
- 新增索引表：`template_catalog`、`tab_index`、`node_index`、`block_index`、`knowledge_chunks`。
- `project_versions` 包含父版本、模板来源、patch、validation、hash、路径、exportable。
- `node_index` 使用 `input_sources`。
- 可选依赖 `postgres` 增加 `psycopg[binary]` 和 `langgraph-checkpoint-postgres`。

影响：

- 仓库具备 PostgreSQL schema 基线。
- 应用运行时尚未执行迁移，也未连接 PostgreSQL。

验证：

- `tests/test_migrations.py` 校验核心表、版本审计字段、`input_sources` 术语。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险低，未改变运行时代码路径。
- 回滚可删除迁移文件和迁移测试。

文档同步：

- `文档/项目结构.md` 已补充 `migrations/postgres/001_initial_schema.sql`。

后续动作：

- 接入 Alembic 或明确 SQL 迁移执行流程。
- 有数据库实例后验证 SQL 实际执行。

DoD：

- 初始 SQL 覆盖方案列出的核心表。
- 迁移结构测试通过。

### ER-20260428-005 文件型 session store

状态：已完成

修改人：Codex

关联文件：

- `app/services/session_store.py`
- `app/main.py`
- `.env.example`
- `.gitignore`
- `tests/test_session_store.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：原 `SESSIONS` 是 `app/main.py` 进程内字典，重启丢失且职责不清。

目标：先建立可替换的 session 存储边界，本地通过文件持久化验收。

代码/契约变化：

- 新增 `FileSessionStore`。
- session 存储目录默认 `projects/sessions`，可通过 `PROJECT_SESSIONS_DIR` 配置。
- `app.main` 不再直接持有 `SESSIONS`。
- session 文件按 `thread_id.json` 保存。
- 非法 `thread_id` 路径字符会被拒绝。
- `.gitignore` 忽略 `projects/sessions/`。

影响：

- API session 可跨应用重启保留本地状态。
- 多进程共享仍不是生产级保证，后续应迁移 PostgreSQL。

验证：

- 覆盖保存、重载、按项目查找、非法 thread id。
- API 端到端测试使用临时 session store，避免污染真实目录。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中等，session 存取路径从内存变为文件。
- 回滚可恢复内存字典，但会丧失重启保留能力。

文档同步：

- README、`文档/项目结构.md` 已同步运行时目录和状态边界。

后续动作：

- 用 PostgreSQL `sessions` 表替换文件 store。
- 增加 session 过期清理和并发写保护。

DoD：

- 不再出现运行时代码直接访问 `SESSIONS`。
- session store 测试通过。
- API 端到端测试通过。

### ER-20260428-006 LangGraph 内存 checkpointer

状态：已完成

修改人：Codex

关联文件：

- `app/graph/checkpointing.py`
- `app/graph/workflow.py`
- `.env.example`
- `pyproject.toml`
- `tests/test_workflow_api.py`
- `文档/项目结构.md`

背景：LangGraph 原生恢复需要 checkpointer 和 `configurable.thread_id`。

目标：在无数据库实例条件下接入可测试的内存 checkpointer，并保留 PostgreSQL 后端扩展位。

代码/契约变化：

- 新增 `get_checkpointer()`。
- `LANGGRAPH_CHECKPOINTER_BACKEND` 默认 `memory`。
- `workflow.compile(checkpointer=...)` 支持 checkpointer 注入。
- `invoke_workflow()` 始终以 `thread_id` config 调用。

影响：

- 当前进程内可通过 thread id 读取 graph checkpoint 快照。
- 重启后 LangGraph checkpoint 不保留；session store 仍保留 API 状态。

验证：

- 测试覆盖 `get_workflow().get_state({"configurable": {"thread_id": ...}})`。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中等，LangGraph invoke 始终带 thread config。
- 回滚可将 `build_workflow()` 改回无 checkpointer 编译。

文档同步：

- `文档/项目结构.md` 已同步。

后续动作：

- 有 PostgreSQL 实例后接入 `langgraph-checkpoint-postgres` 并执行 `.setup()`。
- 实现 interrupt/resume 前必须保证持久化 checkpointer。

DoD：

- 同一 thread id 可读取 checkpoint state。
- 全量测试通过。

### ER-20260428-007 不可变子版本和版本列表

状态：已完成

修改人：Codex

关联文件：

- `app/services/json_project.py`
- `app/graph/nodes.py`
- `app/main.py`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：原补丁应用直接保存到当前版本路径，违背“每次修改创建新版本，不覆盖旧版本”。

目标：补丁成功后创建子版本，并让版本列表从元数据读取。

代码/契约变化：

- `create_project_version()` 元数据新增 `parent_version_id`、`json_sha256`、`exportable`。
- 新增 `create_project_version_from_nodes()`。
- 新增 `list_project_versions()`。
- 工作流补丁应用成功后写入新 `{version_id}.json` 和 `{version_id}.meta.json`，并更新当前 state 指针。
- `/api/projects/{project_id}/versions` 改为读取 `.meta.json`。

影响：

- 父版本 JSON 保持不可变。
- 每次有效补丁会增加版本文件和元数据文件。

验证：

- 服务层覆盖父版本不被覆盖、子版本元数据、版本列表顺序。
- API 端到端覆盖补丁后版本列表包含父子两个版本。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中等，版本文件数量增加。
- 回滚可恢复覆盖保存，但不建议。

文档同步：

- README、`文档/项目结构.md` 已同步。

后续动作：

- 版本元数据迁移到 PostgreSQL。
- 增加版本 diff API。

DoD：

- 补丁后 `current_project_version_id` 变化。
- 原版本文件内容不变。
- 版本列表来自元数据。

### ER-20260428-008 回滚接口

状态：已完成

修改人：Codex

关联文件：

- `app/main.py`
- `app/services/json_project.py`
- `app/services/session_store.py`
- `tests/test_services.py`
- `tests/test_session_store.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：阶段 1 验收要求版本可回滚且回滚不删除历史版本。

目标：提供基于当前文件版本模型的回滚 API。

代码/契约变化：

- 新增 `get_project_version()`。
- `FileSessionStore` 新增 `list_items()` 和 `find_items_by_project_id()`。
- 新增 `POST /api/projects/{project_id}/rollback`，请求体字段 `target_version_id`。
- 回滚前加载目标版本并执行基础校验。
- 回滚只切换 session 当前版本指针，不删除任何版本文件。

影响：

- 当前项目可回到任一已有且校验通过的版本。
- 多 session 指向同一 project 时，会同步更新同项目 session 指针。

验证：

- API 端到端覆盖补丁后回滚到父版本、历史版本仍保留、导出内容回到父版本。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中等，当前实现以 session store 为项目指针来源，不是最终数据库事务。
- 回滚接口可移除；历史版本文件不受影响。

文档同步：

- README、`文档/项目结构.md` 已同步。

后续动作：

- PostgreSQL 项目表接入后，将当前版本指针迁移到 `projects.current_version_id`。
- 增加审计事件。

DoD：

- 回滚不删除历史版本。
- 回滚后 export 返回目标版本。
- 全量测试通过。

### ER-20260428-009 补丁 dry-run 和节点级 diff 摘要

状态：已完成

修改人：Codex

关联文件：

- `app/services/patch_engine.py`
- `app/graph/nodes.py`
- `app/main.py`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：方案要求补丁执行前具备 dry-run、diff 预览、影响范围和确认基础。

目标：提供无副作用 dry-run，返回可给前端展示的节点级 diff 摘要。

代码/契约变化：

- 新增 `dry_run_patch()`。
- 新增 `dry_run_patch_to_project()`。
- 新增 `build_node_diff()`。
- `apply_patch()` 返回 `diff`。
- 工作流应用补丁时先 dry-run，校验通过后创建子版本。
- 新增 `POST /api/planner/dry-run`。
- dry-run API 支持传 `message` 走 planner，或传 `pending_patch` 直接 dry-run。
- dry-run API 返回中移除完整 `nodes`，避免泄漏大工程 JSON。

影响：

- 前端可展示 `diff.summary`、`affected_node_ids`、新增/删除/修改节点摘要。
- 后续高风险确认和 apply confirmation 可复用该结果。

验证：

- 服务层覆盖 dry-run 不修改源节点、diff 字段变化正确。
- API 覆盖 dry-run 不保存、不返回 `nodes`。
- 工作流补丁结果包含 diff。
- 最近全量验收：`34 passed`

风险/回滚：

- 风险中等，diff 基于节点 id 对比；若未来允许节点 id 重写，需要扩展 diff 语义。
- 回滚可移除 dry-run API，并恢复工作流直接 `apply_patch()`。

文档同步：

- README、`文档/项目结构.md` 已同步。

后续动作：

- 增加 diff API：`/api/projects/{project_id}/diff`。
- 增加风险分级和人工确认。
- 前端展示 diff 摘要。

DoD：

- dry-run 不保存文件。
- dry-run 不返回完整工程节点数组。
- diff 摘要包含 affected node ids。
- 全量测试通过。

### ER-20260428-010 项目版本 diff API

状态：已完成

修改人：Codex

关联文件：

- `app/services/project_diff.py`
- `app/main.py`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：已有补丁 dry-run 能返回节点级 diff，但缺少按项目历史版本查询 diff 的正式接口，前端无法稳定展示版本差异。

目标：基于不可变版本文件提供可复用的版本对比服务和 API，不引入新的存储依赖。

代码/契约变化：

- 新增 `diff_project_versions()`，按 `project_id`、`from_version_id`、`to_version_id` 读取版本元数据和工程文件。
- 新增 `GET /api/projects/{project_id}/diff`。
- `to_version_id` 缺省时使用当前项目版本。
- `from_version_id` 缺省时使用目标版本的 `parent_version_id`。
- 返回 `from_version`、`to_version` 和节点级 `diff`，不返回完整工程节点数组。
- 目标版本没有父版本且未显式提供 `from_version_id` 时返回 400。

影响：

- 前端可直接查询当前版本相对父版本的差异。
- 回滚和版本列表仍保持原行为，不删除历史版本。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_project_diff_between_versions_returns_node_summary tests\test_workflow_api.py::test_api_end_to_end -q`
  - 结果：`2 passed`
- 最近全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：索引规模 5/31/8413，`35 passed`

风险/回滚：

- 风险低，diff 只读版本文件，不产生外部副作用。
- 当前 diff 基于节点 id 对比；未来 `copy_block` 或重写 id 时需要增加更丰富的映射语义。
- 回滚可移除 `project_diff.py` 和 `/api/projects/{project_id}/diff` 路由，不影响版本创建和回滚。

文档同步：

- README 已增加 diff API 和当前能力说明。
- `文档/项目结构.md` 已补充 `project_diff.py`。

后续动作：

- 前端展示版本 diff 摘要。
- 增加更完整的 JSON diff 可视化和局部图定位。
- 后续 PostgreSQL 版本表接入后，diff 元数据读取应迁移到数据库。

DoD：

- 服务层可对比两个版本并返回节点级 diff。
- API 可默认对比当前版本与父版本。
- API 可显式对比任意两个版本。
- diff 响应不包含完整工程节点数组。
- 全量测试通过。

### ER-20260428-011 导出前阻塞检查

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `app/main.py`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：方案要求“所有导出必须经过校验服务，并且校验报告中不能有阻塞级错误”，现有导出接口只做路径检查和文件返回。

目标：在不引入新存储依赖的前提下，让导出接口统一执行最终校验，并把阻塞原因写入校验报告契约。

代码/契约变化：

- `validate_project()` 返回新增字段：
  - `exportable`
  - `blocked_export_reasons`
- `GET /api/projects/{project_id}/export` 和 `GET /api/projects/export` 统一调用导出前校验。
- 存在 error 级问题时返回 400，并在响应中包含 `validation_report`。
- 有效工程的导出行为保持不变。

影响：

- 无效工程 JSON 不再能绕过校验直接导出。
- 前端后续可直接读取 `exportable` 和 `blocked_export_reasons` 展示导出检查结果。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_existing_templates_validate_cleanly tests\test_workflow_api.py::test_api_end_to_end tests\test_workflow_api.py::test_export_rejects_invalid_project_json -q`
  - 结果：`3 passed`
- 最近全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：索引规模 5/31/8413，`36 passed`

风险/回滚：

- 风险低，有效工程导出仍返回原 JSON 文件。
- 当前阻塞规则只基于 `error_count`，未来校验体系扩展 L4/L5/L6 后需要加入风险级别和策略配置。
- 回滚可移除 `_export_valid_project_file()` 的校验调用，但不建议。

文档同步：

- README 已增加导出前阻塞检查能力说明。
- `文档/项目结构.md` 已同步当前导出边界。

后续动作：

- 校验报告格式继续向方案中的 `summary`、`suggestion`、多级阻塞策略演进。
- 前端增加导出前检查面板。

DoD：

- 校验报告可表达是否允许导出。
- 路径导出拒绝 error 级无效工程。
- 项目导出拒绝 error 级无效工程。
- 有效工程导出不受影响。
- 全量测试通过。

### ER-20260428-012 功能块索引和检索 API

状态：已完成

修改人：Codex

关联文件：

- `scripts/build_indexes.py`
- `app/services/retrieval.py`
- `app/main.py`
- `indexes/blocks/block_index.jsonl`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：方案阶段 3 要求建立功能块级索引，支撑后续局部上下文加载、`copy_block` 和 LLM 结构化 planner。此前只有模板、页面、节点三层索引。

目标：一次性补齐可运行的功能块索引构建、检索服务和 API，优先覆盖现有模板中可验证的典型局部逻辑。

代码/契约变化：

- `scripts/build_indexes.py` 新增生成 `indexes/blocks/block_index.jsonl`。
- 功能块按 tab 总览和功能关键词聚合两类生成。
- 功能关键词覆盖水泵、旁通阀、阀门、防冻、排风机、直膨机、冷却塔、主机、故障反馈、PID、CO2。
- 功能块记录：
  - `block_id`
  - `template_id`
  - `source_path`
  - `tab_id` / `tab_label`
  - `function_type`
  - `node_ids`
  - `anchor_node_ids`
  - `entry_node_ids`
  - `exit_node_ids`
  - `node_type_counts`
  - `risk_tags`
  - `text_for_search`
- `app/services/retrieval.py` 新增 `load_block_index()` 和 `search_blocks()`。
- `app/main.py` 新增 `POST /api/blocks/search`。

影响：

- 索引层级从三层升级为四层：模板、页面、功能块、节点。
- 后续 planner 可先检索局部功能块，不必把完整工程 JSON 放入上下文。
- 当前功能块提取是确定性启发式，不依赖 LLM 或向量库。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_retrieval_searches_function_blocks tests\test_services.py::test_index_files_are_parseable tests\test_workflow_api.py::test_api_end_to_end -q`
  - 结果：`3 passed`
- 最近全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`37 passed`

风险/回滚：

- 风险中等，索引构建输出新增文件，依赖旧索引目录的外部脚本需要适配。
- 当前功能块提取基于关键词和一跳上下游，能覆盖水泵、旁通阀、排风机、直膨机等现有模板中存在的块；模板中未出现的防冻文本无法凭空生成有效工程块。
- 回滚可移除 `block_index.jsonl` 生成和 `/api/blocks/search`，不影响模板、页面、节点检索。

文档同步：

- README 已补充功能块索引、API 和索引规模。
- `文档/项目结构.md` 已补充 `indexes/blocks/block_index.jsonl`。

后续动作：

- 增加从 `block_id` 加载局部 JSON 片段的上下文 API。
- 为 `copy_block` 复用 `node_ids`、入口、出口和风险标签。
- 扩展模板或标注后，补齐防冻保护等当前模板缺失的块。

DoD：

- 索引构建生成 `block_index.jsonl`。
- 功能块检索能命中水泵、旁通阀、排风机等典型块。
- API 可按模板和项目类型检索功能块。
- 全量测试通过。

### ER-20260428-013 LLM Gateway 和结构化需求抽取

状态：已完成

修改人：Codex

关联文件：

- `app/core/config.py`
- `.env.example`
- `app/services/llm_gateway.py`
- `app/services/requirement_extractor.py`
- `app/main.py`
- `tests/test_llm_gateway.py`
- `README.md`
- `文档/项目结构.md`

背景：方案阶段 4 要求新增 LLM Gateway、需求抽取 prompt 和 schema。用户明确要求如需引入 LLM，使用 DeepSeek API，同时预留其他主流 LLM 接口，并允许读取 `.env` 和使用真实 LLM 环境验收。

调研依据：

- DeepSeek 官方文档说明 API 兼容 OpenAI/Anthropic，OpenAI 兼容 `base_url` 为 `https://api.deepseek.com`。
- DeepSeek 官方 JSON Output 文档说明使用 `response_format={"type":"json_object"}`，并要求 prompt 中明确要求 JSON。

目标：先建立可替换的 LLM 调用边界，并落地一个可验收的结构化需求抽取能力，不让 LLM 直接修改工程 JSON。

代码/契约变化：

- `app/core/config.py` 增加轻量 `.env` 加载；进程环境变量优先。
- 新增 LLM 配置：
  - `LLM_PROVIDER`
  - `LLM_TIMEOUT_SECONDS`
  - `LLM_TEMPERATURE`
  - `LLM_MAX_TOKENS`
  - `DEEPSEEK_*`
  - `OPENAI_*`
  - `AZURE_OPENAI_*`
  - `ANTHROPIC_*`
- 新增 `app/services/llm_gateway.py`：
  - 默认 provider 为 `deepseek`
  - 支持 DeepSeek/OpenAI/Azure OpenAI 的 OpenAI-compatible Chat Completions 调用
  - Anthropic 配置入口已预留，Messages API 尚未实际接入
  - 结构化输出使用 JSON Output，并在服务层解析 JSON
- 新增 `app/services/requirement_extractor.py`：
  - Pydantic schema：项目类型、置信度、摘要、设备、控制功能、通讯、IO、保护逻辑、缺失字段、澄清问题、风险等级
  - prompt 明确要求只输出 JSON
  - LLM 输出必须通过 Pydantic 二次校验
- 新增 `POST /api/requirements/extract`。

影响：

- 项目具备可替换的 LLM 调用边界。
- 后续模板选择、澄清问题和 LLM 结构化 planner 可复用需求抽取结果。
- 当前 LLM 仍只生成结构化需求，不执行补丁、不写文件、不导出工程。

验证：

- 本地可重复测试：
  - `pytest tests\test_llm_gateway.py -q`
  - 结果：`3 passed, 1 skipped`
- 真实 DeepSeek 集成测试：
  - `$env:RUN_LLM_INTEGRATION='1'; pytest tests\test_llm_gateway.py::test_requirement_extractor_real_deepseek -q -s`
  - 结果：`1 passed`
- 最近全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`40 passed, 1 skipped`

风险/回滚：

- 风险中等，新增外部 LLM 调用路径；默认 API 不会自动调用，只有显式请求 `/api/requirements/extract` 才会触发。
- `.env` 读取不会输出密钥；后续生产环境应改为密钥管理或部署环境变量注入。
- DeepSeek JSON Output 保证合法 JSON，但不保证符合业务 schema，因此已增加 Pydantic 二次校验。
- 回滚可移除 `llm_gateway.py`、`requirement_extractor.py` 和 `/api/requirements/extract`，不会影响确定性补丁链路。

文档同步：

- README 已增加 LLM 配置、需求抽取 API 和真实 LLM 验收命令。
- `文档/项目结构.md` 已补充 LLM Gateway 和需求抽取服务。

后续动作：

- 将需求抽取接入 LangGraph 的 `extract_requirement` 节点。
- 基于功能块检索结果实现 LLM 结构化 planner dry-run。
- 接入 Anthropic Messages API 或通过兼容代理实现。

DoD：

- LLM Gateway 可用 DeepSeek 真实 API 返回 JSON。
- 结构化需求抽取通过 Pydantic 校验。
- API 可触发需求抽取。
- 普通全量测试不依赖外部 LLM。
- 真实 DeepSeek 集成测试通过。

### ER-20260428-014 功能块局部上下文加载

状态：已完成

修改人：Codex

关联文件：

- `app/services/retrieval.py`
- `app/main.py`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`

背景：阶段 3 不仅要求生成功能块索引，还要求后续 LLM 调用不依赖完整工程 JSON，而是加载预算受控的局部上下文。此前已完成 `block_index.jsonl` 和 `/api/blocks/search`，但还缺少按 `block_id` 加载局部 JSON 片段的正式服务边界。

目标：补齐阶段 3 剩余核心项，为后续 `copy_block` 和 LLM 结构化 planner 提供局部上下文输入。

代码/契约变化：

- `app/services/retrieval.py` 新增：
  - `get_block_by_id()`
  - `load_block_context()`
- `load_block_context()` 返回：
  - `block` 功能块公开元数据
  - `source_path`
  - `nodes` 节点摘要
  - `internal_edges` 内部边
  - `boundary.inbound_edges`
  - `boundary.outbound_edges`
  - `missing_node_ids`
  - `budget`
- 支持参数：
  - `max_nodes`
  - `max_chars`
  - `include_raw_nodes`
- 默认不返回 raw 节点，避免把大工程片段无控制地塞入上下文。
- 新增 `POST /api/blocks/context`。

影响：

- 阶段 3 的“功能块索引与局部上下文”主链路闭环。
- 后续 LLM planner 可按 `block_id` 获取局部上下文，不需要完整工程 JSON。
- `copy_block` 可复用 `node_ids`、内部边、入口、出口和风险标签。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_retrieval_loads_block_context_with_budget tests\test_services.py::test_retrieval_reports_missing_node tests\test_workflow_api.py::test_api_end_to_end -q`
  - 结果：`3 passed`
- 最近全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`41 passed, 1 skipped`

风险/回滚：

- 风险低到中，新增 API 为只读上下文加载，不修改工程 JSON。
- 当前上下文预算按 JSON 字符估算；后续接入真实模型时可升级为 token 估算。
- 当前功能块仍依赖启发式索引质量；模板补充和人工标注会提升上下文准确性。
- 回滚可移除 `load_block_context()` 和 `/api/blocks/context`，不影响原功能块检索。

文档同步：

- README 已增加 `/api/blocks/context`。
- `文档/项目结构.md` 已同步 retrieval 职责和当前边界。

后续动作：

- 阶段 3 后续增强可做 schema 与节点索引更深关联，但核心闭环已完成。
- 下一步应基于 block context 推进 LLM 结构化 planner dry-run，而不是直接让 LLM 修改 JSON。

DoD：

- 可按 `block_id` 加载局部上下文。
- 上下文包含节点摘要、内部边、边界边。
- 上下文受 `max_nodes` 和 `max_chars` 控制。
- API 可用，非法 block id 有明确错误。
- 全量测试通过。

### ER-20260428-015 LLM 结构化 planner dry-run

状态：已完成

修改人：Codex

关联文件：

- `app/services/llm_planner.py`
- `app/main.py`
- `tests/test_llm_gateway.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 4 要求 planner 从规则/正则升级为“LLM 结构化规划 + 确定性工具执行 + 强校验”。此前已完成 LLM Gateway、需求抽取和功能块局部上下文，但 LLM 尚未参与补丁规划主链路。

目标：在不破坏现有确定性 planner 的前提下，新增显式开启的 LLM 结构化补丁规划 dry-run。LLM 只能输出补丁意图，不能直接写工程 JSON。

代码/契约变化：

- 新增 `app/services/llm_planner.py`：
  - `plan_patch_with_llm()`
  - `StructuredPatchPlan`
  - `PlannedOperation`
  - `LLMPlannerError`
- LLM planner 会收集：
  - 项目摘要
  - 页面列表
  - 知识库检索结果
  - tab/node 检索结果
  - 功能块检索结果
  - `load_block_context()` 返回的局部上下文
- LLM 输出使用 Pydantic 校验，允许的 op 限定为当前 patch engine 已实现的：
  - `update_param`
  - `rename_node`
  - `add_comment`
  - `add_node_from_schema`
  - `connect`
  - `disconnect`
- `POST /api/planner/plan` 和 `POST /api/planner/dry-run` 新增可选参数：
  - `use_llm`
  - `provider`
- 默认 `use_llm=false`，继续使用原确定性 planner。
- `use_llm=true` 且生成计划后，dry-run 仍由 `dry_run_patch_to_project()` 执行，并继续移除返回中的完整 `nodes`。

影响：

- 阶段 4 已具备 LLM 结构化 planner dry-run 的最小闭环。
- 可支持低/中风险多步补丁规划，例如 `add_node_from_schema` + `connect`。
- 未知 op 会在 LLM planner schema 层被拒绝，不会进入 patch engine。
- 旧 API 调用不受影响。

验证：

- 定向测试：
  - `pytest tests\test_llm_gateway.py -q`
  - 结果：`6 passed, 1 skipped`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`44 passed, 1 skipped`

风险/回滚：

- 风险中等，新增能力依赖真实 LLM 输出质量，但默认关闭。
- 当前仍未接入 LangGraph interrupt，高风险确认还停留在后续阶段。
- 当时尚未建立 LLM planner 评测集；已由 ER-20260428-016 补齐第一版样例和真实结构测试。
- 回滚可移除 `use_llm` 分支和 `llm_planner.py`，不影响现有规则 planner。

文档同步：

- README 已补充 LLM planner dry-run 能力、调用示例和测试基线。
- `文档/项目结构.md` 已补充 `llm_planner.py` 职责和当前边界。

后续动作：

- 扩充 `evals/patch_planning_cases.jsonl`，增加更多 AHU 和机房群控真实改造样例。
- 持续对真实 DeepSeek planner 输出做集成验收。
- 增加高风险识别后的人工确认流程，后续再接入 LangGraph interrupt。

DoD：

- LLM planner 只输出结构化补丁意图。
- 支持多步补丁 schema 校验。
- 未知 op 被拒绝。
- dry-run API 可执行 LLM 计划并返回 diff/校验摘要。
- 默认旧规则 planner 不受影响。
- 全量测试通过。

### ER-20260428-016 LLM planner 评测集和真实结构测试

状态：已完成

修改人：Codex

关联文件：

- `evals/patch_planning_cases.jsonl`
- `tests/test_planner_evals.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 4 不能只实现 LLM planner 接口，还需要可持续评测 LLM 输出是否符合结构化补丁规划约束。用户要求真实 LLM 的结构测试也必须覆盖。

目标：建立第一版补丁规划评测集，并提供普通离线测试和显式开启的真实 DeepSeek 结构化 planner 测试。

代码/契约变化：

- 新增 `evals/patch_planning_cases.jsonl`，首批覆盖：
  - 机房群控水泵比较阈值修改。
  - 机房群控新增常量并连到比较节点。
  - 机房群控添加备注。
  - AHU PID 比例增益节点改名。
  - 目标不唯一时返回追问。
- 每个样例包含：
  - `case_id`
  - `project_type`
  - `template_id`
  - `template_path`
  - `message`
  - `expected`
  - `reference_plan`
- 新增 `tests/test_planner_evals.py`：
  - 离线测试校验所有 golden plan 符合 `StructuredPatchPlan`。
  - planned 样例必须能被 patch engine dry-run 且 diff 摘要符合预期。
  - needs_clarification 样例必须包含追问。
  - 真实 DeepSeek 测试会调用 `plan_patch_with_llm()`，再对返回 `pending_patch` 做 dry-run。
- 真实 LLM 测试默认跳过，需显式设置 `RUN_LLM_INTEGRATION=1`。

影响：

- 阶段 4 具备第一版质量门槛，后续 prompt、schema、上下文或 planner 调整可以通过评测样例防止回归。
- 普通全量测试不消耗 LLM 调用。
- 真实 LLM 结构测试可验证 DeepSeek 返回是否能进入确定性 dry-run 链路。

验证：

- 定向离线测试：
  - `pytest tests\test_planner_evals.py -q`
  - 结果：`1 passed, 1 skipped`
- 真实 DeepSeek 结构测试：
  - `$env:RUN_LLM_INTEGRATION='1'; pytest tests\test_planner_evals.py::test_llm_planner_real_deepseek_returns_structured_dry_runnable_plan -q -s`
  - 结果：`1 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`45 passed, 2 skipped`

风险/回滚：

- 风险低，新增评测文件和测试不改变运行时补丁执行行为。
- 真实 LLM 测试受网络、密钥、额度和模型输出波动影响，因此默认跳过。
- 当前评测集规模仍小，只能作为第一版质量门槛，不能代表生产成功率。
- 回滚可移除 `evals/patch_planning_cases.jsonl` 和 `tests/test_planner_evals.py`，不影响 API。

文档同步：

- README 已补充评测目录和真实 planner 验收命令。
- `文档/项目结构.md` 已补充 `evals/` 和 `tests/test_planner_evals.py`。

后续动作：

- 扩充评测集到阶段 4 目标样例量，覆盖更多 AHU 和机房群控场景。
- 增加 LLM planner 失败反馈重试：schema 校验失败、dry-run 失败、选择器不唯一时把错误回填给模型重新规划。
- 补齐高风险确认和审计记录。

DoD：

- 有可维护的 patch planning JSONL 评测集。
- golden plan 可离线校验和 dry-run。
- 真实 DeepSeek planner 结构测试通过。
- 普通全量测试默认不调用真实 LLM。
- 全量测试通过。

### ER-20260428-017 LLM planner 失败反馈重试

状态：已完成

修改人：Codex

关联文件：

- `app/services/llm_planner.py`
- `app/main.py`
- `tests/test_llm_gateway.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：LLM 结构化 planner 已能生成补丁计划，但真实模型输出可能出现 schema 不合规、选择器不唯一、dry-run 执行失败或校验失败。生产级方案要求失败时返回结构化错误并可重试，而不是把潜在 bug 留给下一阶段。

目标：建立第一版失败反馈重试机制，让模型在明确错误反馈下重新输出完整结构化补丁计划。

代码/契约变化：

- `plan_patch_with_llm()` 新增参数：
  - `max_attempts`
  - `feedback_messages`
- LLM 输出未通过 `StructuredPatchPlan` schema 校验时，会把 schema 错误摘要反馈给模型重试。
- `/api/planner/plan` 和 `/api/planner/dry-run` 新增 `llm_max_attempts`，范围 1 到 3。
- `/api/planner/dry-run` 在 `use_llm=true` 且未手动传入 `pending_patch` 时启用失败反馈重试。
- dry-run 失败反馈覆盖：
  - patch engine 执行失败，例如选择器不唯一。
  - dry-run 校验未通过，例如拓扑引用错误。
- API 返回中新增 `planner_attempts`，用于查看每次规划状态、风险等级、操作数量和失败原因。
- 手动传入 `pending_patch` 时仍按原确定性 dry-run 行为执行，不做自动重试。

影响：

- LLM planner 从“一次性生成”升级为“可反馈修正”的结构化规划链路。
- 目标不唯一等问题可被反馈给 LLM 重新规划或转为追问。
- 普通规则 planner 和手动补丁 dry-run 不受影响。

验证：

- 定向测试：
  - `pytest tests\test_llm_gateway.py tests\test_planner_evals.py -q`
  - 结果：`9 passed, 2 skipped`
- 真实 DeepSeek planner 结构测试：
  - `$env:RUN_LLM_INTEGRATION='1'; pytest tests\test_planner_evals.py::test_llm_planner_real_deepseek_returns_structured_dry_runnable_plan -q -s`
  - 结果：`1 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`47 passed, 2 skipped`

风险/回滚：

- 风险中等，失败反馈重试会增加真实 LLM 调用次数；已用 `llm_max_attempts` 限制最多 3 次。
- 真实 LLM 仍可能连续输出无效计划，API 会返回 400 和 `planner_attempts`。
- 当前重试只覆盖 planner dry-run 阶段，尚未接入 LangGraph 工作流节点。
- 回滚可移除 `llm_max_attempts`、`feedback_messages` 和 dry-run 的 LLM 重试分支，旧规则 planner 不受影响。

文档同步：

- README 已补充 LLM planner 失败反馈重试能力和测试基线。
- `文档/项目结构.md` 已同步 LLM planner 当前边界。

后续动作：

- 将失败反馈重试接入 LangGraph 的自然语言补丁规划节点。
- 增加高风险确认和审计记录，后续再迁移到 LangGraph interrupt。
- 扩充真实 LLM planner 评测集。

DoD：

- schema 失败可重试。
- dry-run 执行失败可反馈重试。
- dry-run 校验失败可反馈重试。
- API 返回规划尝试摘要。
- 真实 DeepSeek planner dry-run 结构测试通过。
- 全量测试通过。

### ER-20260428-018 LLM planner 失败反馈重试接入 LangGraph

状态：已完成

修改人：Codex

关联文件：

- `app/services/planner_execution.py`
- `app/graph/state.py`
- `app/graph/nodes.py`
- `app/main.py`
- `tests/test_llm_gateway.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：ER-20260428-017 已在 `/api/planner/dry-run` 中实现 LLM planner 失败反馈重试，但 LangGraph 自然语言补丁规划节点仍只使用规则 planner，工作流路径没有复用同一套 dry-run、校验和错误反馈机制。

目标：把 LLM planner 反馈重试接入 LangGraph 的自然语言补丁规划节点，同时保持手动 `pending_patch` 的确定性执行路径不被自动重试覆盖。

代码/契约变化：

- 新增 `app/services/planner_execution.py`：
  - `plan_patch_with_llm_dry_run_feedback()` 统一封装 LLM 补丁规划、dry-run、失败反馈重试和 `planner_attempts` 摘要。
  - `PlannerDryRunFeedbackError` 用于携带多轮失败后的结构化失败信息。
- `/api/planner/dry-run` 改为复用共享服务入口，避免 API 和工作流维护两份重试逻辑。
- `AgentState` 新增：
  - `planner_dry_run`
  - `planner_attempts`
  - `use_llm_planner`
  - `llm_provider`
  - `llm_max_attempts`
- `plan_patch_node()` 在 `use_llm_planner=true` 时调用 LLM planner 共享执行入口；未开启时仍使用规则 planner。
- 工作流只自动应用 `risk_level=low` 的 LLM 计划；中高风险计划保留 `planner_dry_run` 并进入 `awaiting_patch_confirmation`。
- `pending_patch` 已存在时，路由仍优先进入 `apply_pending_patch_node()`，不会被 LLM 自动重试逻辑覆盖。
- 会话创建和消息接口新增显式 LLM planner 开关：
  - `use_llm_planner`
  - `llm_provider`
  - `llm_max_attempts`

影响：

- 工作流自然语言补丁规划可使用 LLM 生成多步结构化补丁，并在真正应用前完成 dry-run 校验。
- 中高风险 LLM 计划不会自动落盘，后续需要确认闭环再应用。
- 选择器不唯一、patch engine 执行失败或 dry-run 校验失败可反馈给 LLM 重试；多轮仍失败时转为 `awaiting_patch_clarification`。
- 手动补丁、规则 planner 和现有版本创建/回滚/导出流程不受影响。

验证：

- 定向测试：
  - `pytest tests\test_llm_gateway.py tests\test_workflow_api.py -q`
  - 结果：`24 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`54 passed`

风险/回滚：

- 风险中等；工作流启用 LLM planner 后会增加真实模型调用次数，仍由 `llm_max_attempts` 限制。
- 默认不开启 `use_llm_planner`，因此普通会话继续走规则 planner；开启后也只有 low 风险计划会自动应用。
- 回滚可移除 `planner_execution.py` 并让 API/工作流分别回到原实现；确定性补丁链路不受影响。

文档同步：

- README 已补充工作流启用 LLM planner 的调用方式。
- `文档/项目结构.md` 已补充 `planner_execution.py` 职责和当前边界。

后续动作：

- 继续扩充真实 LLM planner 评测样例。
- 优先实现高风险确认状态闭环，再迁移到 LangGraph interrupt。
- 阶段 5 可从 `replace_constant` 开始推进。

DoD：

- 工作流可显式启用 LLM planner。
- 工作流复用 dry-run 反馈重试机制。
- 中高风险 LLM 计划不会自动应用。
- schema 失败重试有工作流测试覆盖。
- 选择器不唯一反馈后可转为澄清状态。
- dry-run 校验失败反馈有 API 测试覆盖。
- 手动 `pending_patch` 路径不被覆盖。

### ER-20260428-019 中高风险补丁确认闭环

状态：已完成

修改人：Codex

关联文件：

- `app/graph/state.py`
- `app/graph/nodes.py`
- `app/main.py`
- `app/static/index.html`
- `app/static/app.js`
- `app/static/styles.css`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：ER-20260428-018 已让 LLM 中高风险计划停在 `awaiting_patch_confirmation`，但状态中没有可确认后应用的补丁入口，用户只能看到 dry-run 摘要，无法完成确认、取消、确认后版本创建和校验闭环。

目标：在暂不引入 LangGraph interrupt 的前提下，用现有状态和 API 层完成可验收的高风险确认闭环。

代码/契约变化：

- `AgentState` 新增：
  - `pending_confirmation_patch`
  - `risk_assessment`
  - `patch_confirmation`
- LLM planner 返回 `risk_level=medium/high` 时，工作流将补丁放入 `pending_confirmation_patch`，不放入 `pending_patch`，避免路由自动落盘。
- 手动 `pending_patch` 中的 `add_node_from_schema`、`connect`、`disconnect`、疑似 IO/通讯字段修改会先 dry-run，并转为 `awaiting_patch_confirmation`。
- 新增 `POST /api/sessions/{thread_id}/patch-confirmation`：
  - `{"action": "approve"}`：把待确认补丁转入已确认执行路径，应用补丁并创建不可变子版本。
  - `{"action": "cancel"}`：清理待确认补丁，不改变当前版本。
- 确认记录绑定确认时的 `current_project_version_id`，避免同一个旧确认被后续版本复用。
- 静态工作台补充待确认补丁面板、确认应用和取消按钮。

影响：

- 中高风险补丁确认前只展示 dry-run、diff 摘要和风险原因，不保存工程文件。
- 确认后复用既有 patch engine、校验和子版本创建路径。
- 取消确认后当前项目版本保持不变，后续可继续输入新需求或重新发起补丁。
- 当前仍不是 LangGraph 原生 interrupt；后续接入 PostgreSQL checkpointer 后再迁移。

验证：

- 定向测试：
  - `pytest tests\test_workflow_api.py::test_workflow_llm_planner_requires_confirmation_for_non_low_risk tests\test_workflow_api.py::test_workflow_requires_confirmation_for_manual_medium_risk_patch tests\test_workflow_api.py::test_api_confirms_or_cancels_medium_risk_patch -q`
  - 结果：`3 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`56 passed`

风险/回滚：

- 风险中等，手动中风险补丁从“直接应用”改为“先确认”，这是生产方案要求的行为收紧。
- 当前风险识别是第一版启发式，主要覆盖结构/连线/IO 通讯字段；保护逻辑影响仍需阶段 6 规则库增强。
- 回滚可移除确认端点和 `pending_confirmation_patch` 分支，恢复中风险手动补丁直接应用，但不建议。

文档同步：

- README 已补充确认接口和当前能力说明。
- `文档/项目结构.md` 已同步当前 LangGraph/确认边界。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 5 优先实现 `replace_constant`，再推进 `enable_dynamic_input`。
- 阶段 6 增加保护逻辑规则库后，把风险识别从启发式升级为规则校验。
- 有 PostgreSQL checkpointer 后，将状态/API 确认迁移到 LangGraph interrupt。

DoD：

- 中高风险 LLM 计划不会自动落盘。
- 手动中风险补丁不会绕过确认直接保存。
- 确认可应用补丁并创建子版本。
- 取消确认不改变当前版本。
- API 和工作台均可触发确认/取消。
- 全量索引构建和 pytest 通过。

### ER-20260428-020 结构化需求澄清和模板候选解释

状态：已完成

修改人：Codex

关联文件：

- `app/services/requirement_analysis.py`
- `app/graph/state.py`
- `app/graph/nodes.py`
- `app/graph/workflow.py`
- `app/main.py`
- `app/static/index.html`
- `app/static/app.js`
- `app/static/styles.css`
- `evals/template_selection_cases.jsonl`
- `tests/test_template_selection_evals.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 2 尚未完整落地。此前工作流只把用户原文保存到 `requirement_summary`，模板候选只展示基础 reasons，缺少结构化需求清单、待澄清问题和模板候选解释。

目标：先完成阶段 2A 的确定性底座，不把真实 LLM 抽取放入默认工作流，保证普通本地验收稳定。

代码/契约变化：

- 新增 `app/services/requirement_analysis.py`：
  - `analyze_requirement()`：确定性合并多轮需求，输出项目类型、设备、控制功能、通讯/IO、保护逻辑、缺失字段、待澄清问题和确认项。
  - `explain_template_candidate()`：为模板候选生成匹配项、缺失项、预计改造成本和风险点。
- `AgentState` 新增：
  - `open_questions`
  - `confirmed_requirements`
- `collect_requirements()` 改为生成结构化摘要；项目类型或基本设备/控制目标不足时进入澄清状态。
- 通讯方式、保护逻辑、设备数量缺失当前作为待确认问题展示，不默认阻断模板选择，避免原型过早卡死。
- 模板候选返回：
  - `matched_items`
  - `missing_items`
  - `estimated_modification_cost`
  - `risk_points`
- 静态工作台新增项目类型快捷按钮、结构化需求清单、待澄清问题和模板候选解释展示。
- 新增 `evals/template_selection_cases.jsonl` 和 `tests/test_template_selection_evals.py`。

影响：

- 模板选择前已有可读的结构化需求状态。
- 对“我要做一个控制程序”或“我要做机房群控程序”这类过于简略的输入，会先提示补充项目类型或设备/控制目标。
- 对已包含基本设备和控制目标的需求，继续推荐模板并展示匹配/缺失/成本/风险。
- 默认工作流仍不调用真实 LLM，真实 LLM 需求抽取仍保留在显式 API。

验证：

- 定向测试：
  - `pytest tests\test_template_selection_evals.py tests\test_workflow_api.py::test_workflow_waits_for_project_type tests\test_workflow_api.py::test_workflow_can_create_project_version tests\test_workflow_api.py::test_api_end_to_end -q`
  - 结果：`5 passed`
- 工作流回归：
  - `pytest tests\test_workflow_api.py -q`
  - 结果：`16 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`58 passed`

风险/回滚：

- 风险中等，模板选择前的状态机行为变严格；已保持阻塞条件保守，只阻断项目类型或基本设备/控制目标不足的输入。
- 需求抽取是确定性关键词版本，不代表最终生产效果；后续可接入 LLM 抽取和更完整字段 schema。
- 回滚可移除 `requirement_analysis.py` 的工作流接入，恢复原始原文保存和模板 reasons 展示。

文档同步：

- README 已补充结构化需求、模板候选解释和评测基线。
- `文档/项目结构.md` 已补充 `requirement_analysis.py`、模板选择评测和当前边界。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 2B：扩充 AHU/机房群控模板选择样例到各 10 个，统计 Top-1/Top-3。
- 将 LLM 需求抽取作为显式可选路径接入工作流，并保留确定性 fallback。
- 增强模板混合评分中的“预计修改成本”权重。
- 前端后续可继续强化模板确认体验，但不必在阶段 2A 迁移 React。

DoD：

- 工作流有结构化需求摘要。
- 缺少项目类型或基本设备/控制目标时会先澄清。
- 模板候选包含匹配项、缺失项、预计改造成本和风险点。
- 有模板选择评测样例和测试。
- 静态工作台可展示需求清单和候选解释。
- 全量索引构建和 pytest 通过。

### ER-20260428-021 模板选择评测集扩充和 Top-1/Top-3 统计

状态：已完成

修改人：Codex

关联文件：

- `evals/template_selection_cases.jsonl`
- `tests/test_template_selection_evals.py`
- `README.md`
- `文档/演进记录.md`

背景：ER-20260428-020 只建立了第一版小样例质量门，阶段 2B 明确要求把 AHU 和机房群控模板选择样例扩充到各 10 个，并统计 Top-1/Top-3。

目标：补齐模板选择评测规模和命中率断言，让后续模板评分、需求抽取或检索逻辑调整有明确回归约束。

代码/契约变化：

- `evals/template_selection_cases.jsonl` 扩充为 22 条：
  - 10 条机房群控可推荐样例。
  - 10 条 AHU 可推荐样例。
  - 2 条应进入澄清状态的样例。
- 可推荐样例新增 `expected_top1` 字段。
- `tests/test_template_selection_evals.py` 新增评测统计：
  - 断言 AHU 和机房群控可推荐样例各不少于 10 条。
  - 断言当前样例 Top-1 命中率为 100%。
  - 断言当前样例 Top-3 命中率为 100%。

影响：

- 阶段 2B 具备可重复运行的离线质量门槛。
- 当前模板库只有 5 个模板，评测重点是防止既有模板排序、项目类型过滤和澄清边界回归。
- 未改变 API、工作流和前端运行时行为。

验证：

- 定向测试：
  - `pytest tests\test_template_selection_evals.py -q`
  - 结果：`2 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`58 passed`

风险/回滚：

- 风险低，改动集中在评测样例和测试断言。
- 当前 Top-1 断言基于现有模板库和评分逻辑；后续引入更多模板时，可能需要按新的模板标注更新 golden 期望。
- 回滚可恢复旧评测文件和测试断言，不影响运行时代码。

文档同步：

- README 已补充模板选择评测规模和 Top-1/Top-3 统计说明。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 可将 LLM 需求抽取作为显式可选路径接入工作流，并保留确定性 fallback。
- 增强模板混合评分中的“预计修改成本”权重。
- 若转入阶段 5，优先实现 `replace_constant`，再做 `enable_dynamic_input`。

DoD：

- AHU 可推荐评测样例不少于 10 条。
- 机房群控可推荐评测样例不少于 10 条。
- 澄清样例继续覆盖项目类型缺失和设备/控制目标缺失。
- 测试统计 Top-1/Top-3 并作为质量门槛。
- 全量索引构建和 pytest 通过。

### ER-20260428-022 `replace_constant` 低风险补丁操作

状态：已完成

修改人：Codex

关联文件：

- `app/services/patch_engine.py`
- `app/services/planner.py`
- `app/services/llm_planner.py`
- `evals/patch_planning_cases.jsonl`
- `tests/test_services.py`
- `tests/test_llm_gateway.py`
- `tests/test_planner_evals.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 5 要求从 `replace_constant` 开始扩展补丁引擎。此前只能用通用 `update_param` 修改常量或设定值，缺少受限语义 op，不利于 LLM planner 明确表达低风险“只替换值”的意图。

目标：新增独立 `replace_constant` op，并确保它具备 schema、执行器、dry-run、diff、风险等级、文档示例和测试。

代码/契约变化：

- `patch_engine` 新增 `replace_constant`：
  - 需要 `node_selector` 和 `value`。
  - `field` 可选；未指定时按节点类型选择默认值字段。
  - 支持 `constInput.fixedValue/outOfServiceValue`、`swInput.outOfServiceValue/swInputDefault`、`compare.tripPoint`、数学模块 `fixedValue`。
  - 拒绝结构字段、未知字段、非白名单节点类型和非白名单字段。
- 规则 planner 新增常量/固定值/设定值替换识别，生成 `replace_constant`。
- LLM planner schema 和 prompt 允许 `replace_constant`，并将其作为 low 风险操作。
- patch planning 评测集新增 `plant_replace_constant_value` 样例。
- 工作流新增自然语言 `replace_constant` 落版本回归测试。

影响：

- 常量、软件输入设定值和静态阈值替换具备更明确的低风险结构化表达。
- 现有 `update_param` 仍保留，用于一般参数修改。
- 中高风险确认逻辑不受影响，`replace_constant` 默认低风险，不需要确认。

验证：

- 定向测试：
  - `pytest tests\test_services.py tests\test_llm_gateway.py tests\test_planner_evals.py -q`
  - 结果：`38 passed`
- 工作流定向测试：
  - `pytest tests\test_workflow_api.py::test_workflow_plans_and_applies_replace_constant_patch -q`
  - 结果：`1 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`62 passed`

风险/回滚：

- 风险中低，新增 op 与原 `update_param` 并存，不改变已有补丁契约。
- 当前白名单只覆盖已确认的值字段；后续如新增模块类型，需要扩展字段矩阵并补测试。
- 回滚可移除 `replace_constant` 分支、planner/schema 接入、评测样例和测试，恢复仅用 `update_param`。

文档同步：

- README 已新增 `replace_constant` 能力和示例。
- `文档/项目结构.md` 已同步补丁引擎和 LLM planner 当前边界。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 5B：实现 `enable_dynamic_input`，同步维护 `inputs`、`inputsCount`、`inputsOption` 和 `wires`。
- 继续扩充 `evals/patch_planning_cases.jsonl`，优先覆盖 AHU CO2 控制、新风阀联动、送风温度 PID 参数、过滤网报警、机房群控水泵阈值和旁通阀压差设定。

DoD：

- `replace_constant` 有执行器和白名单约束。
- dry-run 返回 diff 且不修改源节点。
- 规则 planner 可生成 `replace_constant`。
- LLM planner schema 接受 `replace_constant` 并拒绝未知 op。
- 工作流可应用低风险 `replace_constant` 并创建子版本。
- 全量索引构建和 pytest 通过。

### ER-20260428-023 `enable_dynamic_input` 中风险补丁操作

状态：已完成

修改人：Codex

关联文件：

- `app/services/patch_engine.py`
- `app/services/planner.py`
- `app/services/llm_planner.py`
- `app/graph/nodes.py`
- `evals/patch_planning_cases.jsonl`
- `tests/test_services.py`
- `tests/test_llm_gateway.py`
- `tests/test_planner_evals.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 5 要求实现 `enable_dynamic_input`，用于开启动态输入端口，并同步维护端口数量、动态选项和 `wires` 形态。该操作会改变节点拓扑，必须按中风险补丁确认。

目标：新增第一版受控 `enable_dynamic_input`，只负责启用动态端口，不自动连线；连线继续由 `connect` 单独表达。

代码/契约变化：

- `patch_engine` 新增 `enable_dynamic_input`：
  - `compare`、`limit`：启用 `inputAuxEnable`，将 `inputs` 扩到 2，并补齐 `wires`。
  - `pid`、`fuzzypid`：按 `input_option`/`input_options` 更新 `inputsOption`，按基础输入数加选项数同步 `inputs` 和 `wires`。
  - `linear`：支持 `inputD`、`outputD`，同步 `inputs` 和 `wires`。
  - 拒绝减少端口、未知节点类型、非法动态输入选项和无效 `inputsOption`。
- 规则 planner 可识别“启用节点 xxx 的动态输入”类请求。
- LLM planner schema 和 prompt 允许 `enable_dynamic_input`，并标记为中风险。
- 工作流风险评估将 `enable_dynamic_input` 纳入中风险确认门。
- patch planning 评测集新增动态阈值输入样例。

影响：

- 动态输入端口开启具备结构化补丁表达和 dry-run diff。
- 确认前不创建新版本，确认后沿用既有补丁确认闭环。
- 当前版本只启用端口，不自动连接上游源；后续复杂链路仍需组合 `add_node_from_schema` 和 `connect`。

验证：

- 定向测试：
  - `pytest tests\test_services.py tests\test_llm_gateway.py tests\test_planner_evals.py tests\test_workflow_api.py::test_workflow_requires_confirmation_for_dynamic_input_patch -q`
  - 结果：`43 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`67 passed`

风险/回滚：

- 风险中等，动态输入会改变节点端口和 `wires` 结构；已强制进入中风险确认。
- 当前对 PID 类模块按现有工程 JSON 的紧凑端口顺序处理，后续如果前端局部图需要固定端口编号展示，应再补端口语义映射。
- 回滚可移除 `enable_dynamic_input` 分支、planner/schema 接入、风险评估、评测样例和测试。

文档同步：

- README 已新增 `enable_dynamic_input` 能力和示例。
- `文档/项目结构.md` 已同步补丁引擎和 LLM planner 当前边界。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 5C：推进 `copy_block` 前，建议先补更多 patch planning 样例，覆盖“新增 CO2 控制链路”这类需要 `add_node_from_schema + enable_dynamic_input + connect` 的组合计划。
- 阶段 6 前置：补动态端口校验，明确 `inputsOption`、`inputAuxEnable`、`inputs` 和 `wires` 的一致性规则。

DoD：

- `enable_dynamic_input` 有执行器和选项白名单约束。
- dry-run 返回 diff 且不修改源节点。
- 规则 planner 可生成 `enable_dynamic_input`。
- LLM planner schema 接受 `enable_dynamic_input`。
- 工作流把 `enable_dynamic_input` 识别为中风险并等待确认。
- 全量索引构建和 pytest 通过。

### ER-20260428-024 动态端口一致性校验

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `tests/test_services.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：ER-20260428-023 已实现 `enable_dynamic_input`，但校验器只检查 `wires` 不大于 `inputs`，还不能确定动态端口字段之间是否一致。阶段 6 前置任务要求明确 `inputsOption`、`inputAuxEnable`、`inputs`、`inputsCount` 和 `wires` 的一致性规则。

目标：在不提前引入完整 schema 参数校验的前提下，先把动态输入端口这一类结构风险纳入导出阻塞校验。

代码/契约变化：

- `validator` 新增动态端口一致性校验：
  - `compare`、`limit`：按 `inputAuxEnable` 推导 `inputs` 应为 1 或 2。
  - `pid`、`fuzzypid`：按基础输入数 3 加 `inputsOption` 数量推导 `inputs`，并校验选项白名单和重复选项。
  - `linear`：按 `inputD`、`outputD` 推导 `inputs` 应为 1、3 或 5。
  - 若存在 `inputsCount`，必须与推导出的端口数量一致。
  - 动态端口节点的 `wires` 长度必须与推导出的端口数量一致。
- 校验问题新增 `dynamic_inputs_mismatch`、`dynamic_inputs_count_mismatch`、`dynamic_wires_inputs_mismatch`、`invalid_inputs_option` 等错误码。
- 新增服务层测试覆盖动态端口字段不一致、非法选项、`inputsCount` 不一致和 `wires` 长度不一致。

影响：

- `enable_dynamic_input` dry-run 后不只依赖执行器自身维护端口，也会被导出前校验再次检查。
- 现有 5 个模板已通过更严格的动态端口校验。
- 当前仍不是完整 L3 schema 参数校验；字段类型、枚举、范围、IO/通讯冲突和保护逻辑规则仍留在阶段 6 后续任务。

验证：

- 定向测试：
  - `pytest tests\test_services.py -q`
  - 结果：`29 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`68 passed`

风险/回滚：

- 风险较低，规则只作用于已有动态端口类型，不改变补丁执行逻辑。
- 如果后续发现真实工程存在旧格式动态节点，可将对应错误降级为 warning 或补兼容迁移。
- 回滚可移除 `validator` 中动态端口校验分支和对应测试。

文档同步：

- README 已补充动态端口一致性校验能力和验收数量。
- `文档/项目结构.md` 已同步校验器职责。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 5C：优先扩充组合补丁评测样例，再推进 `copy_block`。
- 阶段 6：继续补完整 schema 参数校验、IO/通讯冲突和保护逻辑规则库。

DoD：

- 动态端口字段不一致会产生 error 级校验问题。
- 现有模板全部通过动态端口校验。
- `enable_dynamic_input` dry-run 结果继续校验通过。
- 全量索引构建和 pytest 通过。

### ER-20260428-025 组合补丁评测样例

状态：已完成

修改人：Codex

关联文件：

- `evals/patch_planning_cases.jsonl`
- `tests/test_planner_evals.py`
- `app/services/llm_planner.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 5C 建议在推进 `copy_block` 前，先补齐已有 op 的组合补丁评测，尤其是 `add_node_from_schema + enable_dynamic_input + connect` 这类会同时新增节点、改变端口和修改连线的链路。

目标：用 golden plan 和 dry-run 验证复杂局部改造能由现有结构化补丁表达，避免在 `copy_block` 前留下组合链路缺口。

代码/契约变化：

- `patch_planning_cases.jsonl` 新增 3 个三步组合样例：
  - AHU：新增 CO2 浓度设定常量，启用比较判断动态阈值输入并接入。
  - AHU：新增送风 PID 输出上限设定常量，启用 `highPidOutLimit` 动态输入并接入。
  - 机房群控：新增旁通阀压差设定常量，启用旁通阀比较判断动态阈值输入并接入。
- `tests/test_planner_evals.py` 要求评测集中至少包含 3 个 `add_node_from_schema + enable_dynamic_input + connect` 组合样例，并逐一执行 dry-run。
- `llm_planner` prompt 新增组合计划规则：
  - 新增信号接入 compare/limit 动态阈值时必须显式先启用动态端口。
  - 新增设定值接入 PID 动态参数时必须指定 `input_option`。
  - 禁止用 `connect` 隐式创建端口。

影响：

- 现有 patch engine 不新增 op，但复杂补丁组合已有可回归评测。
- LLM planner 后续 prompt 或 schema 调整时，必须保持这些组合计划 dry-run 通过。
- 中风险确认门仍由既有风险识别覆盖，因为组合中包含 `add_node_from_schema`、`enable_dynamic_input` 和 `connect`。

验证：

- 定向测试：
  - `pytest tests\test_planner_evals.py -q`
  - 结果：`2 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`68 passed`

风险/回滚：

- 风险低，主要改动为评测样例和 prompt 约束，不改变补丁执行器。
- 组合样例使用明确节点 id 和唯一新增节点名称，避免选择器歧义。
- 回滚可移除新增 JSONL 样例、测试断言和 prompt 组合规则。

文档同步：

- README 已补充 LLM planner 评测覆盖组合补丁。
- `文档/项目结构.md` 已同步 LLM planner 评测边界。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 5D：开始设计并实现 `copy_block` 前，先明确 block 入口/出口语义、内部 id 重写、坐标偏移、目标 tab 选择、内部连线保持、外部入口出口连接策略和跨模板兼容策略。
- 阶段 6：继续补完整 schema 参数校验、IO/通讯冲突和保护逻辑规则库。

DoD：

- 至少 3 个三步组合补丁 golden plan 可 dry-run。
- 组合补丁 diff 摘要和校验结果符合预期。
- LLM planner prompt 明确组合补丁的端口启用顺序。
- 全量索引构建和 pytest 通过。

### ER-20260428-026 `copy_block` 高风险功能块复制

状态：已完成

修改人：Codex

关联文件：

- `app/services/patch_engine.py`
- `app/services/llm_planner.py`
- `evals/patch_planning_cases.jsonl`
- `tests/test_services.py`
- `tests/test_llm_gateway.py`
- `tests/test_planner_evals.py`
- `tests/test_workflow_api.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 5 要求支持 `copy_block`，但该操作涉及入口/出口语义、内部 id 重写、坐标偏移、目标 tab、内部连线保持、外部连接和跨模板兼容策略。ER-20260428-025 已先补组合补丁评测，本轮实现第一版受控复制。

目标：实现可 dry-run、可 diff、必须确认的最小 `copy_block`：只复制功能块内部节点和内部连线，不隐式接入任何外部入口/出口。

代码/契约变化：

- `patch_engine` 新增 `copy_block`：
  - 输入：`block_id` 或 `source_block_id`、`target_tab_selector`。
  - 可选：`x_offset`、`y_offset`、`offset`、`name_prefix`。
  - 从 `block_index.jsonl` 定位源功能块和源工程 JSON。
  - 复制 `node_ids` 对应节点，生成全新节点 id。
  - 所有复制节点 `z` 改为目标 tab。
  - 坐标按偏移量移动，节点名可加前缀。
  - 只保留源功能块内部连线并重写引用 id。
  - 丢弃所有指向功能块外部的输入引用，不自动创建外部连接。
  - 变更报告返回 `id_mapping`、复制节点数、丢弃外部输入引用数量和目标 tab。
- `llm_planner` schema 和 prompt 允许 `copy_block`，并要求 `block_id` 和 `target_tab_selector`。
- `copy_block` 风险等级沿用工作流已有规则：高风险，必须人工确认后应用。
- patch planning 评测集新增 `plant_copy_bypass_block_without_external_connections`。

影响：

- 可以把已索引功能块作为局部子图复制到当前工程版本中。
- 复制结果不会隐式接错 IO、通讯、保护或设备联锁；后续接线必须由显式 `connect` 表达。
- 当前第一版不做跨模板 schema 兼容性深校验，不自动布局避让，也不自动连接入口/出口。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_patch_engine_copies_block_with_new_ids_and_internal_wires_only tests\test_services.py::test_patch_engine_rejects_unsafe_or_ambiguous_changes tests\test_llm_gateway.py::test_llm_planner_accepts_copy_block_operation tests\test_planner_evals.py::test_patch_planning_eval_cases_are_valid_and_dry_runnable -q`
  - 结果：`4 passed`
  - `pytest tests\test_workflow_api.py::test_workflow_requires_high_risk_confirmation_for_copy_block -q`
  - 结果：`1 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`71 passed`

风险/回滚：

- 风险高，复制局部子图可能带来重复逻辑和未接入口/出口；已强制进入高风险确认。
- 当前丢弃外部入口/出口连线是保守策略，避免隐式接错保护、IO 或通讯链路。
- 如果后续需要自动外部接线，必须先增加入口/出口角色标注、影响范围预览和二次确认。
- 回滚可移除 `copy_block` 执行分支、LLM schema/prompt 接入、评测样例和测试。

文档同步：

- README 已补充 `copy_block` 能力、示例和当前限制。
- `文档/项目结构.md` 已同步补丁引擎和功能块索引当前能力。
- `文档/演进记录.md` 已新增本记录。

后续动作：

- 阶段 5E：继续推进 `set_io_point` 前，应先建立 IO/通讯字段识别和冲突校验。
- 阶段 6：继续补完整 schema 参数校验、IO/通讯冲突和保护逻辑规则库。
- 后续增强 `copy_block` 时，可增加入口/出口角色标注、复制后布局避让和显式外部连接辅助，但仍不能隐式接线。

DoD：

- `copy_block` 可按 `block_id` 复制节点并重写 id。
- 内部连线被保留并指向新 id。
- 外部入口/出口连线被丢弃且报告计数。
- dry-run 返回 diff 且源节点不被修改。
- 工作流识别为高风险并等待确认。
- 全量索引构建和 pytest 通过。

### ER-20260428-027 schema 参数合法性校验

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `tests/test_services.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 6 要求导出前能发现 schema 字段类型、枚举、范围、必填参数和未知字段问题。ER-20260428-024 已补动态端口一致性，但还没有把 `schemas/` 中的参数定义纳入全工程校验。

目标：在不破坏现有历史模板兼容性的前提下，为 schema 已知节点增加 L3 参数合法性校验，并继续作为 error 级导出阻塞问题。

代码/契约变化：

- `validator` 新增 schema 索引缓存，按节点 `type` 对应 `schemas/*/*.json` 的 `module_type`。
- 对 schema 已知节点新增校验：
  - 字段类型：字符串、布尔、整数、数字和字符串数组。
  - 枚举：普通枚举值和数组枚举项。
  - 范围：`minimum`、`maximum`。
  - 必填参数：schema 中没有默认值的参数缺失时报错。
  - 未知字段：节点字段不在 schema `template_json` 字段集合中时报错。
- 兼容当前模板中已有的数字字符串，例如 `"30"`、`"4"`，按 schema 数字/整数规则解析后校验范围，避免把历史模板误判为错误。
- 新增错误码：`schema_invalid_type`、`schema_invalid_enum`、`schema_below_minimum`、`schema_above_maximum`、`schema_missing_required_param`、`schema_unknown_field`。

影响：

- 补丁 dry-run、项目校验和导出前校验都会捕获 schema 参数违规。
- `update_param`、`replace_constant` 等修改已有字段的能力多了一层导出前兜底。
- 当前仍未覆盖 IO/通讯地址冲突和保护逻辑规则库；这些属于阶段 6 后续任务。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_validator_rejects_schema_parameter_violations tests\test_services.py::test_existing_templates_validate_cleanly -q`
  - 结果：`2 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`72 passed`

风险/回滚：

- 风险中等，校验规则变严格后可能暴露真实项目中的历史脏数据；已对数字字符串做兼容，现有 5 个模板保持零错误零告警。
- 如果后续发现旧工程存在可兼容字段，可优先修正 schema `template_json` 字段集合或增加明确兼容字段，不建议直接关闭 schema 校验。
- 回滚可移除 `validator` 中 schema 参数校验分支和对应测试。

文档同步：

- README 已补充 schema 参数合法性校验能力，并从未完成列表移除该项。
- `文档/项目结构.md` 已同步校验器职责。
- `文档/演进记录.md` 已新增本记录并更新当前基线。

后续动作：

- 阶段 6：继续补 IO/通讯冲突校验和保护逻辑规则库。
- 阶段 5E：若推进 `set_io_point`，应基于 IO/通讯字段识别和冲突校验设计高风险确认规则。

DoD：

- schema 字段类型、枚举、范围、必填参数和未知字段违规会产生 error 级校验问题。
- 现有模板全部通过 schema 参数校验。
- dry-run 和导出前校验继续使用同一份校验报告。
- 全量索引构建和 pytest 通过。

### ER-20260428-028 IO/通讯冲突校验

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `app/services/patch_engine.py`
- `tests/test_services.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 6 要求导出前发现 IO 地址冲突、BACnet/Modbus/MQTT 对象名冲突和通讯点表重复。ER-20260428-027 已完成 schema 参数合法性校验，本轮继续补 L4 IO/通讯冲突校验，为后续 `set_io_point` 提供确定性安全网。

目标：在不修改工程 JSON 的前提下，增加跨节点冲突扫描，并保证现有 5 个模板保持零错误零告警。

代码/契约变化：

- `validator` 新增跨节点 IO/通讯冲突校验：
  - `hardware_io_channel_conflict`：`hwInput`/`hwOutput` 按 `hwExpander + hwChannelIndex` 去重。
  - `modbus_point_conflict`：`modbusOutput` 按 `modbusPort + modbusTcpIPaddr/rtu + modbusTcpPort + modbusAddress + functionCode + modbusRegAddr` 去重。
  - `bacnet_object_conflict`：本地 BACnet 暴露对象按 `bacnetObjectType + bacnetObjectInstance` 去重，仅 `bacnetVisible` 启用且对象类型非 `OBJECT_DISABLED` 时参与。
  - `bacip_point_conflict`：`bacipOutput` 按 `bacnetIpDeviceInstance + bacnetIpObjectType + bacnetIpObjectInstance` 去重。
  - `mqtt_object_name_conflict`：`mqttin`/`mqttout` 按 `objectName` 去重。
- 冲突键对数字字符串做归一化，例如 `"7"` 和 `7` 视为同一个地址。
- 空字段不参与冲突组；Modbus RTU 的空 `modbusTcpIPaddr` 归一为 `rtu`。
- `copy_block` 联动调整：复制节点时禁用本地 BACnet 暴露对象，重置 `bacnetVisible`、`bacnetObjectType`、`bacnetObjectInstance`、`bacnetObjectPrefix` 和描述字段，避免复制功能块后复用原对象号。

影响：

- 导出前校验可以阻止重复硬件 IO 通道和重复通讯点位。
- 后续实现 `set_io_point` 时，可以复用同一套校验作为 dry-run 后的安全检查。
- `copy_block` 继续只复制内部逻辑，不隐式复用本地 BACnet 对象；复制后如需重新暴露点位，应通过显式高风险补丁完成。
- 当前只做地址/对象唯一性，不做通讯协议语义校验、读写方向兼容性或保护逻辑影响分析。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_validator_rejects_io_and_communication_conflicts tests\test_services.py::test_existing_templates_validate_cleanly -q`
  - 结果：`2 passed`
  - `pytest tests\test_planner_evals.py::test_patch_planning_eval_cases_are_valid_and_dry_runnable tests\test_workflow_api.py::test_workflow_requires_high_risk_confirmation_for_copy_block -q`
  - 结果：`2 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`74 passed`

风险/回滚：

- 风险中等。某些真实项目可能允许多个逻辑节点读取同一远端点位；当前按生产安全优先处理为导出阻塞，后续如有业务例外，应显式建白名单或按读写方向细分规则。
- 现有模板已验证无冲突。
- 回滚可移除 `validator` 中 IO/通讯冲突扫描和对应测试。

文档同步：

- README 已补充 IO/通讯冲突校验能力和 `copy_block` 本地 BACnet 去绑定行为，并从未完成列表移除该项。
- `文档/项目结构.md` 已同步校验器职责。
- `文档/演进记录.md` 已新增本记录并更新当前基线。

后续动作：

- 阶段 6：继续补保护逻辑规则库。
- 阶段 5E：推进 `set_io_point` 时，必须补字段白名单、风险分级、dry-run、确认门、文档示例、评测样例和测试。

DoD：

- 重复硬件 IO、Modbus、BACnet、本地/远端对象和 MQTT `objectName` 会产生 error 级校验问题。
- `copy_block` 复制含本地 BACnet 暴露对象的功能块时，不产生重复对象号。
- 现有模板全部通过 IO/通讯冲突校验。
- dry-run 和导出前校验继续使用同一份校验报告。
- 全量索引构建和 pytest 通过。

### ER-20260428-029 set_io_point 高风险点位修改

状态：已完成

修改人：Codex

关联文件：

- `app/services/patch_engine.py`
- `app/services/llm_planner.py`
- `evals/patch_planning_cases.jsonl`
- `tests/test_services.py`
- `tests/test_workflow_api.py`
- `tests/test_planner_evals.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 5 要求补丁引擎支持 `set_io_point`，用于修改 IO 通道、Modbus 地址、BACnet/BACnet IP 点位和 MQTT 对象名。ER-20260428-028 已完成 IO/通讯冲突校验，可作为该高风险操作的 dry-run 安全网。

目标：新增一个只能修改点位白名单字段的结构化补丁操作，禁止绕过 patch engine 直接改 JSON，并在工作流层强制人工确认。

代码/契约变化：

- `patch_engine` 新增 `set_io_point`：
  - 需要 `node_selector` 和非空 `params`。
  - 只支持 `hwInput`、`hwOutput`、`modbusOutput`、`bacipOutput`、`mqttin`、`mqttout`。
  - 只允许修改已有字段，不允许新增未知字段或修改 `id/type/z/wires` 等结构字段。
  - 硬件 IO 支持 `hwExpander`、`hwChannelIndex`。
  - Modbus 支持 `modbusPort`、`modbusTcpIPaddr`、`modbusTcpPort`、`modbusAddress`、`functionCode`、`modbusRegAddr`。
  - 本地 BACnet 支持暴露开关、对象类型、对象实例、对象前缀、描述和单位字段。
  - BACnet/IP 支持设备号、对象类型、对象实例和优先级。
  - MQTT 支持 `topic`、`objectName`。
- 点位字段新增基础类型和范围校验，例如 IO 通道、Modbus 地址、BACnet 实例号和 BACnet/IP 优先级。
- `llm_planner` schema 和 prompt 新增 `set_io_point`，要求节点、字段和值明确；风险规则标记为 high。
- 工作流已有风险识别覆盖 `set_io_point`，本轮补测试确认该操作 dry-run 通过后仍停在 `confirm_patch`。
- 评测集新增 `plant_set_hw_input_channel`，覆盖高风险 `set_io_point` 计划、schema 校验和 golden dry-run。

影响：

- 工程师可以用结构化补丁修改明确点位，并在保存前看到 diff 和校验报告。
- 若点位改到已占用通道或重复通讯对象，dry-run 会返回无效校验报告，不会创建新版本。
- 当前不做协议读写方向兼容、设备点表语义或保护逻辑影响分析，这些仍属于后续规则库范围。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_patch_engine_sets_io_point_with_conflict_validation tests\test_services.py::test_patch_engine_sets_communication_point_fields tests\test_services.py::test_patch_engine_rejects_unsafe_or_ambiguous_changes tests\test_workflow_api.py::test_workflow_requires_high_risk_confirmation_for_set_io_point tests\test_planner_evals.py::test_patch_planning_eval_cases_are_valid_and_dry_runnable -q`
  - 结果：`5 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`77 passed`

风险/回滚：

- 风险中等。点位修改会影响真实现场 IO/通讯绑定，已强制高风险确认，并复用冲突校验阻断重复地址。
- 当前字段白名单偏保守；如后续需要支持更多点位字段，应先补 schema/测试/评测，再扩展白名单。
- 回滚可移除 `set_io_point` 执行分支、LLM schema/prompt 接入、评测样例和对应测试。

文档同步：

- README 已补充 `set_io_point` 能力、示例、字段范围和高风险确认说明。
- `文档/项目结构.md` 已同步补丁引擎和 LLM planner schema 当前能力。
- `文档/演进记录.md` 已新增本记录并更新当前基线。

后续动作：

- 阶段 6：继续补保护逻辑规则库。
- 阶段 5 后续若增强 `copy_block` 外部接线，必须先建立入口/出口角色标注和影响范围预览。

DoD：

- `set_io_point` 只能修改点位白名单字段。
- 点位冲突可在 dry-run 校验中被阻断。
- 工作流识别为高风险并等待确认。
- LLM planner schema 和评测样例覆盖该操作。
- 全量索引构建和 pytest 通过。

### ER-20260428-030 保护逻辑规则库第一版

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `tests/test_services.py`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 6 要求导出前发现保护逻辑问题。此前校验已覆盖结构、schema、动态端口和 IO/通讯冲突，但还不能识别明显的保护逻辑被停用、关键执行器无联锁或设备缺少故障/运行/反馈线索。

目标：先落地保守的 L5 保护逻辑规则库第一版，保证现有模板仍全部通过，同时能拦截人工构造的明显保护缺陷。

代码/契约变化：

- `validator` 新增保护逻辑校验入口 `_validate_protection_logic_rules`。
- 新增 `protection_signal_disabled`：节点文本包含防冻、故障、反馈、过滤、滤网、缺风、报警、到位、保护或联锁等保护关键词时，不允许 `outOfService` 为真。
- 新增 `critical_actuator_without_interlock`：`hwOutput`、`modbusOutput`、`bacipOutput`、`mqttout` 等关键执行器输出，如果名称包含送风机、排风机、新风阀、直膨、水泵、主机、热泵、蝶阀、旁通阀等关键词，且输入端没有任何上游来源，则报错。
- 新增 `missing_protection_logic`：当项目中出现相关设备词时，要求至少存在同设备词关联的故障、运行、反馈、到位或报警线索：
  - AHU 送风相关逻辑需要故障、缺风、报警或运行线索。
  - AHU 直膨机相关逻辑需要故障线索。
  - 水泵相关逻辑需要故障以及运行/反馈线索。
  - 主机或热泵相关逻辑需要故障和运行线索。
  - 蝶阀相关逻辑需要到位或反馈线索。

影响：

- 导出前校验可以阻止明显绕过联锁、停用保护信号或缺失基础保护线索的工程。
- 当前规则只做文本线索和连线存在性判断，不分析完整布尔逻辑、读写方向、保护链路影响范围或业务例外。
- 规则设计为保守第一版，现有 5 个模板保持零错误零告警。

验证：

- 定向测试：
  - `pytest tests\test_services.py::test_validator_rejects_protection_logic_violations tests\test_services.py::test_existing_templates_validate_cleanly -q`
  - 结果：`2 passed`
- 全量验收：
  - `python scripts\build_indexes.py`
  - `pytest -q`
  - 结果：模板 5、页面 31、节点 8413、功能块 129，`78 passed`

风险/回滚：

- 风险中等。文本规则可能无法理解复杂保护链路，也可能对特殊命名工程不够敏感；当前以保守阻断明显缺陷为主。
- 后续若真实项目存在业务例外，应增加明确白名单或按设备类型细化规则，不建议关闭整类保护校验。
- 回滚可移除 `validator` 中保护逻辑校验入口、规则常量和对应测试。

文档同步：

- README 已补充保护逻辑规则库第一版能力，并从未完成列表移除保护逻辑规则库。
- `文档/项目结构.md` 已同步校验器职责和当前规则边界。
- `文档/演进记录.md` 已新增本记录并更新当前基线。

后续动作：

- 继续细化保护逻辑规则库：补读写方向、保护链路影响范围、设备类型规则和业务例外白名单。
- 若开始做前端校验报告 UI，应让保护逻辑问题可定位到节点或规则说明。

DoD：

- 保护信号停用会产生 error 级校验问题。
- 关键执行器输出无上游联锁会产生 error 级校验问题。
- 关键设备缺少故障、运行或反馈线索会产生 error 级校验问题。
- 现有模板全部通过保护逻辑规则库第一版。

### ER-20260428-031 机器可读保护逻辑规则文件

状态：已完成

修改人：Codex

关联文件：

- `rules/protection_logic.json`
- `app/services/validator.py`
- `tests/test_services.py`
- `knowledge/工程规范.md`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：生产级方案要求保护逻辑规则在 `knowledge/工程规范.md` 和机器可读 `rules/*.json` 中双份维护。ER-20260428-030 已完成第一版保护校验，但规则仍硬编码在 `validator.py`，后续扩充读写方向、保护链路影响范围和业务例外白名单时维护成本偏高。

目标：新增机器可读保护逻辑规则文件，并让校验器从规则文件加载保护关键词、关键执行器和设备保护线索规则，同时保持现有模板全部通过。

代码/契约变化：

- 新增 `rules/protection_logic.json`：
  - `text_fields`：用于抽取节点语义文本的字段。
  - `protection_signal_keywords`：保护、故障、反馈、急停等停用检查关键词。
  - `critical_actuator`：关键执行器节点类型和名称关键词。
  - `requirement_rules`：设备词与必需保护线索词的规则列表。
- `validator.py` 新增 `_protection_rules()`，使用 UTF-8 读取规则文件并缓存；文件缺失时使用默认规则兜底。
- 保护信号停用、关键执行器无联锁、设备保护线索缺失均改为使用规则文件加载后的配置。
- `tests/test_services.py` 新增规则文件驱动测试，覆盖 JSON 中新增的“急停”关键词和“电加热”关键执行器关键词。
- `knowledge/工程规范.md` 补充规则文件维护说明。

影响：

- 后续扩充保护规则可优先改 `rules/protection_logic.json`，再补测试验证。
- 当前规则仍是保守文本线索和连线存在性判断，不做完整布尔逻辑证明。
- 现有模板未新增误报。

验证：

```powershell
conda activate midea
pytest tests\test_services.py::test_validator_rejects_protection_logic_violations tests\test_services.py::test_validator_uses_machine_readable_protection_rules tests\test_services.py::test_existing_templates_validate_cleanly -q
```

结果：`3 passed`

```powershell
conda activate midea
python scripts\build_indexes.py
pytest -q
```

结果：模板 5、页面 31、节点 8413、功能块 129，`79 passed`

风险/回滚：

- 风险中等。规则文件可维护性提升，但文本规则仍可能受命名质量影响。
- 回滚可移除 `rules/protection_logic.json`、`validator.py` 中规则加载逻辑和新增测试，恢复 ER-20260428-030 的硬编码规则。

文档同步：

- README 已更新当前能力和测试数量。
- 项目结构已记录 `rules/` 目录。
- 工程规范已说明保护规则文件维护要求。
- 演进记录已更新当前进度、变更索引和下次建议。

后续动作：

- 继续细化保护逻辑规则库，补读写方向、保护链路影响范围和业务例外白名单。
- 若新增业务例外，必须用明确规则表达并补回归样例，避免关闭整类保护校验。

DoD：

- 保护规则文件存在并由校验器读取。
- 新增规则文件驱动行为测试。
- 现有模板全部通过。
- 全量验收通过。

### ER-20260428-032 保护链路影响范围和例外白名单第一版

状态：已完成

修改人：Codex

关联文件：

- `rules/protection_logic.json`
- `app/services/validator.py`
- `tests/test_services.py`
- `knowledge/工程规范.md`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：ER-20260428-031 已将保护规则迁移到机器可读文件，但仍主要做节点文本线索和“是否有输入”的粗粒度判断。生产级方案要求继续补保护链路影响范围和业务例外白名单。

目标：在不误报现有模板的前提下，补充第一版可解释链路规则：关键执行器不能只由常量直连；当项目中存在防冻保护信号时，送风机或新风阀执行器的上游链路必须保留防冻线索；确有例外时支持规则级白名单。

代码/契约变化：

- `rules/protection_logic.json` 新增：
  - `direct_bypass`：定义常量、强制、旁路等直接驱动源。
  - `upstream_chain_rules`：定义保护源关键词、目标执行器关键词、上游必需关键词和最大追踪深度。
  - `exceptions`：定义按规则、节点 id、节点类型或名称关键词匹配的业务例外白名单。
- `validator.py` 新增：
  - `critical_actuator_direct_bypass`：关键执行器直接且仅由常量/强制/旁路源驱动时报错。
  - `missing_protection_chain`：存在保护源信号但目标执行器上游追踪范围内缺少对应保护线索时报错。
  - 基于 `wires` 的 `input_sources` 语义向上游追踪，默认最大深度由规则控制。
  - 规则例外匹配逻辑，避免特殊业务场景只能关闭整类校验。
- `tests/test_services.py` 新增常量直连旁路、防冻链路缺失和规则例外白名单测试。

影响：

- 导出校验能识别一类明显绕过保护的链路问题，而不只看节点本身是否存在。
- 当前链路规则仍是第一版，只追踪上游文本线索，不做完整布尔逻辑证明。
- 现有 5 个模板全部通过，未产生新误报。

验证：

```powershell
conda activate midea
pytest tests\test_services.py::test_validator_rejects_protection_logic_violations tests\test_services.py::test_validator_uses_machine_readable_protection_rules tests\test_services.py::test_validator_rejects_direct_bypass_of_critical_actuator tests\test_services.py::test_validator_rejects_missing_protection_chain_when_signal_exists tests\test_services.py::test_validator_allows_protection_rule_exception tests\test_services.py::test_existing_templates_validate_cleanly -q
```

结果：`6 passed`

```powershell
conda activate midea
python scripts\build_indexes.py
pytest -q
```

结果：模板 5、页面 31、节点 8413、功能块 129，`82 passed`

风险/回滚：

- 风险中等。链路追踪依赖节点命名和 `wires` 上游语义，复杂保护逻辑仍可能需要更精细的设备类型规则。
- 回滚可移除 `direct_bypass`、`upstream_chain_rules`、`exceptions` 规则段和 `validator.py` 对应函数，保留 ER-20260428-031 的机器可读基础规则。

文档同步：

- README 已更新保护规则能力和测试数量。
- 项目结构已更新规则文件职责。
- 工程规范已补充常量直连旁路、保护链路和白名单要求。
- 演进记录已更新当前进度、变更索引和下次建议。

后续动作：

- 继续补更多设备类型的链路规则，例如电加热、直膨机、水泵、主机和蝶阀。
- 继续补读写方向规则，例如输入类保护信号不能被当作执行输出，输出类点位必须经过控制链路。
- 若真实项目需要例外，必须在规则文件中明确表达并补测试。

DoD：

- 常量直连旁路会产生 error 级校验问题。
- 防冻信号存在但未进入送风机/新风阀上游链路会产生 error 级校验问题。
- 规则级业务例外白名单可抑制指定误报。
- 现有模板全部通过。
- 全量验收通过。

### ER-20260428-033 保护逻辑读写方向规则第一版

状态：已完成

修改人：Codex

关联文件：

- `rules/protection_logic.json`
- `app/services/validator.py`
- `tests/test_services.py`
- `knowledge/工程规范.md`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：ER-20260428-032 已补保护链路影响范围和例外白名单。阶段 6 后续要求继续补读写方向，避免把读信号当作输出点位，或把物理输入当作可被上游逻辑驱动的变量。

目标：先落地保守的读写方向规则第一版，只拦截明显错误并保证现有模板不误报。

代码/契约变化：

- `rules/protection_logic.json` 新增 `direction_rules`：
  - `source_input_types`：物理输入或订阅输入类型，当前为 `hwInput`、`mqttin`。
  - `physical_output_types`：物理输出类型，当前为 `hwOutput`。
  - `read_signal_keywords`：故障、反馈、状态、报警、防冻、急停、到位等读信号关键词。
  - 两类错误文案。
- `validator.py` 新增 `_validate_direction_rules`：
  - `input_signal_has_upstream_source`：物理输入或订阅输入存在上游源引用时报错。
  - `physical_output_looks_like_read_signal`：物理输出点位名称表达读信号时报错。
  - 两类规则均支持 `exceptions` 白名单。
- `tests/test_services.py` 新增 3 个测试：
  - 物理输入被上游常量驱动。
  - 物理输出命名为运行反馈。
  - 方向规则例外白名单。

影响：

- 可以拦截一类明显读写方向错误，避免保护输入或反馈状态被错误改成输出控制点。
- 当前未把 `modbusOutput`、`bacipOutput`、`mqttout` 纳入读信号命名阻断，因为现有模板存在用通讯输出发布故障状态的合法场景；后续需要按协议和点表方向继续细分。
- 现有 5 个模板全部通过。

验证：

```powershell
conda activate midea
pytest tests\test_services.py::test_validator_rejects_input_signal_with_upstream_source tests\test_services.py::test_validator_rejects_physical_output_named_like_read_signal tests\test_services.py::test_validator_allows_direction_rule_exception tests\test_services.py::test_existing_templates_validate_cleanly -q
```

结果：`4 passed`

```powershell
conda activate midea
python scripts\build_indexes.py
pytest -q
```

结果：模板 5、页面 31、节点 8413、功能块 129，`85 passed`

风险/回滚：

- 风险较低。规则限定在 `hwInput`、`mqttin` 和 `hwOutput`，避免误伤当前软件变量和通讯状态发布点。
- 回滚可移除 `direction_rules` 配置、`_validate_direction_rules` 和新增测试。

文档同步：

- README 已更新保护规则能力和测试数量。
- 项目结构已更新规则文件职责。
- 工程规范已补充读写方向规则。
- 演进记录已更新当前进度、变更索引和下次建议。

后续动作：

- 继续按协议细分通讯点位读写方向，例如 Modbus、BACnet/IP、MQTT 发布和订阅。
- 补更多设备类型保护链路规则，例如电加热、直膨机、水泵、主机和蝶阀。

DoD：

- 物理输入或订阅输入被上游驱动会产生 error 级校验问题。
- 物理输出命名为读信号会产生 error 级校验问题。
- 方向规则支持业务例外白名单。
- 现有模板全部通过。
- 全量验收通过。
- 全量索引构建和 pytest 通过。

### ER-20260428-034 校验报告 summary 和 suggestion

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `tests/test_services.py`
- `README.md`
- `文档/演进记录.md`

背景：阶段 6 要求校验报告具备更适合前端展示和导出阻塞判断的结构。此前报告已有 `error_count`、`warning_count`、`blocked_export_reasons`，但还缺统一 `summary` 和问题级修复建议。

目标：在保持旧字段兼容的前提下，为校验报告补充 `summary` 和每个 issue 的 `suggestion`。

代码/契约变化：

- `ValidationIssue` 新增 `suggestion` 字段。
- `_build_report()` 新增：
  - `summary.error_count`
  - `summary.warning_count`
  - `summary.risk_count`
- `_build_report()` 会为没有显式建议的问题按错误码填充默认修复建议。
- 保留旧字段 `error_count`、`warning_count`、`issue_count`、`blocked_export_reasons`，避免破坏现有 API 和测试。
- `tests/test_services.py` 新增校验报告结构测试。

影响：

- 前端和导出检查面板可直接使用 `summary` 展示统计。
- 校验问题可展示修复建议，减少只返回错误码的使用成本。

验证：

```powershell
conda activate midea
pytest tests\test_services.py::test_validator_report_has_summary_and_suggestions tests\test_services.py::test_existing_templates_validate_cleanly -q
```

结果：通过，包含现有模板回归。

风险/回滚：

- 风险较低。新增字段为兼容扩展，不移除旧字段。
- 回滚可移除 `suggestion` 字段、`summary` 生成和对应测试。

文档同步：

- README 已补充校验报告能力。
- 演进记录已更新当前进度和变更索引。

后续动作：

- 前端校验问题列表可优先显示 `summary` 和 `suggestion`。

DoD：

- 校验报告包含 `summary`。
- 每个 issue 包含非空 `suggestion`。
- 旧字段保持可用。
- 现有模板全部通过。

### ER-20260428-035 dry-run 变更风险校验

状态：已完成

修改人：Codex

关联文件：

- `app/services/validator.py`
- `app/services/patch_engine.py`
- `tests/test_services.py`
- `knowledge/工程规范.md`
- `README.md`
- `文档/项目结构.md`
- `文档/演进记录.md`

背景：阶段 6 的 L6 变更风险校验要求发现删除、断线、设备数量变化和保护逻辑影响范围。此前校验主要看最终工程 JSON，无法识别“最终结构仍合法，但 dry-run 断开了保护链路”的风险。

目标：把变更前后 diff 风险校验接入 patch dry-run，在保存前阻断保护节点删除、关键执行器删除、保护关键词移除和保护链路断线。

代码/契约变化：

- `validator.py` 新增 `validate_project_change(before_nodes, after_nodes)`：
  - `change_removed_protection_node`
  - `change_removed_critical_actuator`
  - `change_removed_protection_keyword`
  - `change_removed_protection_wire`
- `validator.py` 新增 `merge_validation_reports()`，用于合并最终工程校验和变更风险校验。
- `patch_engine.py` 的 `dry_run_patch()` 与 `apply_patch_to_project()` 合并：
  - `validate_project(after_nodes)`
  - `validate_project_change(before_nodes, after_nodes)`
- 变更风险问题计入 `summary.risk_count`。
- `tests/test_services.py` 新增保护链路断线和 dry-run 合并风险报告测试。

影响：

- `disconnect` 等操作如果断开保护信号或关键执行器上游保护链路，即使最终 JSON 基础结构合法，也会在 dry-run 阶段返回无效校验报告。
- 当前项目还没有 `delete_node` 执行器，但 L6 风险校验已能覆盖未来删除节点后的风险。

验证：

```powershell
conda activate midea
pytest tests\test_services.py::test_validator_change_rejects_removed_protection_wire tests\test_services.py::test_patch_engine_dry_run_includes_change_risk_validation tests\test_services.py::test_existing_templates_validate_cleanly -q
```

结果：通过，包含现有模板回归。

风险/回滚：

- 风险中等。L6 会让部分原本结构合法的断线 dry-run 变为无效，这是符合生产安全目标的行为变化。
- 回滚可让 `patch_engine.py` 只调用 `validate_project()`，并移除 `validate_project_change()` 及对应测试。

文档同步：

- README 已补充 dry-run 变更风险校验能力。
- 项目结构已补充校验器边界。
- 工程规范已补充 dry-run 风险要求。
- 演进记录已更新当前进度和变更索引。

后续动作：

- 如果新增 `delete_node` 或 `delete_block`，必须复用 `validate_project_change()` 覆盖删除保护逻辑风险。

DoD：

- 保护链路断线会产生 error 级变更风险问题。
- dry-run 合并变更风险校验。
- 变更风险计入 `summary.risk_count`。
- 现有模板全部通过。

### ER-20260428-036 设备级保护规则扩充

状态：已完成

修改人：Codex

关联文件：

- `rules/protection_logic.json`
- `app/services/validator.py`
- `tests/test_services.py`
- `README.md`
- `文档/演进记录.md`

背景：ER-20260428-030 到 ER-20260428-033 已建立保护规则、链路规则和读写方向规则，但设备级规则仍偏少。阶段 6 要求 AHU 和机房群控关键设备保护逻辑可维护扩充。

目标：补充低误报设备级规则，并保持现有模板全部通过。

代码/契约变化：

- `rules/protection_logic.json` 和默认规则新增：
  - `ahu_electric_heater_fault`：电加热相关逻辑缺少故障线索时报错。
  - `ahu_filter_alarm`：过滤/滤网相关逻辑缺少报警线索时报错。
  - `plant_bypass_valve_limit`：旁通阀相关逻辑缺少限幅、上限或下限线索时报错。
- `tests/test_services.py` 新增设备级保护缺口测试。

影响：

- 校验器能覆盖更多 AHU 和机房群控设备级保护缺口。
- 当前规则仍以文本线索为主；真实项目若存在特殊命名，应通过规则白名单或补充同义词解决。

验证：

```powershell
conda activate midea
pytest tests\test_services.py::test_validator_rejects_device_specific_protection_rule_gaps tests\test_services.py::test_existing_templates_validate_cleanly -q
```

结果：通过，包含现有模板回归。

```powershell
conda activate midea
python scripts\build_indexes.py
pytest -q
```

结果：模板 5、页面 31、节点 8413、功能块 129，`89 passed`

风险/回滚：

- 风险中等。设备规则依赖命名文本，后续真实模板扩展时可能需要增加同义词或白名单。
- 回滚可移除新增 3 条规则和对应测试。

文档同步：

- README 已更新保护规则能力和测试数量。
- 演进记录已更新当前进度、变更索引和下次建议。

后续动作：

- 阶段 6 当前已形成本地高质量闭环；后续规则扩充建议基于真实项目样例进行。
- 若继续推进生产化，优先转向阶段 7 前端校验报告 UI 或阶段 1 PostgreSQL 实例接入。

DoD：

- 电加热缺故障线索会产生 error 级问题。
- 过滤/滤网缺报警线索会产生 error 级问题。
- 旁通阀缺限幅/上下限线索会产生 error 级问题。
- 现有模板全部通过。
- 全量索引构建和 pytest 通过。

